---
phase: 02-e2e-critical-journeys
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - playwright.config.ts
  - e2e/tests/auth-onboarding.spec.ts
  - e2e/helpers/supabase-mocks.ts
autonomous: true

must_haves:
  truths:
    - "A new user can enter an access code, sign up, complete all 10 onboarding steps + evolution, and land on the home screen (E2E verified)"
    - "An existing user can sign in and see their seeded data on the home screen (E2E verified)"
  artifacts:
    - path: "playwright.config.ts"
      provides: "Second Playwright project 'chromium-auth' with webServer on port 5174 without VITE_DEV_BYPASS"
      contains: "chromium-auth"
    - path: "e2e/tests/auth-onboarding.spec.ts"
      provides: "E2E tests for access gate -> sign up -> onboarding -> home AND sign in -> home with data"
      min_lines: 80
    - path: "e2e/helpers/supabase-mocks.ts"
      provides: "Reusable Supabase auth mock setup for page.route()"
      min_lines: 30
  key_links:
    - from: "e2e/tests/auth-onboarding.spec.ts"
      to: "e2e/helpers/supabase-mocks.ts"
      via: "import mock setup functions"
      pattern: "import.*supabase-mocks"
    - from: "playwright.config.ts"
      to: "e2e/tests/auth-onboarding.spec.ts"
      via: "chromium-auth project testMatch pattern"
      pattern: "auth-onboarding"
---

<objective>
Write E2E tests for auth and onboarding journeys (E2E-05 and E2E-06). This requires a second Playwright project running a Vite dev server WITHOUT `VITE_DEV_BYPASS` so the AccessGate, Auth, and Onboarding screens render naturally. Supabase auth calls are mocked via `page.route()`.

Purpose: Verify the full new-user and returning-user auth flows work end-to-end -- the most critical paths for a launch to ~90k users with no beta phase.
Output: 2 passing E2E tests (sign up + sign in), Supabase mock helpers, updated Playwright config with auth project.
</objective>

<execution_context>
@/Users/ejasper/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ejasper/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-e2e-critical-journeys/02-RESEARCH.md
@.planning/phases/01-test-foundation/01-02-SUMMARY.md
@playwright.config.ts
@e2e/fixtures/base.ts
@e2e/helpers/storage.ts
@e2e/tests/smoke.spec.ts
@src/App.tsx
@src/screens/AccessGate.tsx
@src/screens/Auth.tsx
@src/screens/Onboarding.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add chromium-auth Playwright project with second Vite dev server</name>
  <files>playwright.config.ts, e2e/helpers/supabase-mocks.ts</files>
  <action>
  **playwright.config.ts changes:**

  1. Add a second project named `chromium-auth` alongside the existing `chromium` project:
     - `name: 'chromium-auth'`
     - `use: { ...devices['Desktop Chrome'] }`
     - `testMatch: /auth-onboarding\.spec\.ts/` -- only runs the auth test file
  2. Update the existing `chromium` project to EXCLUDE auth tests:
     - `testIgnore: /auth-onboarding\.spec\.ts/`
  3. Add a second `webServer` entry for the auth project. Playwright supports an array of webServer objects:
     - First entry (existing): port 5173, `env: { VITE_DEV_BYPASS: 'true' }` (for existing smoke + future core tests)
     - Second entry (new): `command: 'npx vite --port 5174'`, `url: 'http://localhost:5174'`, `reuseExistingServer: !process.env.CI`, `env: { VITE_SUPABASE_URL: 'http://fake-supabase.test', VITE_SUPABASE_ANON_KEY: 'fake-anon-key-for-e2e' }` -- NO VITE_DEV_BYPASS, WITH fake Supabase env vars so `isSupabaseConfigured` is true and auth form renders
  4. Set `baseURL: 'http://localhost:5174'` on the `chromium-auth` project's `use` config so auth tests hit the non-bypass server

  **e2e/helpers/supabase-mocks.ts:**

  Create a helper that sets up Supabase API mocks on a Page object. Export two functions:

  1. `mockSupabaseSignUp(page: Page)` -- intercepts `**/auth/v1/signup` POST with a 200 response containing a fake user object (`id: 'e2e-test-user-id'`, `email: 'e2e@test.com'`) and fake session (`access_token`, `refresh_token`, `expires_in: 3600`). Also intercepts `**/auth/v1/token?grant_type=refresh_token` with same session response. Also intercepts `**/auth/v1/user` GET to return the fake user.

  2. `mockSupabaseSignIn(page: Page)` -- intercepts `**/auth/v1/token?grant_type=password` POST with same success response format as signUp.

  Both functions should also mock `**/auth/v1/token?grant_type=refresh_token` and `**/rest/v1/**` (generic REST mock returning 200) to prevent any real Supabase calls from failing.

  Use exact code patterns from the research file's "Mock Supabase Auth Responses" section.
  </action>
  <verify>
  - `npx playwright test --list --project=chromium-auth` shows tests from auth-onboarding.spec.ts (file doesn't exist yet, but project should be listed)
  - `npx playwright test --list --project=chromium` does NOT show auth-onboarding tests
  - TypeScript: `npx tsc --noEmit -p e2e/tsconfig.json` passes (or check by opening supabase-mocks.ts)
  </verify>
  <done>
  Playwright config has two projects (chromium + chromium-auth), two webServers (ports 5173/5174), and supabase-mocks.ts exports mockSupabaseSignUp and mockSupabaseSignIn.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write auth and onboarding E2E tests (E2E-05, E2E-06)</name>
  <files>e2e/tests/auth-onboarding.spec.ts</files>
  <action>
  Create `e2e/tests/auth-onboarding.spec.ts` with TWO tests. These tests use base `@playwright/test` (NOT the seededPage fixture) because they must run through auth gates. Import `mockSupabaseSignUp`, `mockSupabaseSignIn` from `../helpers/supabase-mocks`. Import `seedStore`, `seedAllStores`, `STORE_KEYS` from `../helpers/storage`.

  **Test 1: E2E-05 -- access gate -> sign up -> onboarding -> home**

  ```
  test('new user: access gate -> sign up -> onboarding -> home', async ({ page }) => {
  ```

  Flow (follow research E2E-05 flow exactly):
  1. Set up Supabase mocks BEFORE navigation: `await mockSupabaseSignUp(page)`
  2. Navigate to `/` -- NO seeded data means `hasAccess === false`, AccessGate renders
  3. Wait for AccessGate: `await expect(page.locator('[data-testid="access-gate"]')).toBeVisible()`
  4. Enter access code (8+ chars): `await page.locator('[data-testid="access-code-input"]').fill('TESTCODE1234')`
  5. Click submit: `await page.locator('[data-testid="access-submit-button"]').click()`
  6. Wait for success modal with "Access Granted" or "Begin" button, then click "Begin" (use `page.getByRole('button', { name: /begin/i })`)
  7. Auth screen appears: `await expect(page.locator('[data-testid="auth-screen"]')).toBeVisible()`
  8. Auth defaults to signup mode. Fill email: `await page.locator('[data-testid="auth-email-input"]').fill('e2e@test.com')`
  9. Fill password: `await page.locator('[data-testid="auth-password-input"]').fill('TestPassword123!')`
  10. Fill confirm password: `await page.getByLabel(/confirm password/i).fill('TestPassword123!')`
  11. Click submit: `await page.locator('[data-testid="auth-submit-button"]').click()`
  12. After mock responds, Onboarding renders. Wait: `await expect(page.locator('[data-testid="onboarding-screen"]')).toBeVisible({ timeout: 10000 })`
  13. **Onboarding steps (10 steps + evolution):**
      - Step 1 (Welcome): Click "Start" or "Let's Go" or "Begin" -- find the CTA button via `page.getByRole('button')` with flexible name
      - Step 2 (Name): Fill `[data-testid="onboarding-username-input"]` with "E2ENewUser", click `[data-testid="onboarding-next-button"]` (text "Continue")
      - Step 3 (Gender): Click `[data-testid="onboarding-gender-male"]`, click Continue
      - Step 4 (Fitness): Click first fitness level option (use `page.getByRole('button').filter({ hasText: /beginner/i })` or similar), click Continue
      - Step 5 (Days): Click `[data-testid="onboarding-training-days-3"]`, click Continue
      - Step 6 (Schedule): Days auto-selected from step 5, click Continue
      - Step 7 (Goal): Fill `[data-testid="onboarding-weight-input"]` with "180", `[data-testid="onboarding-height-input"]` with "70", `[data-testid="onboarding-age-input"]` with "28", click `[data-testid="onboarding-goal-recomp"]`, click Continue
      - Step 8 (Avatar): Click first avatar option, click Continue
      - Step 9 (Features): Click Continue
      - Step 10 (Tutorial): Click "Begin" button
      - Evolution step: Wait for the "Begin" button to appear (1.5s JS timer for animation): `await expect(page.getByRole('button', { name: /begin/i })).toBeVisible({ timeout: 5000 })`, then click it
  14. Home screen appears: `await expect(page.locator('[data-testid="home-screen"]')).toBeVisible({ timeout: 10000 })`
  15. Assert username visible: `await expect(page.getByText('E2ENewUser')).toBeVisible()`
  16. Assert navigation visible: `await expect(page.locator('nav[aria-label="Main navigation"]')).toBeVisible()`

  **IMPORTANT NOTES for this test:**
  - Use generous timeouts (10s) on screen transitions -- lazy loading + Suspense boundaries add latency
  - The "Continue" button in onboarding may have different text depending on step. Use `[data-testid="onboarding-next-button"]` when available, otherwise use `page.getByRole('button', { name: /continue/i })`
  - If any step's Continue button is disabled (validation), fill required fields first
  - Use `{ timeout: 5000 }` for evolution step's Begin button (JS setTimeout delay)

  **Test 2: E2E-06 -- sign in -> home with existing data**

  ```
  test('existing user: sign in -> home with existing data', async ({ page }) => {
  ```

  Flow:
  1. Set up Supabase mocks: `await mockSupabaseSignIn(page)`
  2. Seed the access store with `hasAccess: true` so AccessGate is skipped: `await seedStore(page, STORE_KEYS.access, { hasAccess: true, licenseKey: 'TEST-E2E-KEY', accessGrantedAt: '2025-12-20T00:00:00.000Z', email: 'e2e@test.com', instanceId: 'e2e-instance-001' }, 2)`
  3. ALSO seed all other stores with full data (user, workouts, macros, xp, avatar, achievements, reminders) using `seedAllStores(page)` -- but this also seeds access. So either call `seedAllStores(page)` and it handles everything, OR call individual `seedStore` for each. The issue: `seedAllStores` also seeds user store with `onboardingComplete: true`, but without `VITE_DEV_BYPASS` the app checks `!user` (Supabase auth user) before checking onboarding. So even with all stores seeded, the app shows Auth screen because `user` is null (no Supabase session). This is actually what we want -- user sees Auth, signs in, mock returns success, then app checks profile and finds `onboardingComplete: true`, so it goes straight to Home.
  4. Seed all stores: call `seedAllStores(page)` (this seeds access with hasAccess: true + all other stores)
  5. Navigate to `/`
  6. App shows Auth screen (hasAccess passes, but Supabase user is null): `await expect(page.locator('[data-testid="auth-screen"]')).toBeVisible({ timeout: 10000 })`
  7. Switch to login mode: `await page.locator('[data-testid="auth-toggle-mode"]').click()`
  8. Fill email: `await page.locator('[data-testid="auth-email-input"]').fill('e2e@test.com')`
  9. Fill password: `await page.locator('[data-testid="auth-password-input"]').fill('TestPassword123!')`
  10. Click submit: `await page.locator('[data-testid="auth-submit-button"]').click()`
  11. After mock responds, app finds seeded profile with `onboardingComplete: true` -> Home renders
  12. Wait for Home: `await expect(page.locator('[data-testid="home-screen"]')).toBeVisible({ timeout: 10000 })`
  13. Assert seeded username visible: `await expect(page.getByText('E2ETestUser')).toBeVisible()`
  14. Assert XP display visible: `await expect(page.locator('[data-testid="home-xp-display"]')).toBeVisible()`
  15. Assert level display visible: `await expect(page.locator('[data-testid="home-level-display"]')).toBeVisible()`
  16. Assert streak display visible: `await expect(page.locator('[data-testid="home-streak-display"]')).toBeVisible()`

  **IMPORTANT NOTES for this test:**
  - `seedAllStores` must be called BEFORE `page.goto()` (it uses `page.addInitScript`)
  - The mock Supabase signIn must also mock `onAuthStateChange` or at minimum the token refresh endpoint, because the app calls `initializeAuth()` on load which triggers a session check
  - If the auth screen shows "Setup Required" instead of the form, it means the fake Supabase env vars aren't being picked up -- verify the chromium-auth project uses port 5174
  </action>
  <verify>
  Run auth tests only:
  ```
  npx playwright test --project=chromium-auth
  ```
  Both tests pass. Also verify existing smoke tests still pass:
  ```
  npx playwright test --project=chromium
  ```
  </verify>
  <done>
  Two E2E tests pass: (1) new user completes full access gate -> sign up -> onboarding -> home flow, (2) existing user signs in and sees seeded data on home screen. All 3 existing smoke tests still pass.
  </done>
</task>

</tasks>

<verification>
1. `npx playwright test --project=chromium-auth` -- both auth tests pass
2. `npx playwright test --project=chromium` -- 3 existing smoke tests still pass (not broken by config changes)
3. `npx playwright test` -- all 5 tests pass across both projects
4. `npx tsc --noEmit` -- zero type errors
</verification>

<success_criteria>
- E2E-05 PASS: Access gate -> sign up -> onboarding (all 10 steps + evolution) -> home screen verified
- E2E-06 PASS: Sign in -> home with existing seeded data verified (username, XP, level, streak visible)
- Existing smoke tests unaffected
- Zero type errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-e2e-critical-journeys/02-01-SUMMARY.md`
</output>
