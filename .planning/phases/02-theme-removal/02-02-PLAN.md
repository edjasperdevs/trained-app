---
phase: 02-theme-removal
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/screens/Workouts.tsx
  - src/screens/AccessGate.tsx
  - src/screens/CheckInModal.tsx
  - src/screens/XPClaimModal.tsx
  - src/screens/Settings.tsx
  - src/screens/AvatarScreen.tsx
  - src/screens/Home.tsx
  - src/screens/Onboarding.tsx
  - src/screens/Achievements.tsx
autonomous: true

must_haves:
  truths:
    - "No screen file imports useTheme from @/themes"
    - "No screen file references isTrained or themeId"
    - "All 9 screen files render only Trained-branch styling and copy"
    - "theme.labels.* references in screens are replaced with LABELS from src/design/constants.ts"
    - "theme.avatarStages references are replaced with AVATAR_STAGES from src/design/constants.ts"
    - "getStandingOrder is imported from src/design/constants.ts, not src/themes"
    - "Settings screen has no theme toggle UI element"
    - "App builds with zero TypeScript errors after changes"
  artifacts:
    - path: "src/screens/Settings.tsx"
      provides: "Settings without theme toggle section"
    - path: "src/screens/Home.tsx"
      provides: "Home screen importing getStandingOrder from @/design/constants"
    - path: "src/screens/Onboarding.tsx"
      provides: "Onboarding with zero isTrained ternaries (was 63)"
  key_links:
    - from: "src/screens/Home.tsx"
      to: "src/design/constants.ts"
      via: "import getStandingOrder from constants instead of themes"
      pattern: "import.*getStandingOrder.*from.*design/constants"
    - from: "src/screens/Settings.tsx"
      to: "N/A"
      via: "Theme toggle section completely removed"
      pattern: "No toggleTheme reference"
---

<objective>
De-branch all 9 screen files (323 isTrained/useTheme occurrences) and remove the theme toggle from Settings.

Purpose: Remove all conditional theme branching from every screen so the entire app renders only the Trained design path. After this plan, zero files in the codebase reference isTrained, useTheme, or theme.labels -- clearing the path for Plan 03 to safely delete the theme infrastructure.

Output: 9 de-branched screen files. Settings screen with theme toggle removed. Zero useTheme imports anywhere in src/screens/ or src/components/.
</objective>

<execution_context>
@/Users/ejasper/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ejasper/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/02-theme-removal/02-01-SUMMARY.md
@src/themes/trained.ts
@src/design/constants.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: De-branch lower-count screens (Workouts, AccessGate, CheckIn, Achievements)</name>
  <files>
    src/screens/Workouts.tsx
    src/screens/AccessGate.tsx
    src/screens/CheckInModal.tsx
    src/screens/Achievements.tsx
  </files>
  <action>
Apply the same de-branching pattern from Plan 01 to the 4 lower-count screen files. For each file:

**Standard de-branching steps (same as Plan 01):**
1. Remove `import { useTheme } from '@/themes'` (and `getStandingOrder` import from themes if present)
2. Remove `const { theme, themeId } = useTheme()` and `const isTrained = themeId === 'trained'`
3. Resolve every `isTrained ? X : Y` to Trained branch (X)
4. Resolve every `if (isTrained)` block to keep only the Trained path
5. Replace `theme.labels.*` with `LABELS.*` (import from `@/design/constants`)
6. Replace `theme.avatarStages[*]` with `AVATAR_STAGES[*]` (import from `@/design/constants`)
7. Remove dead GYG-only code paths

**File-specific notes:**

**Workouts.tsx (~14 occurrences):**
- Replace `theme.labels` references (xp, level, etc.) with LABELS
- Remove border-radius ternaries (keep Trained values)
- Remove GYG-specific styling alternatives
- Has `theme.labels.minimalWorkout` -- replace with `LABELS.minimalWorkout`

**AccessGate.tsx (~32 occurrences):**
- Remove GYG styling for the access gate form
- Keep Trained styling: dark, minimal, uppercase headings
- Remove GYG glassmorphism card styles
- Remove GYG-specific animations and transitions
- Keep border-left accent patterns

**CheckInModal.tsx (~23 occurrences):**
- Replace `theme.labels.checkIn` with `LABELS.checkIn`
- Replace `theme.labels.dailyQuests` with `LABELS.dailyQuests`
- Replace all other theme.labels references with LABELS
- Remove GYG styling for the modal and its internal components
- Keep Trained styling: minimal, structured, border-left accents
- If `getStandingOrder` is called, change import to `import { getStandingOrder, LABELS } from '@/design/constants'`

**Achievements.tsx (~8 occurrences):**
- Replace `theme.labels.achievements` with `LABELS.achievements`
- Remove border-radius ternaries
- Remove GYG styling alternatives
- Lowest count -- straightforward

After all 4 files: verify build.
  </action>
  <verify>
1. `npx tsc --noEmit` -- zero errors
2. `grep -r "useTheme\|isTrained\|themeId" src/screens/Workouts.tsx src/screens/AccessGate.tsx src/screens/CheckInModal.tsx src/screens/Achievements.tsx` -- zero matches
3. `grep -r "theme\.labels\|theme\.avatarStages" src/screens/Workouts.tsx src/screens/AccessGate.tsx src/screens/CheckInModal.tsx src/screens/Achievements.tsx` -- zero matches
  </verify>
  <done>4 screen files fully de-branched. Zero isTrained, useTheme, or theme.labels references remain in these files.</done>
</task>

<task type="auto">
  <name>Task 2: De-branch high-count screens (Home, Settings, XPClaim, AvatarScreen, Onboarding) and remove theme toggle</name>
  <files>
    src/screens/Home.tsx
    src/screens/Settings.tsx
    src/screens/XPClaimModal.tsx
    src/screens/AvatarScreen.tsx
    src/screens/Onboarding.tsx
  </files>
  <action>
Apply de-branching to the 5 highest-count screen files. Same standard steps as Task 1.

**File-specific notes:**

**Home.tsx (~45 occurrences):**
- Heavy use of `theme.labels.*` throughout -- replace all with `LABELS.*`
- Currently imports `getStandingOrder` from `@/themes` -- change to `import { getStandingOrder, LABELS } from '@/design/constants'`
- The `getStandingOrder(theme, 'training')` calls need to be simplified to `getStandingOrder('training')` (new signature from constants.ts takes only context, not theme)
- Remove GYG label alternatives throughout (e.g., "Sprint" -> "Weekly", "Deploy" -> "Claim")
- Remove GYG glassmorphism styling
- Keep Trained styling: uppercase headers, font-heading, tracking-wide, border-left accents

**Settings.tsx (~38 occurrences):**
- CRITICAL: Remove the entire theme toggle UI section. This includes:
  - The theme selection buttons/radio
  - The `toggleTheme` destructured from `useTheme()`
  - Any visual indicator of current theme
  - The "Theme" or "Appearance" section heading if it only contains the toggle
- Replace remaining `theme.labels.*` with `LABELS.*`
- Remove all GYG styling alternatives
- Keep Trained label text for settings items

**XPClaimModal.tsx (~38 occurrences):**
- Heavy use of `theme.labels.*` (xp, weeklyXPClaim, level, etc.) -- replace with LABELS
- Remove GYG confetti colors (keep Trained palette, same as BadgeUnlockModal in Plan 01)
- Remove GYG animations (bouncy avatar, glow effects)
- Keep Trained styling: restrained animations, uppercase text, minimal decoration
- If has `theme.avatarStages` references, replace with `AVATAR_STAGES`

**AvatarScreen.tsx (~33 occurrences):**
- Replace `theme.avatarStages` with `AVATAR_STAGES`
- Replace `theme.labels.*` with `LABELS.*`
- Remove GYG styling (rounded-full vs rounded-lg, glass effects)
- Keep Trained avatar shapes (rounded-lg) and restrained animations
- Remove GYG glow/pulse effects

**Onboarding.tsx (~63 occurrences -- highest in codebase):**
- This is the largest file. Work methodically top-to-bottom.
- Replace all `theme.labels.*` references with `LABELS.*`
- Replace `theme.avatarStages` with `AVATAR_STAGES`
- Remove GYG styling for every onboarding step/screen
- Keep Trained styling: uppercase headings, font-heading, minimal decoration
- Remove GYG character/icon alternatives
- Remove GYG copy text alternatives (keep Trained copy)
- Remove GYG layout alternatives
- Pay attention to the avatar class names: `theme.labels.avatarClasses.dominant` etc. -> `LABELS.avatarClasses.dominant`

After all 5 files: verify build.
  </action>
  <verify>
1. `npx tsc --noEmit` -- zero errors
2. `npm run build` -- succeeds
3. `grep -r "useTheme\|isTrained\|themeId" src/screens/ --include="*.tsx" -l` -- zero files (excluding index.ts barrel if it re-exports)
4. `grep -r "theme\.labels\|theme\.avatarStages\|getStandingOrder.*themes" src/screens/ --include="*.tsx" -l` -- zero files
5. `grep -r "toggleTheme" src/screens/Settings.tsx` -- zero matches (theme toggle removed)
6. `npm test -- --run` -- all existing tests pass
  </verify>
  <done>
All 9 screen files fully de-branched. Settings has no theme toggle. Zero isTrained, useTheme, theme.labels, or theme.avatarStages references remain in any screen file. The entire src/screens/ and src/components/ directories are theme-free.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. `npm run build` succeeds
3. `grep -r "useTheme\|isTrained\|themeId" src/screens/ src/components/ --include="*.tsx" --include="*.ts" -l` returns zero files (excluding index.ts barrels and test files)
4. `grep -r "theme\.labels\|theme\.avatarStages" src/screens/ src/components/ --include="*.tsx" -l` returns zero files
5. Settings.tsx has no theme toggle (grep for toggleTheme returns nothing)
6. All existing tests pass: `npm test -- --run`
</verification>

<success_criteria>
- 9 screen files de-branched: zero conditional theme logic remains
- Settings screen theme toggle completely removed
- All theme.labels/avatarStages references use LABELS/AVATAR_STAGES from src/design/constants.ts
- getStandingOrder imported from src/design/constants.ts (not src/themes)
- TypeScript compiles, build succeeds, tests pass
- Combined with Plan 01: zero files in src/screens/ or src/components/ reference the theme system
</success_criteria>

<output>
After completion, create `.planning/phases/02-theme-removal/02-02-SUMMARY.md`
</output>
