---
phase: 02-theme-removal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/design/constants.ts
  - src/components/Button.tsx
  - src/components/Card.tsx
  - src/components/Navigation.tsx
  - src/components/ProgressBar.tsx
  - src/components/Toast.tsx
  - src/components/XPDisplay.tsx
  - src/components/Avatar.tsx
  - src/components/StreakDisplay.tsx
  - src/components/ReminderCard.tsx
  - src/components/WeeklySummary.tsx
  - src/components/Badges.tsx
  - src/components/BadgeUnlockModal.tsx
autonomous: true

must_haves:
  truths:
    - "No component file imports useTheme from @/themes"
    - "No component file references isTrained or themeId"
    - "All 12 component files render only Trained-branch styling"
    - "theme.labels.* references in components are replaced with constants from src/design/constants.ts"
    - "App builds with zero TypeScript errors after changes"
  artifacts:
    - path: "src/design/constants.ts"
      provides: "Exported LABELS, AVATAR_STAGES, STANDING_ORDERS constants extracted from trainedTheme"
      exports: ["LABELS", "AVATAR_STAGES", "STANDING_ORDERS", "getStandingOrder"]
    - path: "src/components/Button.tsx"
      provides: "Button with only Trained styling path"
    - path: "src/components/Card.tsx"
      provides: "Card with only Trained styling path"
  key_links:
    - from: "src/design/constants.ts"
      to: "src/themes/trained.ts"
      via: "Values copied from trainedTheme (not imported -- decoupled)"
      pattern: "LABELS|AVATAR_STAGES|STANDING_ORDERS"
    - from: "src/components/XPDisplay.tsx"
      to: "src/design/constants.ts"
      via: "Direct import of LABELS constant"
      pattern: "import.*LABELS.*from.*design/constants"
---

<objective>
De-branch all 12 component files (128 isTrained/useTheme occurrences) and create the shared constants file that replaces theme.labels/avatarStages/standingOrders.

Purpose: Remove all conditional theme branching from primitive and composite components so they render only the Trained design path. Create src/design/constants.ts to hold label strings, avatar stage names, and standing orders that were previously accessed via useTheme().

Output: 12 de-branched component files + 1 new constants file. Zero useTheme imports in any component file.
</objective>

<execution_context>
@/Users/ejasper/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ejasper/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/themes/trained.ts
@src/themes/types.ts
@src/themes/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create src/design/constants.ts with theme-extracted values</name>
  <files>src/design/constants.ts</files>
  <action>
Create `src/design/constants.ts` that extracts and exports all non-CSS-token values from the Trained theme. These are values that components currently access via `theme.labels.*`, `theme.avatarStages`, and `theme.standingOrders`.

Export the following:

1. **LABELS** object (type-safe, matching ThemeLabels shape from src/themes/types.ts):
   - Copy all values from `trainedTheme.labels` in src/themes/trained.ts
   - Example: `xp: 'DP'`, `level: 'Rank'`, `streak: 'Obedience Streak'`, etc.
   - Include the nested `avatarClasses` object

2. **AVATAR_STAGES** array (string[]):
   - Copy the 13 stages from `trainedTheme.avatarStages`
   - ['Uninitiated', 'Novice', 'Trainee', 'Pledged', 'Collared', 'Devoted', 'Bound', 'Proven', 'Mastered', 'Dominant', 'Sovereign', 'Ascended', 'Unchained']

3. **STANDING_ORDERS** object (matching StandingOrderCategories shape):
   - Copy all 4 categories (discipline, selfCare, growth, reward) from `trainedTheme.standingOrders`

4. **getStandingOrder** function:
   - Move the `getStandingOrder` function from src/themes/index.ts into this file
   - Simplify signature: `getStandingOrder(context: 'training' | 'rest' | 'missed' | 'claim' | 'default' = 'default'): string`
   - It should reference the local STANDING_ORDERS constant instead of taking a theme parameter

Do NOT import from src/themes/ -- copy the values directly. This decouples the constants from the theme system so Plan 03 can delete themes without breaking anything.

Do NOT export types -- only export the runtime constants and the function.
  </action>
  <verify>
Run `npx tsc --noEmit` to confirm no type errors. Verify the file exists and exports LABELS, AVATAR_STAGES, STANDING_ORDERS, getStandingOrder.
  </verify>
  <done>src/design/constants.ts exists with all 4 exports. TypeScript compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: De-branch all 12 component files</name>
  <files>
    src/components/Button.tsx
    src/components/Card.tsx
    src/components/Navigation.tsx
    src/components/ProgressBar.tsx
    src/components/Toast.tsx
    src/components/XPDisplay.tsx
    src/components/Avatar.tsx
    src/components/StreakDisplay.tsx
    src/components/ReminderCard.tsx
    src/components/WeeklySummary.tsx
    src/components/Badges.tsx
    src/components/BadgeUnlockModal.tsx
  </files>
  <action>
For each of the 12 component files, apply the de-branching pattern:

**Step 1: Remove useTheme import and call**
- Delete `import { useTheme } from '@/themes'` (or from `'@/themes'`)
- Delete `const { themeId } = useTheme()` / `const { theme, themeId } = useTheme()` / `const { themeId } = useTheme()`
- Delete `const isTrained = themeId === 'trained'`

**Step 2: Resolve every isTrained ternary to the Trained branch**
- For `isTrained ? trainedValue : gygValue` -- keep `trainedValue`, delete `gygValue`
- For `!isTrained && ...` -- delete the entire expression
- For `if (isTrained) { ... } else { ... }` -- keep the if-block contents, delete else-block
- EXCEPTION: If GYG branch has the premium value (e.g., `rounded-2xl` vs `rounded-md`), evaluate which is correct for premium aesthetic. Card.tsx border radius: GYG has `rounded-2xl` but Trained has `rounded-md` -- keep `rounded-md` since premium design uses restrained radii (matching Phase 1 token decisions). Button.tsx: GYG has `rounded-xl` but Trained has `rounded` -- keep `rounded`.

**Step 3: Replace theme.labels.* with LABELS constant**
- Add `import { LABELS } from '@/design/constants'` where theme.labels was used
- Replace `theme.labels.xp` with `LABELS.xp`, `theme.labels.level` with `LABELS.level`, etc.
- Replace `theme.labels.achievements` with `LABELS.achievements`, etc.

**Step 4: Replace theme.avatarStages with AVATAR_STAGES constant**
- Add `import { AVATAR_STAGES } from '@/design/constants'` where theme.avatarStages was used
- Replace `theme.avatarStages[...]` with `AVATAR_STAGES[...]`

**Step 5: Simplify confetti hex arrays**
- BadgeUnlockModal.tsx: Remove the `isTrained ? trainedColors : gygColors` branching. Keep only the Trained confetti colors.
- The confetti color constant becomes a simple object (no branching):
  ```
  const confettiColors: Record<BadgeRarity, string[]> = {
    common: ['#4A4A4A', '#5C5C5C', '#3A3A3A'],
    rare: ['#3A5A7A', '#4A6A8A', '#2A4A6A'],
    epic: ['#8B1A1A', '#A52222', '#6B1010'],
    legendary: ['#8B6914', '#AB8924', '#6B5004']
  }
  ```

**Step 6: Clean up dead code**
- Remove any variables, functions, or constants that only existed for the GYG branch
- Remove GYG-specific class names (glass, glass-elevated, glass-subtle, glow-primary, text-glow-primary)
- Remove `RARITY_BG` (GYG version) from Badges.tsx -- keep only `TRAINED_RARITY_BG` and rename it to `RARITY_BG`
- Similarly in BadgeUnlockModal.tsx -- keep TRAINED_RARITY_BG/TRAINED_RARITY_TEXT, rename to RARITY_BG/RARITY_TEXT

**File-by-file notes (approximate isTrained count):**

1. **Button.tsx (4):** Remove GYG variant switch. Remove border-radius ternary. Keep Trained variant classes. Remove the TODO comment about Phase 3.
2. **Card.tsx (3):** Remove GYG glassmorphism variants. Remove border-radius ternary. Keep solid surface variants.
3. **Navigation.tsx (3):** Remove GYG indicator width/shadow. Remove uppercase ternary (keep `uppercase`). Keep `w-10` indicator.
4. **ProgressBar.tsx (3):** Remove gradient ternary (keep `from-primary to-primary-hover`). Remove border-radius ternary (keep `rounded-sm`).
5. **Toast.tsx (6):** Remove GYG colored-background styles. Remove position ternary (keep `bottom-20`). Remove animation direction ternary (keep `y: 20`). Remove border-radius ternary (keep `rounded`).
6. **XPDisplay.tsx (6):** Remove GYG text-glow. Remove glass ternary. Keep Trained styling. Replace `theme.labels.*` with `LABELS.*`.
7. **Avatar.tsx (8):** Remove GYG moodAnimations (keep trainedMoodAnimations, rename to moodAnimations). Remove GYG glow ring. Remove border-radius ternaries (keep `rounded-lg`). Replace `theme.avatarStages` with `AVATAR_STAGES`. Replace `theme.labels.level` with `LABELS.level`.
8. **StreakDisplay.tsx (13):** Remove GYG text variations ("Day Uptime" -> keep "Day"). Remove GYG shapes (keep `rounded-sm` squares). Remove border-radius ternaries. Resolve empty-day content (GYG shows '·', Trained shows '' -- keep '').
9. **ReminderCard.tsx (4):** Remove GYG gradient styles. Remove GYG pulse animation. Keep Trained border-left style.
10. **WeeklySummary.tsx (11):** Remove GYG label alternatives ("Sprint" -> keep "Weekly"). Remove GYG border-radius. Replace `theme.labels.*` with `LABELS.*`.
11. **Badges.tsx (21):** Remove GYG RARITY_BG. Rename TRAINED_RARITY_BG to RARITY_BG. Remove border-radius ternaries (keep `rounded`). Remove GYG text alternatives. Replace `theme.labels.*` with `LABELS.*`.
12. **BadgeUnlockModal.tsx (18):** Remove GYG confetti colors (keep Trained). Remove GYG glow ring for legendary/epic. Remove GYG RARITY_BG/RARITY_TEXT. Rename TRAINED_* variants. Remove border-radius ternaries. Remove GYG spin animation on badge reveal (keep `rotate: 0`). Replace `theme.labels.*` with `LABELS.*`.

After all files are done, verify the app still builds.
  </action>
  <verify>
1. Run `npx tsc --noEmit` -- zero errors
2. Run `grep -r "useTheme\|isTrained\|themeId" src/components/ --include="*.tsx" --include="*.ts" -l` -- should return zero files (excluding test files and index.ts barrel)
3. Run `grep -r "theme\.labels\|theme\.avatarStages" src/components/ --include="*.tsx" -l` -- should return zero files
4. Run `npm run build` -- build succeeds
  </verify>
  <done>
All 12 component files have zero isTrained ternaries, zero useTheme imports, and zero theme.labels references. All theme label/stage references use LABELS/AVATAR_STAGES from src/design/constants.ts. App builds cleanly.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. `npm run build` succeeds
3. No component file (src/components/*.tsx) contains `useTheme`, `isTrained`, or `themeId`
4. No component file contains `theme.labels` or `theme.avatarStages`
5. `src/design/constants.ts` exports LABELS, AVATAR_STAGES, STANDING_ORDERS, getStandingOrder
6. Existing tests pass: `npm test -- --run`
</verification>

<success_criteria>
- 12 component files de-branched: zero conditional theme logic remains
- 1 new constants file created with all label/stage/standing-order values
- TypeScript compiles, build succeeds, tests pass
- Components render only the Trained design path
</success_criteria>

<output>
After completion, create `.planning/phases/02-theme-removal/02-01-SUMMARY.md`
</output>
