---
phase: 21-archetypes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/design/constants.ts
  - src/stores/userStore.ts
  - src/components/ArchetypeSelector.tsx
  - src/components/ArchetypeCard.tsx
  - src/components/index.ts
  - src/screens/Onboarding.tsx
  - src/screens/Settings.tsx
autonomous: true
requirements: [GAME-03]

must_haves:
  truths:
    - "User sees 5 archetype options during onboarding (Bro, Himbo, Brute, Pup, Bull)"
    - "Bro is selectable by all users; Himbo/Brute/Pup/Bull show locked state for non-premium"
    - "User's selected archetype is persisted in profile state"
    - "User can view and change archetype from Settings screen"
  artifacts:
    - path: "src/design/constants.ts"
      provides: "ARCHETYPE_INFO and ARCHETYPE_MODIFIERS constants"
      contains: "ARCHETYPE_INFO"
    - path: "src/stores/userStore.ts"
      provides: "Archetype type and archetype field in UserProfile"
      contains: "archetype:"
    - path: "src/components/ArchetypeSelector.tsx"
      provides: "5-archetype selection UI with premium gating"
      exports: ["ArchetypeSelector"]
    - path: "src/components/ArchetypeCard.tsx"
      provides: "Individual archetype display with locked state"
      exports: ["ArchetypeCard"]
    - path: "src/screens/Onboarding.tsx"
      provides: "Archetype step in onboarding flow"
      contains: "archetype"
    - path: "src/screens/Settings.tsx"
      provides: "Archetype section for changing selection"
      contains: "ARCHETYPE"
  key_links:
    - from: "src/components/ArchetypeSelector.tsx"
      to: "src/stores/subscriptionStore.ts"
      via: "useSubscriptionStore().isPremium"
      pattern: "isPremium"
    - from: "src/screens/Onboarding.tsx"
      to: "src/components/ArchetypeSelector.tsx"
      via: "step === 'archetype'"
      pattern: "ArchetypeSelector"
    - from: "src/screens/Settings.tsx"
      to: "src/components/ArchetypeSelector.tsx"
      via: "archetype section"
      pattern: "ArchetypeSelector"
---

<objective>
Implement archetype type system, selection UI, and integration with onboarding and settings screens.

Purpose: Users can select their personal archetype during onboarding, with premium archetypes gated behind subscription. This is the foundation for personalized DP earning in Plan 02.

Output: Archetype types and constants, ArchetypeSelector/ArchetypeCard components, onboarding archetype step, Settings archetype section.
</objective>

<execution_context>
@/Users/ejasper/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ejasper/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-archetypes/21-RESEARCH.md

# Prior phase summaries for patterns
@.planning/phases/18-gamification-engine/18-01-SUMMARY.md
@.planning/phases/19-subscriptions/19-04-SUMMARY.md

# Source files to modify
@src/design/constants.ts
@src/stores/userStore.ts
@src/components/index.ts
@src/screens/Onboarding.tsx
@src/screens/Settings.tsx
@src/components/PremiumGate.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add archetype types and constants</name>
  <files>
    src/design/constants.ts
    src/stores/userStore.ts
  </files>
  <action>
1. In src/design/constants.ts, add archetype type and constants:
   - Export type `Archetype = 'bro' | 'himbo' | 'brute' | 'pup' | 'bull'`
   - Export `ARCHETYPE_INFO` record with name, tagline, icon (lucide icon name), isPremium, description, boosts for each archetype:
     * bro: "Bro", "Balanced Discipline", "User", false, generalist description, "No modifier bonuses"
     * himbo: "Himbo", "Training Obsessed", "Dumbbell", true, training-focused description, "+50% training DP"
     * brute: "Brute", "Nutrition Machine", "Beef", true, nutrition description, "+50% protein/meal DP"
     * pup: "Pup", "Lifestyle Master", "Heart", true, lifestyle description, "+100% steps/sleep DP"
     * bull: "Bull", "Consistency King", "TrendingUp", true, consistency description, "Streak bonuses (coming v2.1)"
   - Export `ARCHETYPE_MODIFIERS` record mapping archetype to partial Record of DPAction to multiplier:
     * bro: {} (no modifiers)
     * himbo: { training: 1.5 }
     * brute: { meal: 1.5, protein: 1.5 }
     * pup: { steps: 2.0, sleep: 2.0 }
     * bull: {} (deferred to v2.1)
   - Import DPAction type from stores/dpStore (use type-only import)

2. In src/stores/userStore.ts:
   - Import Archetype type from design/constants
   - Add `archetype: Archetype` to UserProfile interface
   - Add `archetype: 'bro'` to initialProfile default
   - Ensure setProfile action handles archetype field
  </action>
  <verify>
    <automated>cd /Users/ejasper/code/trained-app && npx tsc --noEmit && echo "TypeScript check passed"</automated>
  </verify>
  <done>ARCHETYPE_INFO, ARCHETYPE_MODIFIERS, and Archetype type exported from constants.ts. UserProfile has archetype field defaulting to 'bro'. TypeScript compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Create ArchetypeSelector and ArchetypeCard components</name>
  <files>
    src/components/ArchetypeCard.tsx
    src/components/ArchetypeSelector.tsx
    src/components/index.ts
  </files>
  <action>
1. Create src/components/ArchetypeCard.tsx:
   - Props: archetype: Archetype, selected: boolean, locked: boolean, onSelect: () => void
   - Display archetype icon from lucide-react (use dynamic lookup or switch statement)
   - Show name, tagline, boosts text from ARCHETYPE_INFO
   - Show "Premium" badge for isPremium archetypes
   - Show Lock icon overlay when locked=true
   - Use cn() for conditional styling:
     * selected: border-primary
     * locked: opacity-60, cursor-not-allowed
     * default: hover:bg-muted/50
   - Button element with onClick={onSelect}, disabled={locked}
   - Style: p-4 rounded-xl border-2 bg-card

2. Create src/components/ArchetypeSelector.tsx:
   - Props: selected: Archetype, isPremium: boolean, onSelect: (archetype: Archetype) => void
   - Render all 5 archetypes in order: bro, himbo, brute, pup, bull
   - Determine locked state: isLocked = ARCHETYPE_INFO[archetype].isPremium && !isPremium
   - Map to ArchetypeCard components with appropriate props
   - Container: div with space-y-3

3. Export both components from src/components/index.ts barrel file.
  </action>
  <verify>
    <automated>cd /Users/ejasper/code/trained-app && npx tsc --noEmit && echo "TypeScript check passed"</automated>
  </verify>
  <done>ArchetypeCard and ArchetypeSelector components created and exported. Premium archetypes show locked state when isPremium=false. TypeScript compiles.</done>
</task>

<task type="auto">
  <name>Task 3: Integrate archetype selection into Onboarding and Settings</name>
  <files>
    src/screens/Onboarding.tsx
    src/screens/Settings.tsx
  </files>
  <action>
1. In src/screens/Onboarding.tsx:
   - Add 'archetype' to Step type union
   - Add 'archetype' to steps array after 'features' (before 'tutorial')
   - Add archetype field to OnboardingData interface and default value ('bro')
   - Create ArchetypeStep component:
     * Props: value: Archetype, isPremium: boolean, onChange, onNext, onBack
     * Heading: "Choose Your Archetype"
     * Subtext: "Your archetype determines how you earn bonus DP."
     * Render ArchetypeSelector with value/isPremium/onSelect
     * Back and Continue buttons
   - Read isPremium from useSubscriptionStore() for ArchetypeStep
   - In finishOnboarding, pass archetype to initProfile or setProfile
   - Ensure archetype is included in profile initialization

2. In src/screens/Settings.tsx:
   - Import ArchetypeSelector, ARCHETYPE_INFO from design/constants
   - Import useSubscriptionStore for isPremium check
   - Add ARCHETYPE section after existing profile sections (before NOTIFICATIONS or DATA MANAGEMENT):
     * Section header: "ARCHETYPE" (same style as other section headers)
     * ArchetypeSelector component with:
       - selected={profile?.archetype || 'bro'}
       - isPremium={isPremium}
       - onSelect={(archetype) => { setProfile({ archetype }); toast.success(`Archetype changed to ${ARCHETYPE_INFO[archetype].name}`) }}
     * If archetype !== 'bro', show current bonus in a highlight box: "Active Bonus: {boosts}"
   - Wrap in Card/CardContent like other sections
  </action>
  <verify>
    <automated>cd /Users/ejasper/code/trained-app && npx tsc --noEmit && npm run lint 2>/dev/null || true && echo "Verification complete"</automated>
    <manual>Run app locally, complete onboarding to see archetype step, verify Settings shows archetype section</manual>
  </verify>
  <done>Onboarding includes archetype step after features. Settings has archetype section. Premium archetypes locked for non-subscribers. Selected archetype persisted to profile.</done>
</task>

</tasks>

<verification>
Overall phase checks:
1. TypeScript compiles with `tsc --noEmit`
2. Onboarding flow includes archetype selection step
3. Settings displays archetype section with change capability
4. Premium archetypes (Himbo, Brute, Pup, Bull) show locked state for non-premium users
5. Bro archetype always selectable
6. Selected archetype persists in userStore profile
</verification>

<success_criteria>
- [ ] ARCHETYPE_INFO and ARCHETYPE_MODIFIERS exported from constants.ts
- [ ] Archetype type with 5 values (bro, himbo, brute, pup, bull)
- [ ] UserProfile includes archetype field defaulting to 'bro'
- [ ] ArchetypeCard component shows name, tagline, boosts, premium badge, lock state
- [ ] ArchetypeSelector renders 5 archetypes with premium gating
- [ ] Onboarding has archetype step after features
- [ ] Settings has archetype section with change functionality
- [ ] TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/21-archetypes/21-01-SUMMARY.md`
</output>
