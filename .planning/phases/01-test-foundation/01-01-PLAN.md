---
phase: 01-test-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/Button.test.tsx
  - src/components/Card.test.tsx
autonomous: true

must_haves:
  truths:
    - "`vitest run` passes with zero test failures"
    - "`tsc --noEmit` passes with zero type errors"
    - "Store tests (workoutStore, macroStore, xpStore) continue to pass unchanged"
  artifacts:
    - path: "src/components/Button.test.tsx"
      provides: "Updated Button component tests matching post-shadcn class names"
    - path: "src/components/Card.test.tsx"
      provides: "Updated Card component tests matching post-shadcn class names"
  key_links:
    - from: "src/components/Button.test.tsx"
      to: "src/components/Button.tsx"
      via: "CVA variant class assertions"
      pattern: "bg-card|bg-primary|border"
    - from: "src/components/Card.test.tsx"
      to: "src/components/Card.tsx"
      via: "CVA variant class assertions"
      pattern: "bg-card|bg-muted|bg-card/50"
---

<objective>
Repair the 4 failing component tests so `vitest run` passes with zero failures and `tsc --noEmit` confirms zero type errors.

Purpose: Establish a green baseline before adding Playwright infrastructure. Broken tests erode confidence and block CI gating.

Output: All 139 tests pass (135 store + 4 fixed component tests). TypeScript compiles cleanly.
</objective>

<execution_context>
@/Users/ejasper/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ejasper/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

The shadcn/ui migration changed CSS class names used by Button and Card components. Tests assert old class names that no longer exist.

Specific failures (4 tests):
1. Button.test.tsx: "should apply ghost variant styles" -- expects `bg-surface`, actual is `bg-card`
2. Card.test.tsx: "should apply default variant styles" -- expects `bg-surface`, actual is `bg-card`
3. Card.test.tsx: "should apply elevated variant" -- expects `bg-surface-elevated`, actual is `bg-muted`
4. Card.test.tsx: "should apply subtle variant" -- expects `bg-surface/50`, actual is `bg-card/50`

Current component implementations (source of truth for correct class names):
- Button.tsx ghost variant: `bg-card text-foreground hover:bg-muted border border-border`
- Card.tsx default variant: `bg-card border border-border`
- Card.tsx elevated variant: `bg-muted border border-border shadow-card`
- Card.tsx subtle variant: `bg-card/50 border border-border/50`

Store tests (workoutStore, macroStore, xpStore) all pass -- do NOT modify them.
TypeScript already compiles cleanly -- just verify it stays clean.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix failing component test assertions to match post-shadcn class names</name>
  <files>src/components/Button.test.tsx, src/components/Card.test.tsx</files>
  <action>
  Update the 4 failing test assertions to match the actual CSS class names from the post-shadcn/ui migration components:

  In `src/components/Button.test.tsx`:
  - Line 47: Change `'bg-surface'` to `'bg-card'` (ghost variant now uses `bg-card`)

  In `src/components/Card.test.tsx`:
  - Line 36: Change `'bg-surface'` to `'bg-card'` (default variant now uses `bg-card`)
  - Line 44: Change `'bg-surface-elevated'` to `'bg-muted'` (elevated variant now uses `bg-muted`)
  - Line 51: Change `'bg-surface/50'` to `'bg-card/50'` (subtle variant now uses `bg-card/50`)

  Also update the comments on those lines to reflect the new theme token names (e.g., "Trained theme uses bg-card for ghost" instead of "bg-surface for ghost").

  Do NOT modify any other tests. Do NOT change any component source files.
  </action>
  <verify>
  Run `npx vitest run` and confirm:
  - 0 failures
  - All 139 tests pass (135 store + 14 Button + 16 Card + ProgressBar tests, with previous 4 failures now fixed)

  Run `npx tsc --noEmit` and confirm:
  - Zero type errors
  </verify>
  <done>
  `vitest run` exits with 0 failures. `tsc --noEmit` exits with 0 errors. All 6 test files pass. Store tests are untouched and still green.
  </done>
</task>

</tasks>

<verification>
1. `npx vitest run` -- exits 0, all tests pass
2. `npx tsc --noEmit` -- exits 0, no type errors
3. `git diff src/stores/` -- shows NO changes (store tests untouched)
</verification>

<success_criteria>
- Zero test failures in `vitest run`
- Zero type errors in `tsc --noEmit`
- Only Button.test.tsx and Card.test.tsx modified (minimal diff)
- Store tests (workoutStore, macroStore, xpStore) untouched and passing
</success_criteria>

<output>
After completion, create `.planning/phases/01-test-foundation/01-01-SUMMARY.md`
</output>
