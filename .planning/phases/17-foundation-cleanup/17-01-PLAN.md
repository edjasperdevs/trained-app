---
phase: 17-foundation-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/screens/Coach.tsx
  - src/hooks/useClientRoster.ts
  - src/hooks/useClientDetails.ts
  - src/hooks/useCoachTemplates.ts
  - src/components/WorkoutAssigner.tsx
  - src/components/ClientMacroAdherence.tsx
  - src/components/ClientActivityFeed.tsx
  - src/screens/index.ts
  - src/components/index.ts
  - src/App.tsx
  - src/lib/supabase.ts
  - src/lib/index.ts
  - src/screens/Settings.tsx
  - src/components/Navigation.tsx
  - src/lib/analytics.ts
  - src/lib/devSeed.ts
  - src/lib/badge.ts
  - src/hooks/useWeeklyCheckins.ts
  - src/screens/Home.tsx
autonomous: true
requirements:
  - STRIP-01
  - STRIP-02
  - STRIP-03
  - STRIP-04
  - STRIP-05
  - STRIP-06
  - STRIP-07

must_haves:
  truths:
    - "The /coach route no longer exists -- navigating to it shows 404 or redirects"
    - "Coach.tsx, 3 coach-only hooks, and 3 coach-only components are deleted from the filesystem"
    - "isCoach() helper no longer exists in supabase.ts or lib/index.ts"
    - "Settings screen has no Coach Dashboard card or isCoach state"
    - "Navigation component does not check for /coach path"
    - "pullCoachData() still works in sync.ts -- not removed"
    - "Assigned workout display and 'Assigned by Coach' badge still render in Workouts.tsx"
    - "Weekly check-in submission flow still works at /checkin route"
    - "set_by:'coach' macro target handling is preserved in macroStore and sync.ts"
    - "tsc --noEmit passes with zero errors"
  artifacts:
    - path: "src/screens/Coach.tsx"
      provides: "DELETED"
    - path: "src/hooks/useClientRoster.ts"
      provides: "DELETED"
    - path: "src/hooks/useClientDetails.ts"
      provides: "DELETED"
    - path: "src/hooks/useCoachTemplates.ts"
      provides: "DELETED"
    - path: "src/components/WorkoutAssigner.tsx"
      provides: "DELETED"
    - path: "src/components/ClientMacroAdherence.tsx"
      provides: "DELETED"
    - path: "src/components/ClientActivityFeed.tsx"
      provides: "DELETED"
    - path: "src/App.tsx"
      provides: "No CoachGuard, no /coach route, no Coach lazy import, pullCoachData preserved"
    - path: "src/hooks/useWeeklyCheckins.ts"
      provides: "Client functions preserved, coach functions removed"
    - path: "src/screens/Home.tsx"
      provides: "Coach response modal removed, hasCoach + weeklyCheckinDue preserved"
  key_links:
    - from: "src/App.tsx"
      to: "src/lib/sync.ts:pullCoachData"
      via: "import + online/visibility/foreground handlers"
      pattern: "pullCoachData"
    - from: "src/screens/Workouts.tsx"
      to: "src/stores/workoutStore.ts:assignedWorkout"
      via: "store hook"
      pattern: "assignedWorkout|hasAssignment"
    - from: "src/hooks/useWeeklyCheckins.ts"
      to: "supabase weekly_checkins table"
      via: "submitCheckin + fetchMyCheckins"
      pattern: "submitCheckin|fetchMyCheckins"
---

<objective>
Strip all coach DASHBOARD code from the codebase while preserving client-side coach data flows (pullCoachData, assigned workouts, weekly check-ins, coach-set macros).

Purpose: Remove ~4000+ lines of coach dashboard code that now lives in the separate welltrained-coach app, creating a clean foundation for V2 development. The coach DASHBOARD is removed but client-facing coach features remain intact.

Output: Clean TypeScript compilation with zero coach dashboard artifacts, all client coach flows preserved.
</objective>

<execution_context>
@/Users/ejasper/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ejasper/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-foundation-cleanup/17-RESEARCH.md

Key source files to understand before modifying:
@src/App.tsx
@src/lib/sync.ts
@src/lib/supabase.ts
@src/screens/Settings.tsx
@src/components/Navigation.tsx
@src/hooks/useWeeklyCheckins.ts
@src/screens/Home.tsx
@src/lib/devSeed.ts
@src/lib/analytics.ts
@src/lib/badge.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete coach-only files and fix all broken imports/exports</name>
  <files>
    src/screens/Coach.tsx (DELETE)
    src/hooks/useClientRoster.ts (DELETE)
    src/hooks/useClientDetails.ts (DELETE)
    src/hooks/useCoachTemplates.ts (DELETE)
    src/components/WorkoutAssigner.tsx (DELETE)
    src/components/ClientMacroAdherence.tsx (DELETE)
    src/components/ClientActivityFeed.tsx (DELETE)
    src/screens/index.ts
    src/components/index.ts
    src/App.tsx
    src/lib/supabase.ts
    src/lib/index.ts
  </files>
  <action>
    **Step 1 — Delete 7 coach-only files:**
    Delete these files entirely (they are coach dashboard code with no client usage):
    - `src/screens/Coach.tsx` (~2158 lines, the entire coach dashboard screen)
    - `src/hooks/useClientRoster.ts` (coach-only roster hook)
    - `src/hooks/useClientDetails.ts` (coach-only client detail hook)
    - `src/hooks/useCoachTemplates.ts` (coach-only template hook)
    - `src/components/WorkoutAssigner.tsx` (coach-only assignment component)
    - `src/components/ClientMacroAdherence.tsx` (coach-only adherence component)
    - `src/components/ClientActivityFeed.tsx` (coach-only activity feed component)

    **Step 2 — Fix barrel exports referencing deleted files:**
    - `src/screens/index.ts`: Remove the `Coach` export line
    - `src/components/index.ts`: Remove `ClientMacroAdherence` and `ClientActivityFeed` export lines. Also remove `WorkoutAssigner` if exported.

    **Step 3 — Clean App.tsx (the biggest change):**
    Read App.tsx carefully first. Remove these specific items:
    - The `import { isCoach } from '@/lib/supabase'` (or from `'@/lib'`)
    - The `CoachGuard` function definition (inline component, approximately lines 23-33)
    - The `const Coach = lazy(() => import('./screens/Coach'))` lazy import
    - The `/coach` route in the UNAUTHENTICATED routes section (pre-auth coach route)
    - The `const isCoachRoute = location.pathname === '/coach'` variable
    - The `isCoachRoute` bypass in the onboarding completion check (the condition that lets coach route skip onboarding)
    - The `/coach` route in the AUTHENTICATED routes section (the main coach route wrapped in CoachGuard)

    **CRITICAL PRESERVATIONS in App.tsx — DO NOT REMOVE:**
    - `import { pullCoachData } from '@/lib/sync'` — KEEP (STRIP-03)
    - All `pullCoachData()` calls in handleOnline, handleVisibilityChange, appStateChange handlers — KEEP
    - The `/checkin` route — KEEP (STRIP-05)
    - All sync-related imports and calls — KEEP

    **Step 4 — Remove isCoach() from supabase.ts and lib/index.ts:**
    - `src/lib/supabase.ts`: Delete the `isCoach()` function (approximately lines 43-56). Keep everything else (getSupabaseClient, getLoggedInUser, etc.).
    - `src/lib/index.ts`: Remove the `isCoach` export. Keep all other exports.

    **Step 5 — Run `tsc --noEmit`:**
    Run TypeScript compilation check. It should pass. If it fails, trace the errors and fix any remaining references to deleted files/exports. Common issues:
    - Types from deleted hooks still imported somewhere
    - WorkoutAssigner imported by a file other than Coach.tsx (check grep)
    - isCoach imported somewhere besides App.tsx and Settings.tsx

    Do NOT proceed to Task 2 until `tsc --noEmit` passes cleanly.
  </action>
  <verify>
    <automated>cd /Users/ejasper/code/trained-app && npx tsc --noEmit 2>&1 | head -50</automated>
    <manual>Verify the 7 coach-only files no longer exist on disk. Verify App.tsx has no /coach route.</manual>
  </verify>
  <done>
    Seven coach-only files deleted. All barrel exports cleaned. App.tsx has no CoachGuard, no /coach route, no Coach lazy import. isCoach() removed from supabase.ts and lib/index.ts. pullCoachData and /checkin route preserved. tsc --noEmit passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Clean coach references from shared files and trim useWeeklyCheckins</name>
  <files>
    src/screens/Settings.tsx
    src/components/Navigation.tsx
    src/lib/analytics.ts
    src/lib/devSeed.ts
    src/lib/badge.ts
    src/hooks/useWeeklyCheckins.ts
    src/screens/Home.tsx
  </files>
  <action>
    **Step 1 — Clean Settings.tsx:**
    Read Settings.tsx. Remove:
    - The `import { isCoach as checkIsCoach ... }` import (or adjust to keep other imports from same line like `getSupabaseClient`)
    - The `const [isCoach, setIsCoach] = useState(false)` state
    - The `checkIsCoach().then(setIsCoach)` call inside useEffect
    - The entire Coach Dashboard card block (lines ~912-929): `{isCoach && ( <Card>...</Card> )}`

    **PRESERVE in Settings.tsx:**
    - `hasCoach` state and the `coach_clients` check — needed for weekly check-in notification visibility
    - All other Settings functionality (profile, notifications, data export, etc.)

    **Step 2 — Clean Navigation.tsx:**
    Read Navigation.tsx. Remove the check that returns `null` when `location.pathname === '/coach'` (approximately line 23). The Navigation component should always render for authenticated users now.

    **Step 3 — Clean analytics.ts:**
    Read analytics.ts. Remove the `coachDashboardViewed` event method. Keep all other analytics events.

    **Step 4 — Clean devSeed.ts:**
    Read devSeed.ts. Remove mock data functions that are ONLY used by deleted coach hooks:
    - `getMockClients` / mock client roster data (used by useClientRoster — deleted)
    - `mockWorkoutTemplates` (used by useCoachTemplates — deleted)
    - `mockAssignedWorkouts` for coach view (used by useClientDetails — deleted)
    - Coach-specific mock check-in data that is only consumed by coach functions

    **PRESERVE in devSeed.ts:**
    - Mock data used by preserved client-side code (e.g., mock weekly check-in data used by useWeeklyCheckins client functions)
    - All other mock data (profiles, workouts, macros, etc.)

    Before deleting anything from devSeed.ts, use grep to verify which functions are imported where. Only remove functions with zero remaining importers after coach file deletion.

    **Step 5 — Clean badge.ts:**
    Read badge.ts. Remove any coach-specific badge logic (e.g., coach response badge computation). Keep client-facing badge logic.

    **Step 6 — Trim useWeeklyCheckins.ts:**
    Read useWeeklyCheckins.ts carefully. Remove COACH-SIDE functions only:
    - `fetchPendingCheckins` function
    - `fetchClientCheckins` function
    - `submitReview` function
    - `PendingCheckin` interface (if only used by coach functions)
    - `pendingCheckins` state variable
    - `clientCheckins` state variable

    **PRESERVE in useWeeklyCheckins.ts (STRIP-05):**
    - `submitCheckin` function
    - `fetchMyCheckins` function
    - `hasCheckinForCurrentWeek` function
    - `isCoachingClient` function
    - `myCheckins` state
    - All types used by preserved functions (WeeklyCheckin, etc.)

    Update the return object to only export preserved functions and state.

    **Step 7 — Clean Home.tsx:**
    Read Home.tsx. Remove:
    - `showCoachResponse` state and its setter
    - Coach response modal JSX (the modal that shows coach's check-in review response)
    - `latestCheckinInfo` reading from localStorage for coach response detection
    - `hasCoachResponse` logic that triggers the coach response banner

    **PRESERVE in Home.tsx:**
    - `hasCoach` state (needed for weekly check-in banner visibility)
    - `weeklyCheckinDue` state and the check-in due banner
    - The weekly check-in banner that links to /checkin

    **Step 8 — Run `tsc --noEmit`:**
    Must pass cleanly. Fix any remaining type errors.

    **Step 9 — Run `npm run build`:**
    Verify the production build succeeds. This catches any issues that tsc alone might miss (Vite bundling, CSS, etc.).
  </action>
  <verify>
    <automated>cd /Users/ejasper/code/trained-app && npx tsc --noEmit && npm run build 2>&1 | tail -20</automated>
    <manual>Verify Settings has no Coach Dashboard card. Verify /checkin route still exists in App.tsx. Verify useWeeklyCheckins exports only client functions.</manual>
  </verify>
  <done>
    All coach references removed from shared files. Settings has no coach dashboard card. Navigation renders on all routes. Analytics has no coachDashboardViewed. devSeed has no coach-only mock data. useWeeklyCheckins trimmed to client-only functions. Home.tsx has no coach response modal but retains check-in due banner. tsc --noEmit and npm run build both pass.
  </done>
</task>

</tasks>

<verification>
1. `tsc --noEmit` passes with zero errors
2. `npm run build` succeeds
3. `grep -r 'Coach.tsx\|CoachGuard\|isCoach\|useClientRoster\|useClientDetails\|useCoachTemplates\|WorkoutAssigner\|ClientMacroAdherence\|ClientActivityFeed' src/ --include='*.ts' --include='*.tsx' | grep -v 'node_modules'` returns zero results (except comments)
4. `grep -r 'pullCoachData' src/` confirms pullCoachData is still referenced in sync.ts and App.tsx
5. `grep -r 'assignedWorkout\|hasAssignment' src/` confirms assigned workout logic preserved in workoutStore and Workouts.tsx
6. `grep -r '/checkin' src/App.tsx` confirms the check-in route exists
7. `grep -r "set_by.*coach\|setBy.*coach\|coach.*macros" src/stores/macroStore.ts src/lib/sync.ts` confirms coach macro handling preserved
</verification>

<success_criteria>
- Zero coach dashboard files remain in the codebase
- Zero references to deleted files/exports in any remaining file
- pullCoachData(), assigned workout display, weekly check-in submission, and coach-set macro handling all preserved
- TypeScript compiles cleanly and production build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/17-foundation-cleanup/17-01-SUMMARY.md`
</output>
