---
phase: 17-foundation-cleanup
plan: 02
type: execute
wave: 2
depends_on:
  - 17-01
files_modified:
  - src/index.css
  - index.html
autonomous: false
requirements:
  - DESIGN-01
  - DESIGN-02
  - DESIGN-03
  - DESIGN-04
  - DESIGN-05
  - DESIGN-06

must_haves:
  truths:
    - "All screens use lime (#C8FF00) signal color instead of red (#D55550)"
    - "Primary CTAs have lime background with dark text (#0A0A0A) and 0.75rem border radius"
    - "Bottom navigation icons are Muted (#A1A1AA) by default and lime (#C8FF00) when active"
    - "Progress bars use lime fill on Surface (#26282B) track"
    - "Cards use Surface (#26282B) background with no heavy shadows"
    - "Typography hierarchy uses Oswald (display/stats), Inter (body/UI), JetBrains Mono (data/numbers) — already in place, verify unchanged"
    - "Destructive actions use #B91C1C, visually distinct from the new lime primary"
    - "No hard-coded red hex values (#D55550, #E0605A, #d55550, #e0605a) remain in the codebase"
  artifacts:
    - path: "src/index.css"
      provides: "All Dopamine Noir V2 color tokens in :root and @theme blocks"
      contains: "--primary: #C8FF00"
    - path: "index.html"
      provides: "Updated mask-icon color matching V2 brand"
  key_links:
    - from: "src/index.css :root"
      to: "Tailwind utility classes throughout all components"
      via: "CSS custom properties consumed by @theme"
      pattern: "--primary.*#C8FF00"
    - from: "src/index.css @theme"
      to: "Component Tailwind classes (bg-primary, text-primary, etc.)"
      via: "Tailwind 4 @theme directive"
      pattern: "--color-primary.*#C8FF00"
---

<objective>
Migrate the entire app from the red (#D55550) color system to the Dopamine Noir V2 lime (#C8FF00) color system by updating CSS custom properties and fixing any hard-coded color values.

Purpose: Establish the V2 visual identity so every subsequent phase builds on the correct design tokens. The lime signal color, darker surface values, and brighter foreground text define the Dopamine Noir V2 brand.

Output: Every screen in the app renders with V2 colors. No red primary artifacts remain.
</objective>

<execution_context>
@/Users/ejasper/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ejasper/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-foundation-cleanup/17-RESEARCH.md
@.planning/phases/17-foundation-cleanup/17-01-SUMMARY.md

Key source files:
@src/index.css
@index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CSS tokens to Dopamine Noir V2 and fix hard-coded colors</name>
  <files>
    src/index.css
    index.html
  </files>
  <action>
    **Step 1 — Update :root CSS variables in index.css:**
    Read `src/index.css`. Find the `:root` block and update these values:

    ```
    --radius:              0.625rem  →  0.75rem
    --background:          #0A0A0A   →  #0A0A0A  (no change)
    --foreground:          #E8E8E8   →  #FAFAFA
    --card:                #141414   →  #26282B
    --card-foreground:     #E8E8E8   →  #FAFAFA
    --popover:             #1C1C1C   →  #26282B
    --popover-foreground:  #E8E8E8   →  #FAFAFA
    --primary:             #D55550   →  #C8FF00
    --primary-foreground:  #FFFFFF   →  #0A0A0A
    --secondary:           #4A4A4A   →  #26282B
    --secondary-foreground:#E8E8E8   →  #FAFAFA
    --muted:               #1C1C1C   →  #26282B
    --muted-foreground:    #888888   →  #A1A1AA
    --accent:              #1C1C1C   →  #26282B
    --accent-foreground:   #E8E8E8   →  #FAFAFA
    --destructive:         #D55550   →  #B91C1C
    --border:              #2A2A2A   →  #2A2A2A  (keep distinct from card for visual separation — research open question #3)
    --input:               #2A2A2A   →  #2A2A2A  (match border)
    --ring:                #D55550   →  #C8FF00
    ```

    Also update chart colors in :root:
    - `--chart-1` through `--chart-5`: replace any red (#D55550) references with lime-derived values. Use a palette that works with lime: #C8FF00 (lime), #A1A1AA (muted), #FAFAFA (foreground), #66CC66 (green accent), #4488FF (blue accent).

    Also update sidebar colors if present (match the main color scheme).

    **Step 2 — Update @theme block custom tokens:**
    Find the `@theme` block in index.css. Update all red-referencing custom tokens:

    ```
    --color-surface:          #141414  →  #26282B
    --color-surface-elevated: #1C1C1C  →  #2E3033  (slightly lighter than surface)
    --color-primary-hover:    #E0605A  →  #D4FF33  (lighter lime for hover)
    --color-primary-muted:    rgba(213,85,80,0.15)  →  rgba(200,255,0,0.15)
    ```

    Update all glow/shadow tokens:
    ```
    --shadow-glow:         rgba(213,85,80,0.2)  →  rgba(200,255,0,0.2)
    --shadow-glow-intense: rgba(213,85,80,0.4)  →  rgba(200,255,0,0.4)
    ```

    Update any other @theme tokens that reference the old red (#D55550, #E0605A, #C14540, etc.) or old surface (#141414, #1C1C1C) values. The exact token names may vary — read the full @theme block and update every occurrence.

    **Step 3 — Search for hard-coded red hex values in component files:**
    Run:
    ```bash
    grep -rn '#D55550\|#E0605A\|#d55550\|#e0605a\|#C14540\|#c14540\|#8B1A1A\|rgb(213.*85.*80)\|rgba(213.*85.*80' src/ --include='*.ts' --include='*.tsx' --include='*.css'
    ```

    For each match:
    - If it's a CSS variable definition in index.css: already updated in Steps 1-2
    - If it's a hard-coded hex in a component file (e.g., confetti colors, SVG fills, inline styles): replace with the lime equivalent or the appropriate CSS variable reference
    - If it's in a comment: update the comment to reference the new color

    **Confetti colors specifically:** The research notes confetti hex arrays in BadgeUnlockModal/XPClaimModal. These are JS constants. Replace red confetti colors with lime-palette equivalents (#C8FF00 lime, #D4FF33 light lime, #A0CC00 dark lime, etc.).

    **Step 4 — Update index.html mask-icon color:**
    Read `index.html`. Find the `<link rel="mask-icon" ... color="#8B1A1A">` tag.
    Change `color="#8B1A1A"` to `color="#C8FF00"` (V2 signal color).
    Verify `theme-color` is already `#0a0a0a` (should be correct, no change needed).

    **Step 5 — Verify no red remnants:**
    Run a comprehensive search:
    ```bash
    grep -rn '#D55550\|#E0605A\|#d55550\|#e0605a\|#C14540\|#8B1A1A' src/ index.html --include='*.ts' --include='*.tsx' --include='*.css' --include='*.html'
    ```
    This must return zero results (or only irrelevant matches like comments explaining the migration).

    **Step 6 — Build and verify:**
    Run `npx tsc --noEmit && npm run build` to confirm everything compiles and builds correctly.

    **IMPORTANT — What NOT to change:**
    - Typography fonts (DESIGN-06): Oswald/Inter/JetBrains Mono are already in place. Do not modify font-family declarations.
    - `--border` and `--input`: Keep at #2A2A2A (slightly lighter than --card #26282B) to maintain visual card boundaries. Research open question #3 noted that making card and border identical could make cards invisible.
    - Any color values in `database.types.ts` or test files: these are not visual.
    - IntakeView/intakeApi coach_notes fields: these are intake data, not coach dashboard code.
  </action>
  <verify>
    <automated>cd /Users/ejasper/code/trained-app && npx tsc --noEmit && npm run build 2>&1 | tail -10</automated>
    <manual>Verify --primary is #C8FF00 in index.css. Verify no #D55550 remains in src/.</manual>
  </verify>
  <done>
    All CSS tokens updated to Dopamine Noir V2 values. :root block has lime primary (#C8FF00), dark primary-foreground (#0A0A0A), V2 surface (#26282B), brighter foreground (#FAFAFA), muted text (#A1A1AA), distinct destructive (#B91C1C), and 0.75rem radius. @theme block has updated surface, hover, muted, and glow tokens. All hard-coded red hex values replaced. index.html mask-icon updated. Build succeeds.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of Dopamine Noir V2 across all screens</name>
  <files>src/index.css</files>
  <action>
    Start the dev server with `npm run dev` and verify the V2 color system renders correctly across all screens. This is a human visual verification checkpoint — no code changes expected unless issues are found.

    **What was built:** Complete Dopamine Noir V2 color migration — lime (#C8FF00) replaces red (#D55550) as primary signal color across the entire app. All CSS tokens updated, hard-coded colors replaced.

    **How to verify:**
    1. Run `npm run dev` and open http://localhost:5173 in browser
    2. Check the following screens for correct V2 appearance:

    **Home screen:**
    - Primary elements (banners, highlights) should be lime green, not red
    - Background should be very dark (#0A0A0A)
    - Text should be bright white (#FAFAFA)
    - Cards should have subtle dark surface (#26282B)

    **Bottom navigation:**
    - Inactive icons should be muted gray (#A1A1AA)
    - Active icon should be lime (#C8FF00)
    - Top accent bar should be lime

    **Workouts screen:**
    - "Start Workout" button should have lime background with dark text
    - Progress bars should have lime fill
    - Cards should be dark surface with visible borders

    **Macros screen:**
    - Macro tracking bars should use lime fill
    - Numbers should be visible (JetBrains Mono font)

    **Settings screen:**
    - Destructive buttons (delete, reset) should be dark red (#B91C1C), NOT lime
    - Regular buttons/links should be lime
    - Card backgrounds should be dark surface

    **General checks:**
    - No red (#D55550) visible anywhere as a primary/accent color
    - Button border radius should appear slightly more rounded (0.75rem vs 0.625rem)
    - All text should be readable (good contrast on dark backgrounds)

    **Resume signal:** Type "approved" if V2 design looks correct, or describe any color issues found.
  </action>
  <verify>
    <automated>cd /Users/ejasper/code/trained-app && npm run build 2>&1 | tail -5</automated>
    <manual>Visual inspection of all app screens confirms V2 lime color system with no red remnants.</manual>
  </verify>
  <done>User has visually verified the Dopamine Noir V2 color system renders correctly across all screens. Lime signal, dark surfaces, bright foreground, and distinct destructive red all appear as specified.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes
2. `npm run build` succeeds
3. `grep -rn '#D55550\|#E0605A\|#d55550\|#e0605a' src/ index.html --include='*.ts' --include='*.tsx' --include='*.css' --include='*.html'` returns zero results
4. `grep '#C8FF00' src/index.css` confirms lime primary is set
5. `grep '#B91C1C' src/index.css` confirms destructive is distinct from primary
6. `grep '0.75rem' src/index.css` confirms updated radius
7. `grep '#FAFAFA' src/index.css` confirms updated foreground
8. `grep 'Oswald\|Inter\|JetBrains' src/index.css` confirms typography unchanged
</verification>

<success_criteria>
- Every screen renders with Dopamine Noir V2 color system
- Lime (#C8FF00) is the signal/primary color throughout
- Destructive actions use #B91C1C (distinct from primary)
- Cards use Surface (#26282B) background
- Text uses #FAFAFA foreground and #A1A1AA muted
- Typography hierarchy (Oswald/Inter/JetBrains Mono) is unchanged
- No hard-coded red hex values remain
- Build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/17-foundation-cleanup/17-02-SUMMARY.md`
</output>
