---
phase: 32-sign-up-screen
plan: 03
type: execute
wave: 3
depends_on: [32-02]
files_modified:
  - src/screens/auth-screens/SignUpScreen.tsx
autonomous: true
requirements: [SIGNUP-01]

must_haves:
  truths:
    - "Sign Up screen matches mockup with pixel-level accuracy"
    - "All visual gaps from Plan 02 review are addressed"
    - "Button styling and interactions are polished"
  artifacts:
    - path: "src/screens/auth-screens/SignUpScreen.tsx"
      provides: "Polished Sign Up screen matching mockup"
      min_lines: 100
  key_links:
    - from: "src/screens/auth-screens/SignUpScreen.tsx"
      to: "src/lib/apple-auth.ts"
      via: "signInWithApple import"
      pattern: "signInWithApple"
---

<objective>
Apply pixel-polish refinements to the Sign Up screen based on the Plan 02 review findings.

Purpose: Achieve mockup fidelity by addressing all documented visual gaps from the review phase.

Output: Polished SignUpScreen that matches auth_signup.png mockup.
</objective>

<execution_context>
@/Users/ejasper/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ejasper/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/32-sign-up-screen/32-01-SUMMARY.md
@.planning/phases/32-sign-up-screen/32-02-SUMMARY.md
@.planning/phases/32-sign-up-screen/32-02-REVIEW.md
@Design inspo/mockups/auth flow/auth_signup.png
@src/screens/auth-screens/SignUpScreen.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply spacing and sizing refinements</name>
  <files>src/screens/auth-screens/SignUpScreen.tsx</files>
  <action>
Read the 32-02-REVIEW.md file to get the specific refinement tasks.

Apply all spacing and sizing fixes identified in the review:

**Common refinements (adjust based on actual review findings):**

1. **Logo sizing:**
   - If too small/large: adjust w-X h-X classes
   - Ensure aspect ratio is maintained

2. **Vertical spacing:**
   - Logo to wordmark gap: adjust mt-X or mb-X
   - Wordmark to headline gap: adjust spacing
   - Headline to subline gap: adjust spacing
   - Subline to buttons gap: adjust mt-X
   - Button group internal spacing: adjust space-y-X
   - Use consistent spacing tokens (mt-4, mt-6, mt-8, mt-12, etc.)

3. **Button refinements:**
   - Ensure h-14 (56px height)
   - Icon alignment: pl-5 for left padding, flex items-center
   - Icon sizing: w-5 h-5 or similar
   - Text centering: may need absolute positioning for icon with centered text

4. **Overall centering:**
   - Ensure content is vertically centered using flex/justify-center
   - Horizontal centering with items-center
   - If content appears shifted, adjust wrapper styles

5. **Safe area:**
   - pt-safe-top pb-safe-bottom for iOS notch/home indicator
   - Or use px-6 for horizontal padding

Apply ONLY the specific fixes documented in 32-02-REVIEW.md. Do not make arbitrary changes.
  </action>
  <verify>
    <automated>grep -E "(w-[0-9]+|h-[0-9]+|mt-[0-9]+|mb-[0-9]+|space-y-[0-9]+|pl-[0-9]+)" src/screens/auth-screens/SignUpScreen.tsx</automated>
  </verify>
  <done>Spacing and sizing values match review specifications</done>
</task>

<task type="auto">
  <name>Task 2: Apply typography and color refinements</name>
  <files>src/screens/auth-screens/SignUpScreen.tsx</files>
  <action>
Apply typography and color fixes from the review:

1. **Wordmark typography:**
   - Ensure Oswald font: `style={{ fontFamily: "'Oswald', sans-serif" }}`
   - Font size: adjust text-Xx to match mockup
   - Color: #D4A853 (gold)
   - Tracking: tracking-[0.05em] or similar

2. **Headline typography:**
   - Font: Oswald, uppercase
   - Font size: text-2xl or text-3xl
   - Color: #F5F0E8 (warm white)

3. **Subline typography:**
   - Font size: text-sm or text-base
   - Color: #8A8A8A (muted gray)
   - Line height: leading-relaxed if needed

4. **Button text:**
   - Font: Inter or system sans-serif
   - Font weight: font-medium or font-semibold
   - Colors per design brief:
     - Apple: text-white
     - Google: text-[#F5F0E8]
     - Email: text-[#F5F0E8]

5. **OR divider:**
   - Text: text-xs, text-[#8A8A8A]
   - Lines: bg-[#3A3A3A], h-px

6. **Sign In link:**
   - "Already initiated?": text-[#8A8A8A]
   - "Sign In": text-[#D4A853]

7. **Legal copy:**
   - text-xs, text-[#8A8A8A]
   - Links: text-[#D4A853]

8. **Background:**
   - Ensure bg-[#0A0A0A] on outer container

Apply changes per the specific values documented in 32-02-REVIEW.md.
  </action>
  <verify>
    <automated>grep -E "(text-(xs|sm|base|lg|xl|[0-9]xl)|fontFamily|#[A-Fa-f0-9]{6})" src/screens/auth-screens/SignUpScreen.tsx</automated>
  </verify>
  <done>Typography and colors match mockup specifications</done>
</task>

<task type="auto">
  <name>Task 3: Final polish and verification</name>
  <files>src/screens/auth-screens/SignUpScreen.tsx</files>
  <action>
Apply final polish and verify everything works:

1. **Button hover/active states:**
   - Add subtle hover effects if appropriate (opacity change, etc.)
   - Ensure disabled state styling works when loading

2. **Loading indicator polish:**
   - Ensure spinner or loading text is visible and centered in button
   - Maintain button dimensions during loading

3. **Icon refinements:**
   - Apple icon: Ensure white fill, proper size
   - Google icon: Use full-color Google G SVG or appropriate placeholder
   - Mail icon: Ensure gold #D4A853 color

4. **Error state display:**
   - If error state exists, ensure proper styling
   - Red text #EF4444 for errors

5. **Transition animations (optional):**
   - Subtle fade-in on mount
   - Smooth transitions on button press

6. **Final cleanup:**
   - Remove any unused imports
   - Remove console.log statements (except in auth modules)
   - Ensure component is clean and production-ready

7. **Type check:**
   ```bash
   npx tsc --noEmit
   ```
   Ensure no TypeScript errors.

8. **Visual verification:**
   - Run `npm run dev`
   - Navigate to Sign Up screen
   - Compare against mockup one final time
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Sign Up screen is polished, production-ready, and matches mockup</done>
</task>

</tasks>

<verification>
1. All refinement tasks from 32-02-REVIEW.md are addressed
2. `npm run dev` shows Sign Up screen matching mockup
3. All elements properly styled (typography, colors, spacing)
4. `npx tsc --noEmit` passes with no errors
5. No console warnings related to SignUpScreen
</verification>

<success_criteria>
- [ ] All visual gaps from review are fixed
- [ ] Sign Up screen matches mockup visually
- [ ] Logo, wordmark, headline, subline properly styled
- [ ] Auth buttons match design spec exactly
- [ ] OR divider, Sign In link, legal copy all correct
- [ ] TypeScript compilation passes
- [ ] Component is production-ready
</success_criteria>

<output>
After completion, create `.planning/phases/32-sign-up-screen/32-03-SUMMARY.md`
</output>
