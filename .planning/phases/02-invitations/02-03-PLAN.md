---
phase: 02-invitations
plan: 03
type: execute
wave: 2
depends_on: [02-01, 02-02]
files_modified:
  - src/screens/Coach.tsx
autonomous: true

must_haves:
  truths:
    - "Coach can enter an email and click Send Invite to trigger the send-invite Edge Function"
    - "Coach sees invite status list showing pending, accepted, and expired invites"
    - "Sending an invite shows loading state, success feedback, or specific error messages"
    - "Existing users are added directly with appropriate feedback ('Added as client')"
    - "Expired invites are visually distinguished from pending and accepted"
    - "Coach can resend an invite (refreshes expiry)"
  artifacts:
    - path: "src/screens/Coach.tsx"
      provides: "Invite sending UI and invite status list"
      contains: "functions.invoke.*send-invite"
  key_links:
    - from: "src/screens/Coach.tsx"
      to: "send-invite Edge Function"
      via: "supabase.functions.invoke('send-invite', { body: { email } })"
      pattern: "functions\\.invoke.*send-invite"
    - from: "src/screens/Coach.tsx"
      to: "invites table"
      via: "supabase.from('invites').select()"
      pattern: "from.*invites.*select"
---

<objective>
Replace the existing "Add Client" modal in Coach.tsx with the invite flow and add an invite status list showing pending/accepted/expired invites.

Purpose: This is the user-facing UI for the invitation system. The coach enters a client email, clicks send, and the Edge Function handles the rest. The coach can also see the status of all their invites at a glance.

Output: Updated Coach.tsx with invite sending and invite status list.
</objective>

<execution_context>
@/Users/ejasper/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ejasper/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-invitations/02-RESEARCH.md
@.planning/phases/02-invitations/02-01-SUMMARY.md
@.planning/phases/02-invitations/02-02-SUMMARY.md
@src/screens/Coach.tsx
@src/lib/database.types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace Add Client modal with Invite Client flow</name>
  <files>src/screens/Coach.tsx</files>
  <action>
Refactor the "Add Client" modal in Coach.tsx to become the "Invite Client" modal. The existing modal (lines ~645-705) currently:
- Asks for email
- Looks up user by email in profiles
- Requires user to already have an account
- Inserts directly into coach_clients

Replace this with:
1. **Update modal title and copy:** "Invite Client" instead of "Add Client". Description: "Enter your client's email address to send them a signup invite." (Remove "They must have an account already" text.)

2. **Update button text:** "Send Invite" instead of "Add Client". Loading state: "Sending..." instead of "Adding...".

3. **Replace handleInviteClient implementation:**
   - Keep the email format validation (existing regex is fine)
   - Replace the profile lookup + direct insert with `supabase.functions.invoke('send-invite', { body: { email } })`
   - Parse the response:
     - If `data.error`: show error message
     - If `data.action === 'added_directly'`: show "User already had an account -- added as client!" and refresh clients list
     - If `data.action === 'invite_sent'`: show "Invite sent!" and refresh invites list
   - Handle edge function errors (network error, function not found) with user-friendly messages

4. **Update dev bypass mock flow:**
   - Keep the dev bypass path functional but update the success message to match the invite flow
   - Mock the Edge Function response pattern (simulate `invite_sent` and `added_directly` actions)

5. **Update header button:** Change "+ Add Client" button text to "+ Invite Client".

6. **Keep everything else unchanged:** Client list, client detail modal, quick stats, loading/error states, remove client flow -- all stay exactly as they are.
  </action>
  <verify>
  - `npm run build` passes
  - The "Invite Client" modal calls `supabase.functions.invoke('send-invite', ...)`
  - Success and error feedback is shown to the coach
  - Dev bypass mode still works
  </verify>
  <done>
  - Coach can enter an email and send an invite via the Edge Function
  - The modal provides appropriate feedback for all scenarios (invite sent, added directly, errors)
  - The "+ Invite Client" button in the header opens the modal
  </done>
</task>

<task type="auto">
  <name>Task 2: Add invite status list to Coach dashboard</name>
  <files>src/screens/Coach.tsx</files>
  <action>
Add an invite status section to the Coach dashboard, below the quick stats and above the client list.

1. **Add invite state:**
   ```typescript
   interface InviteRow {
     id: string
     email: string
     status: 'pending' | 'accepted' | 'expired'
     created_at: string
     expires_at: string
     accepted_at: string | null
   }
   const [invites, setInvites] = useState<InviteRow[]>([])
   ```

2. **Add fetchInvites function:**
   - Query `invites` table: `.from('invites').select('id, email, status, created_at, expires_at, accepted_at').order('created_at', { ascending: false })`
   - Compute display status: if status is 'pending' but `expires_at` is past, display as 'expired'
   - For dev bypass: return an empty array (no mock invites needed)
   - Call fetchInvites on mount (alongside existing fetchClients)
   - Also call fetchInvites after successful invite send

3. **Render invite status section** (only when invites.length > 0):
   - Section header: "Pending Invites" with count badge
   - Show only pending (and recently expired) invites -- accepted invites are now in the client list
   - Each invite row shows:
     - Email address (with data-sentry-mask on the container)
     - Status pill: pending = yellow/warning, expired = muted/gray
     - Time since sent (e.g., "Sent 2 days ago")
     - For pending invites: "Resend" button that calls handleInviteClient with the same email (which triggers upsert + re-sends email)
   - Use existing Card component for consistent styling
   - Use existing design tokens (text-muted-foreground, text-warning, bg-card, etc.)

4. **Section placement:** After the quick stats grid, before the client list. Only render if there are non-accepted invites to show. This keeps the dashboard clean when all invites have been accepted.

5. **Accepted invites:** Do NOT show accepted invites in the invite section -- they appear as clients in the client list (via the auto-link trigger). This avoids duplication.
  </action>
  <verify>
  - `npm run build` passes
  - Invite status section renders when invites exist
  - Pending invites show email, status pill, and resend button
  - Expired invites are visually distinct from pending
  - Accepted invites do not appear in the invites section
  - fetchInvites is called on mount and after sending an invite
  </verify>
  <done>
  - Coach can see pending and expired invites at a glance
  - Pending invites have a resend action
  - Invite status section only appears when there are relevant invites
  - Accepted invites are represented in the client list, not the invite section
  </done>
</task>

</tasks>

<verification>
- `npm run build` passes with no TypeScript errors
- Coach.tsx calls `supabase.functions.invoke('send-invite', { body: { email } })` for invite sending
- Coach.tsx queries `invites` table for status display
- Invite modal shows "Invite Client" with "Send Invite" button
- Invite status section shows pending/expired invites with resend action
- Dev bypass mode still functions correctly
- No regression in existing client list, client detail modal, or quick stats
</verification>

<success_criteria>
- Coach can enter an email and send an invite (INVITE-01 UI complete)
- Coach can see which invites are pending, accepted, or expired (INVITE-02 UI complete)
- Resend functionality refreshes invite expiry (deduplication UX)
- All three INVITE requirements have their frontend layer complete
</success_criteria>

<output>
After completion, create `.planning/phases/02-invitations/02-03-SUMMARY.md`
</output>
