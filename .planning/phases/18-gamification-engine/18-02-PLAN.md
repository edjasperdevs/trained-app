---
phase: 18-gamification-engine
plan: 02
type: execute
wave: 2
depends_on: [18-01]
files_modified:
  - src/components/DPDisplay.tsx
  - src/components/RankUpModal.tsx
  - src/components/StreakDisplay.tsx
  - src/components/WeeklySummary.tsx
  - src/components/index.ts
  - src/screens/Home.tsx
  - src/screens/Workouts.tsx
  - src/screens/CheckInModal.tsx
  - src/screens/AvatarScreen.tsx
autonomous: true
requirements: [GAME-08, GAME-09]

must_haves:
  truths:
    - "Home screen displays current rank name, rank number, cumulative DP total, and a progress bar toward next rank"
    - "When a user ranks up from any DP-earning action, a celebration modal with confetti plays and the new rank name is shown"
    - "Rank-up celebration fires only once per rank (tracked via lastCelebratedRank)"
    - "Obedience Streak counter is visible on the home screen and reads from dpStore"
    - "Completing a workout awards +50 DP and checks for rank-up"
    - "Checking in awards DP for protein/training and checks for rank-up"
    - "WeeklySummary shows DP data from dpStore (not xpStore)"
    - "Streak is validated on app mount — stale streak resets if lastActionDate is > 1 day old"
  artifacts:
    - path: "src/components/DPDisplay.tsx"
      provides: "Rank name, rank number, DP total, and progress bar component"
      exports: ["DPDisplay"]
    - path: "src/components/RankUpModal.tsx"
      provides: "Rank-up celebration modal with confetti and rank transition display"
      exports: ["RankUpModal"]
    - path: "src/screens/Home.tsx"
      provides: "Home screen with DPDisplay replacing XPDisplay, RankUpModal, and streak from dpStore"
      contains: "DPDisplay"
  key_links:
    - from: "src/components/DPDisplay.tsx"
      to: "src/stores/dpStore.ts"
      via: "useDPStore hook for rank info and DP total"
      pattern: "useDPStore"
    - from: "src/components/RankUpModal.tsx"
      to: "src/screens/XPClaimModal.tsx"
      via: "Reuses confetti pattern (CONFETTI_COLORS, confetti-fall keyframes)"
      pattern: "CONFETTI_COLORS|confetti-fall"
    - from: "src/screens/Home.tsx"
      to: "src/components/DPDisplay.tsx"
      via: "Renders DPDisplay in place of XPDisplay"
      pattern: "<DPDisplay"
    - from: "src/screens/Workouts.tsx"
      to: "src/stores/dpStore.ts"
      via: "awardDP('training') on workout complete"
      pattern: "awardDP.*training"
    - from: "src/screens/CheckInModal.tsx"
      to: "src/stores/dpStore.ts"
      via: "awardDP calls for training/protein on check-in"
      pattern: "awardDP"
---

<objective>
Build the rank progression UI and rank-up celebration, replacing all XP visual components with DP equivalents.

Purpose: This plan delivers the user-visible gamification experience — the rank display, progress bar, celebration animation, and streak counter that make users feel the DP system. Without this, dpStore is invisible.

Output: DPDisplay component, RankUpModal celebration, all screens showing DP/rank data instead of XP/level data.
</objective>

<execution_context>
@/Users/ejasper/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ejasper/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-gamification-engine/18-RESEARCH.md
@.planning/phases/18-gamification-engine/18-01-SUMMARY.md
@src/components/XPDisplay.tsx
@src/components/StreakDisplay.tsx
@src/components/WeeklySummary.tsx
@src/components/ProgressBar.tsx
@src/components/index.ts
@src/screens/Home.tsx
@src/screens/Workouts.tsx
@src/screens/CheckInModal.tsx
@src/screens/AvatarScreen.tsx
@src/screens/XPClaimModal.tsx
@src/design/constants.ts
@src/stores/dpStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DPDisplay and RankUpModal, wire Home/Workouts/CheckInModal</name>
  <files>src/components/DPDisplay.tsx, src/components/RankUpModal.tsx, src/components/index.ts, src/screens/Home.tsx, src/screens/Workouts.tsx, src/screens/CheckInModal.tsx</files>
  <action>
**DPDisplay component** (`src/components/DPDisplay.tsx`):
Create a component that replaces XPDisplay. Structure:
- Import `useDPStore` from `'@/stores'`, `LABELS` from `'@/design/constants'`, `ProgressBar` from `'./ProgressBar'`, `cn` from `'@/lib/cn'`
- Props: `{ compact?: boolean }` (no `showPending` — V2 has no pending concept)
- Read `totalDP`, `currentRank` from useDPStore hook
- Call `useDPStore(s => s.getRankInfo)()` to get `{ name, rank, dpForNext, progress }`
- Render:
  - Rank name as uppercase tracking-wider text in muted-foreground (e.g., "NOVICE")
  - `{LABELS.level} {currentRank}` in text-2xl font-bold text-primary font-display (Oswald)
  - Total DP in font-mono text-primary
  - ProgressBar with `progress={rankInfo.progress}` using `color="gradient"` and `size="lg"`
  - "X to next rank" in muted-foreground font-mono (hide at max rank)
- Compact mode: smaller text sizes, `size="sm"` on ProgressBar
- At max rank (rank 15), show "MAX RANK" instead of progress-to-next

**RankUpModal component** (`src/components/RankUpModal.tsx`):
Adapt the level-up celebration from XPClaimModal.tsx:
- Props: `{ oldRank: number; newRank: number; rankName: string; onClose: () => void }`
- Copy CONFETTI_COLORS array from XPClaimModal.tsx (lime palette: #C8FF00, #D4FF33, #A0CC00, #86B300)
- Reuse the confetti-fall CSS keyframe animation pattern (already in index.css)
- Generate 25 confetti particles with random positions, delays, and colors (same pattern as XPClaimModal)
- Display: fixed fullscreen overlay with backdrop blur
  - Confetti particles falling
  - "RANK UP" header text with pulse-scale animation
  - Rank name in large text-primary font-display (Oswald)
  - Rank transition: "Rank {old}" → arrow → "Rank {new}" with the new rank highlighted in primary
  - Dismiss button or auto-close after 3 seconds
- Import and call `haptics.heavy()` from `'@/lib/haptics'` on mount for tactile feedback
- Schedule a local notification for rank-up using the existing local notification pattern from Phase 15 (import from `@capacitor/local-notifications`). If `isNative()`, schedule an immediate notification with title "Rank Up!" and body "You reached {rankName} (Rank {newRank})!". Use a stable ID (e.g., 100) so it can be identified. If notification scheduling fails, catch silently — it's progressive enhancement.

**Update `src/components/index.ts`:** Add `export { DPDisplay } from './DPDisplay'` and `export { RankUpModal } from './RankUpModal'`. Keep XPDisplay export for now (Phase 24 cleanup).

**Home.tsx migration:**
- Import `useDPStore` from `'@/stores'` instead of `useXPStore`
- Import `DPDisplay` and `RankUpModal` from `'@/components'`
- Replace `<XPDisplay>` with `<DPDisplay>` in the avatar/rank card section
- Remove `XPClaimModal` import and all related state (`showXPClaim`, `canClaimXP`, `pendingXP`, etc.). V2 has no weekly XP claim flow.
- Remove the "Claim XP" button/banner that triggered XPClaimModal
- Add RankUpModal state: `const [rankUpData, setRankUpData] = useState<{ oldRank: number; newRank: number; rankName: string } | null>(null)`
- Show `<RankUpModal>` when `rankUpData` is not null, with `onClose={() => setRankUpData(null)}`
- Read obedience streak from dpStore: `const obedienceStreak = useDPStore(s => s.obedienceStreak)` — pass to StreakDisplay (or StreakDisplay reads it directly in Task 2)
- **Streak validation on mount:** Add a `useEffect` that runs on mount to check if the streak should be reset. Read `lastActionDate` and `obedienceStreak` from dpStore. If `lastActionDate` exists and `getLocalDaysDifference(lastActionDate, today) > 1`, set obedienceStreak to 0 via `useDPStore.setState({ obedienceStreak: 0 })`. This handles users who miss days without opening the app.

**Workouts.tsx migration:**
- Import `useDPStore` from `'@/stores/dpStore'` (direct import, not barrel — Workouts already uses direct xpStore import)
- Replace `logDailyXP(...)` calls with `useDPStore.getState().awardDP('training')`. There are 3 places in Workouts.tsx where `logDailyXP` is called (workout complete for different types).
- After each `awardDP('training')` call, check the return value: `const result = useDPStore.getState().awardDP('training')`. If `result.rankedUp`, set rank-up state. Add state for rank-up modal similar to Home: `const [rankUpData, setRankUpData] = useState(...)`. Render `<RankUpModal>` at the bottom of the component.
- Remove `getTodayLog` import from xpStore (if it was only used for XP display purposes). If it was used to check "already worked out today", replace with dpStore's `getTodayLog()` checking `training > 0`.
- Remove any `XP_VALUES` references.

**CheckInModal.tsx migration:**
- Import `useDPStore` from `'@/stores'` instead of `useXPStore`
- Replace the `logDailyXP(...)` call with individual `awardDP()` calls:
  - If `data.workout` (workout completed): `awardDP('training')` — though this may already have been awarded when the workout was completed. To avoid double-counting, check dpStore's today log: if `getTodayLog()?.training > 0`, skip the training award. Or simpler: let the user earn it here too if they didn't log a workout that day but marked it in check-in. Use the same approach as V1 (if the boolean is true, award it).
  - If `data.protein` (protein target hit): `awardDP('protein')`
  - Meals are NOT awarded at check-in time — they are awarded at meal-log time (Macros.tsx, wired in 18-01 Task 2)
- Handle rank-up return from any awardDP call: track the last rank-up result and show RankUpModal if any call returned `rankedUp: true`. Add RankUpModal state and render.
- Remove all V1 XP references: `XP_VALUES`, `logDailyXP`, `pendingXP`, `streakBonus`, `perfectDay`, etc.
  </action>
  <verify>
    <automated>cd /Users/ejasper/code/trained-app && npx tsc --noEmit && npx vite build 2>&1 | tail -5</automated>
    <manual>Run dev server, navigate to Home screen — DPDisplay shows rank/DP/progress. Complete a workout — DP increments. If rank-up threshold crossed, celebration modal appears.</manual>
  </verify>
  <done>DPDisplay shows rank, DP, progress bar on Home. RankUpModal plays confetti celebration on rank-up with haptic feedback and local notification. Workouts and CheckInModal award DP and handle rank-up. XPClaimModal no longer shown.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate StreakDisplay, WeeklySummary, AvatarScreen, and wire streak reset</name>
  <files>src/components/StreakDisplay.tsx, src/components/WeeklySummary.tsx, src/screens/AvatarScreen.tsx</files>
  <action>
**StreakDisplay.tsx:**
- Import `useDPStore` from `'@/stores'` instead of `useXPStore`
- Replace xpStore streak data with dpStore:
  - Read `obedienceStreak` from `useDPStore(s => s.obedienceStreak)` instead of userStore or xpStore streak
  - Read `longestObedienceStreak` from `useDPStore(s => s.longestObedienceStreak)` if displayed
- The streak label should use `LABELS.streak` from `@/design/constants` (already says "Obedience Streak")
- Adapt any daily log reading: if StreakDisplay reads xpStore.dailyLogs to show recent activity, switch to dpStore.dailyLogs with the V2 DailyDP shape (training/meals/protein counts instead of workout/protein/calories booleans)
- Ensure the fire/flame icon and streak count render correctly with the new data source

**WeeklySummary.tsx:**
- Import `useDPStore` from `'@/stores'` instead of `useXPStore`
- Replace xpStore daily logs with dpStore daily logs:
  - V1 DailyXP has: `workout: boolean, protein: boolean, calories: boolean, checkIn: boolean, perfectDay: boolean, total: number`
  - V2 DailyDP has: `training: number, meals: number, protein: number, steps: number, sleep: number, total: number`
- Update the weekly grid/display to show V2 fields:
  - Show training completion (training > 0), meals tracked (meals count), protein hit (protein > 0)
  - Remove V1-only fields: calories, checkIn, perfectDay, streakBonus, claimed
  - Steps and sleep show as 0 for now (Phase 20 enables them)
- Update total DP display (was XP total, now DP total)
- Replace any level references with rank references
- Remove any "claimed" badge/indicator (V2 has no weekly claim)

**AvatarScreen.tsx:**
- Import `useDPStore` from `'@/stores'` instead of `useXPStore`
- Replace `currentLevel` with `currentRank` for avatar stage calculation
- Replace `getCurrentLevelProgress()` with `useDPStore(s => s.getRankInfo)().progress` for any progress display
- Replace `totalXP` with `totalDP` in any stats display
- Replace level labels with rank labels
- The avatar stage calculation may reference level thresholds — adapt to use rank-based stage mapping (Phase 23 will refine this, but for now map: ranks 1-3 → stage 1, ranks 4-7 → stage 2, ranks 8-11 → stage 3, ranks 12-14 → stage 4, rank 15 → stage 5)

After all changes, verify no remaining xpStore imports exist outside of the allowed files (xpStore.ts, xpStore.test.ts, XPClaimModal.tsx, XPDisplay.tsx, stores/index.ts).
  </action>
  <verify>
    <automated>cd /Users/ejasper/code/trained-app && npx tsc --noEmit && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
    <manual>Home screen shows obedience streak from dpStore. WeeklySummary shows DP-based daily breakdown. AvatarScreen shows rank-based progression.</manual>
  </verify>
  <done>StreakDisplay reads obedience streak from dpStore. WeeklySummary shows V2 daily DP breakdown (training, meals, protein). AvatarScreen uses rank for stage calculation. No xpStore references remain outside allowed dead-code files.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — zero type errors
2. `npx vite build` — production build succeeds
3. `npx vitest run` — all tests pass (dpStore tests + existing regressions)
4. `grep -r "useXPStore" src/ --include="*.ts" --include="*.tsx" -l` — only xpStore.ts, xpStore.test.ts, XPClaimModal.tsx, XPDisplay.tsx, stores/index.ts
5. Home screen visually shows: rank name, rank number, DP total, progress bar, obedience streak
6. Rank-up celebration plays with confetti and haptics when rank threshold is crossed
</verification>

<success_criteria>
- DPDisplay shows rank, DP, and progress bar on Home screen
- RankUpModal celebration fires on rank-up with confetti, haptics, and local notification
- All DP-earning actions (workout, check-in, meal) trigger awardDP and handle rank-up
- StreakDisplay shows obedience streak from dpStore
- WeeklySummary shows V2 DailyDP data
- AvatarScreen uses rank for stage mapping
- Zero xpStore references outside allowed dead-code files
</success_criteria>

<output>
After completion, create `.planning/phases/18-gamification-engine/18-02-SUMMARY.md`
</output>
