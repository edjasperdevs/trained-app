---
phase: 34-email-sign-up-form
plan: 03
type: execute
wave: 3
depends_on: [34-02]
files_modified:
  - src/screens/auth-screens/EmailSignUpScreen.tsx
autonomous: true
requirements: [EMAILSIGNUP-01, EMAILSIGNUP-02, EMAILSIGNUP-03]

must_haves:
  truths:
    - "All visual gaps from 34-02-REVIEW.md are resolved"
    - "Screen styling matches mockup pixel-accurately"
    - "Error states display with correct styling"
    - "Focus states have gold ring animation"
    - "All spacing and typography match design spec"
  artifacts:
    - path: "src/screens/auth-screens/EmailSignUpScreen.tsx"
      provides: "Pixel-polished Email Sign Up form"
      min_lines: 250
  key_links: []
---

<objective>
Pixel-polish the Email Sign Up form based on Review findings from 34-02.

Purpose: Achieve mockup-accurate styling for production-ready screen.

Output: Refined EmailSignUpScreen.tsx with all visual gaps resolved.
</objective>

<execution_context>
@/Users/ejasper/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ejasper/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@Design inspo/mockups/auth flow/auth_email_signup.png
@Design inspo/mockups/auth flow/WellTrained — Authentication Flow Brief.md
@src/screens/auth-screens/EmailSignUpScreen.tsx
@.planning/phases/34-email-sign-up-form/34-02-REVIEW.md
@.planning/phases/34-email-sign-up-form/34-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply visual refinements from Review</name>
  <files>src/screens/auth-screens/EmailSignUpScreen.tsx</files>
  <action>
Read 34-02-REVIEW.md and apply all recommended fixes.

**If 34-02 was approved with no gaps:**
- This task is verification-only
- Verify the screen still matches mockup after any code drift
- No code changes needed

**If 34-02 identified gaps, common refinements include:**

1. **Input field focus state (per auth brief):**
   ```
   focus:outline-none focus:ring-2 focus:ring-[#D4A853] focus:ring-opacity-50
   ```
   - Gold glow border on active/focused input

2. **Spacing adjustments:**
   - Check margin/padding between elements matches mockup
   - Typical spacing: mt-4 between fields, mt-6 before button, mt-8 after button

3. **Typography refinements:**
   - Labels: text-xs uppercase tracking-wider text-[#8A8A8A] mb-2
   - Headline: Use fontFamily: "'Oswald', sans-serif" inline style

4. **Password strength indicator polish:**
   - Ensure h-1 (4px) height
   - gap-1 (4px) between segments
   - Smooth transition: transition-colors duration-200

5. **CTA button disabled state:**
   - opacity-50 when disabled
   - Full opacity when enabled
   - Smooth transition: transition-opacity duration-200

6. **Error state styling:**
   - Error message: text-sm text-[#EF4444] text-center
   - Position below form, above button

7. **Input field error indication (if applicable):**
   - When error displayed, optionally add red border: border-[#EF4444]
   - Only after form submission attempt, not during typing

Apply each fix from the REVIEW.md document precisely.
  </action>
  <verify>
    <automated>grep -q "focus:ring" src/screens/auth-screens/EmailSignUpScreen.tsx && npx tsc --noEmit 2>&1 | head -5</automated>
    <manual>Visual refinements applied per review findings</manual>
  </verify>
  <done>All visual gaps from review are resolved; screen matches mockup</done>
</task>

<task type="auto">
  <name>Task 2: Final verification and phase completion</name>
  <files>src/screens/auth-screens/EmailSignUpScreen.tsx</files>
  <action>
Complete final verification of the Email Sign Up screen:

1. **TypeScript check:**
   ```bash
   npx tsc --noEmit
   ```

2. **Run development server:**
   ```bash
   npm run dev
   ```

3. **Full functionality check:**
   - Navigate to /auth/email-signup
   - Verify back arrow works
   - Type in email field - verify gold focus ring appears
   - Type short password - verify strength shows 1 segment (if 8+ chars only)
   - Add uppercase - verify 2 segments
   - Add number - verify 3 segments
   - Add special character - verify all 4 segments gold
   - Type mismatched confirm password - verify button stays disabled
   - Type matching confirm password - verify button becomes enabled
   - Test show/hide password toggle
   - Click Sign In link - verify navigation to /auth/signin

4. **Edge cases:**
   - Invalid email format keeps button disabled
   - Password under 8 chars keeps button disabled
   - Empty confirm password keeps button disabled
   - Form submission with valid data (if Supabase connected)

5. **Remove any console.log statements**

6. **Ensure no ESLint warnings** for unused variables

7. **Visual polish confirmation:**
   - Compare final implementation to mockup one more time
   - All elements should align with design spec
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -5</automated>
    <manual>Email Sign Up screen is pixel-perfect and fully functional</manual>
  </verify>
  <done>Email Sign Up screen complete with all requirements met</done>
</task>

</tasks>

<verification>
1. All visual gaps from 34-02-REVIEW.md are resolved
2. Screen matches auth_email_signup.png mockup
3. Input focus states show gold ring
4. Password strength indicator works correctly
5. Button disabled/enabled states work correctly
6. Navigation works (back arrow, Sign In link)
7. TypeScript compilation passes
8. No console.log or ESLint warnings
</verification>

<success_criteria>
- [ ] Visual gaps from review resolved
- [ ] Input focus states have gold ring
- [ ] Password strength indicator updates correctly
- [ ] Button disabled until validation passes
- [ ] Form submission works with valid data
- [ ] All navigation handlers work
- [ ] TypeScript compilation passes
- [ ] Screen matches mockup
</success_criteria>

<output>
After completion, create `.planning/phases/34-email-sign-up-form/34-03-SUMMARY.md`
</output>
