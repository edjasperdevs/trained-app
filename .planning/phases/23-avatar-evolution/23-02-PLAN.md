---
phase: 23-avatar-evolution
plan: 02
type: execute
wave: 2
depends_on:
  - 23-01
files_modified:
  - src/screens/Home.tsx
  - src/screens/AvatarScreen.tsx
autonomous: true
requirements:
  - AVATAR-03

must_haves:
  truths:
    - "User sees their evolving avatar silhouette prominently on the home screen"
    - "Avatar on AvatarScreen also uses EvolvingAvatar for consistency"
    - "Avatar transitions smoothly between stages when rank changes"
  artifacts:
    - path: "src/screens/Home.tsx"
      provides: "Home screen with EvolvingAvatar integration"
      contains: "EvolvingAvatar"
    - path: "src/screens/AvatarScreen.tsx"
      provides: "Avatar detail screen with EvolvingAvatar"
      contains: "EvolvingAvatar"
  key_links:
    - from: "src/screens/Home.tsx"
      to: "src/components/EvolvingAvatar.tsx"
      via: "import and render in Avatar & Rank Section"
      pattern: "import.*EvolvingAvatar.*from.*components"
    - from: "src/screens/AvatarScreen.tsx"
      to: "src/components/EvolvingAvatar.tsx"
      via: "import and render in main display"
      pattern: "import.*EvolvingAvatar.*from.*components"
---

<objective>
Integrate EvolvingAvatar into the Home screen and AvatarScreen, replacing the existing icon-based Avatar component with the new stage-aware silhouette avatar.

Purpose: Fulfill AVATAR-03 requirement -- avatar displayed prominently on home screen -- and ensure visual consistency across the app.
Output: Home.tsx and AvatarScreen.tsx updated to use EvolvingAvatar.
</objective>

<execution_context>
@/Users/ejasper/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ejasper/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/23-avatar-evolution/23-01-SUMMARY.md
@src/screens/Home.tsx
@src/screens/AvatarScreen.tsx
@src/components/EvolvingAvatar.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate EvolvingAvatar into Home Screen</name>
  <files>src/screens/Home.tsx</files>
  <action>
1. Add import at top of file:
   ```typescript
   import { EvolvingAvatar } from '@/components'
   ```

2. In the "Avatar & Rank Section" card (around line 217-230), replace:
   ```tsx
   <Avatar size="lg" showMood showLevel level={currentRank} />
   ```
   With:
   ```tsx
   <div className="transition-all duration-500 ease-out">
     <EvolvingAvatar size="lg" />
   </div>
   ```

3. Keep the existing DPDisplay component beside the avatar -- do not change that section structure.

4. Remove the old Avatar import from '@/components' if no longer used elsewhere in this file. Verify first -- the old Avatar component should NOT be removed from the codebase (AvatarScreen may still reference it for mood/character display).

Note: The transition-all wrapper provides smooth animation when the avatar stage changes. The EvolvingAvatar component handles rank-to-stage mapping internally via its dpStore subscription.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <manual>Home screen shows EvolvingAvatar silhouette instead of icon-based Avatar</manual>
  </verify>
  <done>Home screen displays EvolvingAvatar in the Avatar & Rank Section card</done>
</task>

<task type="auto">
  <name>Task 2: Update AvatarScreen to Use EvolvingAvatar</name>
  <files>src/screens/AvatarScreen.tsx</files>
  <action>
1. Add import:
   ```typescript
   import { EvolvingAvatar } from '@/components'
   ```

2. In the "Main Avatar Display" section (around line 33-35), replace:
   ```tsx
   <Avatar size="xl" showMood showLevel level={currentRank} />
   ```
   With:
   ```tsx
   <div className="transition-all duration-500 ease-out">
     <EvolvingAvatar size="xl" />
   </div>
   ```

3. The level display below the avatar is now handled by the DPDisplay component or the "Current Stats" card below. The EvolvingAvatar does NOT show a level label -- this is intentional since the rank info is displayed in dedicated cards.

4. Keep the existing imports for Avatar component IF it's still used elsewhere (e.g., mood display). However, in this screen the old Avatar is only used once in the main display, so:
   - Remove `Avatar` from the import statement: `import { Avatar } from '@/components'`
   - Keep other imports intact (useAvatarStore still used for baseCharacter, currentMood)

5. The "Character Info" and "Mood" cards at the bottom of AvatarScreen can remain unchanged -- they display role and mood which are separate from the evolving avatar visual.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <manual>AvatarScreen shows EvolvingAvatar silhouette in main display area</manual>
  </verify>
  <done>AvatarScreen displays EvolvingAvatar with transition animation</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- Home screen "Avatar & Rank Section" shows EvolvingAvatar (not the old icon)
- AvatarScreen main display shows EvolvingAvatar (not the old icon)
- Avatar visually transitions when changing rank (test by manually setting dpStore.currentRank in devtools)
- Locked stages (3-5) show lock overlay on non-premium native builds
- No visual regression in other areas (mood/character info cards on AvatarScreen still work)
</verification>

<success_criteria>
- Home.tsx imports and renders EvolvingAvatar
- AvatarScreen.tsx imports and renders EvolvingAvatar
- Both screens have transition animation wrapper
- TypeScript compilation passes
- Avatar is prominently visible on home screen (AVATAR-03 satisfied)
</success_criteria>

<output>
After completion, create `.planning/phases/23-avatar-evolution/23-02-SUMMARY.md`
</output>
