---
phase: 23-avatar-evolution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/AvatarStages/Stage1.tsx
  - src/components/AvatarStages/Stage2.tsx
  - src/components/AvatarStages/Stage3.tsx
  - src/components/AvatarStages/Stage4.tsx
  - src/components/AvatarStages/Stage5.tsx
  - src/components/AvatarStages/index.ts
  - src/components/EvolvingAvatar.tsx
  - src/components/LockedAvatar.tsx
  - src/components/index.ts
autonomous: true
requirements:
  - AVATAR-01
  - AVATAR-02

must_haves:
  truths:
    - "EvolvingAvatar renders different SVG silhouette for each of the 5 stages"
    - "Avatar stage changes when user's rank crosses milestone thresholds (1, 4, 8, 12, 15)"
    - "Non-premium users see locked preview with upgrade prompt for stages 3-5"
  artifacts:
    - path: "src/components/EvolvingAvatar.tsx"
      provides: "Main avatar component with stage selection and premium gating"
      exports: ["EvolvingAvatar"]
    - path: "src/components/LockedAvatar.tsx"
      provides: "Locked preview overlay for premium stages"
      exports: ["LockedAvatar"]
    - path: "src/components/AvatarStages/index.ts"
      provides: "Barrel export for all 5 stage SVG components"
      exports: ["Stage1", "Stage2", "Stage3", "Stage4", "Stage5"]
  key_links:
    - from: "src/components/EvolvingAvatar.tsx"
      to: "src/screens/AvatarScreen.tsx"
      via: "imports getAvatarStage function"
      pattern: "import.*getAvatarStage.*from.*AvatarScreen"
    - from: "src/components/EvolvingAvatar.tsx"
      to: "src/stores/dpStore.ts"
      via: "useDPStore selector for currentRank"
      pattern: "useDPStore.*currentRank"
    - from: "src/components/EvolvingAvatar.tsx"
      to: "src/stores/subscriptionStore.ts"
      via: "useSubscriptionStore selector for isPremium"
      pattern: "useSubscriptionStore.*isPremium"
---

<objective>
Create the EvolvingAvatar component with 5-stage SVG silhouettes that change based on user rank, with premium gating for stages 3-5.

Purpose: Deliver the core avatar evolution visual that makes rank progression tangible and drives premium conversion.
Output: EvolvingAvatar component, LockedAvatar overlay, 5 stage SVG components (placeholder silhouettes).
</objective>

<execution_context>
@/Users/ejasper/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ejasper/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/23-avatar-evolution/23-RESEARCH.md
@src/screens/AvatarScreen.tsx (getAvatarStage function)
@src/components/PremiumGate.tsx (premium gating pattern)
@src/stores/dpStore.ts (currentRank state)
@src/stores/subscriptionStore.ts (isPremium state)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 5-Stage SVG Components</name>
  <files>
    src/components/AvatarStages/Stage1.tsx
    src/components/AvatarStages/Stage2.tsx
    src/components/AvatarStages/Stage3.tsx
    src/components/AvatarStages/Stage4.tsx
    src/components/AvatarStages/Stage5.tsx
    src/components/AvatarStages/index.ts
  </files>
  <action>
Create 5 inline SVG React components representing evolving silhouette stages:

1. Create `src/components/AvatarStages/` directory

2. For each Stage{N}.tsx (1-5):
   - Accept props: `size?: 'sm' | 'md' | 'lg' | 'xl'`, `className?: string`
   - Define SIZE_MAP: sm=64, md=96, lg=128, xl=192 (same as existing Avatar)
   - Render inline SVG with viewBox="0 0 100 100", width/height from SIZE_MAP
   - Use `fill="currentColor"` for theme compatibility
   - Add role="img" and aria-label for accessibility

3. Silhouette progression (placeholder shapes until artist delivers):
   - Stage 1 (Initiate): Simple thin figure outline
   - Stage 2 (Disciplined): Slightly more defined, shoulders visible
   - Stage 3 (Conditioned): Athletic build emerging
   - Stage 4 (Tempered): Muscular definition
   - Stage 5 (Master): Full impressive silhouette

4. Create barrel export `index.ts`:
   ```typescript
   export { Stage1 } from './Stage1'
   export { Stage2 } from './Stage2'
   export { Stage3 } from './Stage3'
   export { Stage4 } from './Stage4'
   export { Stage5 } from './Stage5'
   ```

Note: These are placeholder SVGs. STATE.md notes artist assets pending. Design for easy swap -- only the SVG path data needs to change when assets arrive.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <manual>Import Stage1-5 in a test file and confirm they render at all sizes</manual>
  </verify>
  <done>5 SVG stage components exist with consistent props interface and barrel export</done>
</task>

<task type="auto">
  <name>Task 2: Create EvolvingAvatar and LockedAvatar Components</name>
  <files>
    src/components/EvolvingAvatar.tsx
    src/components/LockedAvatar.tsx
    src/components/index.ts
  </files>
  <action>
1. Create `src/components/LockedAvatar.tsx`:
   - Props: `stage: 3 | 4 | 5`, `size?: 'sm' | 'md' | 'lg' | 'xl'`
   - Import Stage3, Stage4, Stage5 from AvatarStages
   - Render the stage SVG with opacity-40, grayscale, blur-[1px] filter
   - Overlay lock button that navigates to /paywall on click
   - Use Lock icon from lucide-react with "Unlock" text label
   - Style: absolute positioning, bg-background/60 overlay, centered content

2. Create `src/components/EvolvingAvatar.tsx`:
   - Props: `size?: 'sm' | 'md' | 'lg' | 'xl'`, `showLocked?: boolean` (default true)
   - Import getAvatarStage from '@/screens/AvatarScreen'
   - Import Stage1-5 from './AvatarStages'
   - Import LockedAvatar from './LockedAvatar'
   - Use Zustand selectors: `useDPStore((s) => s.currentRank)`, `useSubscriptionStore((s) => s.isPremium)`
   - Define STAGE_COMPONENTS array: [Stage1, Stage2, Stage3, Stage4, Stage5]
   - Define PREMIUM_STAGES: [3, 4, 5]
   - Logic:
     - Calculate stage via getAvatarStage(currentRank)
     - If isPremiumStage && !isPremium && showLocked: return LockedAvatar
     - Otherwise: return appropriate StageComponent

3. Update `src/components/index.ts`:
   - Add export: `export { EvolvingAvatar } from './EvolvingAvatar'`
   - Add export: `export { LockedAvatar } from './LockedAvatar'`
   - Do NOT export AvatarStages (internal implementation detail)
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <manual>EvolvingAvatar renders stage 1-2 unlocked, stages 3-5 show locked for non-premium</manual>
  </verify>
  <done>EvolvingAvatar component exists, wired to dpStore and subscriptionStore, premium stages gated</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- All 5 stage SVG components render at each size (sm, md, lg, xl)
- EvolvingAvatar shows correct stage based on currentRank (test with ranks 1, 5, 9, 13, 15)
- Non-premium users see LockedAvatar for stages 3-5 with paywall navigation
- Premium users (or web users) see unlocked stages 3-5
</verification>

<success_criteria>
- EvolvingAvatar component exported from @/components
- Stage changes at rank thresholds: 1-3->stage1, 4-7->stage2, 8-11->stage3, 12-14->stage4, 15->stage5
- Premium gating works: stages 3-5 locked for non-premium native users
- All TypeScript types pass without errors
</success_criteria>

<output>
After completion, create `.planning/phases/23-avatar-evolution/23-01-SUMMARY.md`
</output>
