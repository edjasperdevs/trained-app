---
phase: 04-monitoring-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/sentry.ts
  - src/App.tsx
  - src/components/ErrorBoundary.tsx
  - vite.config.ts
  - .gitignore
  - .env.sentry-build-plugin
  - .env.example
autonomous: true
user_setup:
  - service: sentry
    why: "Source map upload requires auth token"
    env_vars:
      - name: SENTRY_AUTH_TOKEN
        source: "Sentry Dashboard -> Settings -> Developer Settings -> Auth Tokens -> Create New Token (scopes: project:releases, org:read)"
      - name: SENTRY_ORG
        source: "Sentry Dashboard -> Settings -> General -> Organization Slug"
      - name: SENTRY_PROJECT
        source: "Sentry Dashboard -> Settings -> Projects -> Project Slug"

must_haves:
  truths:
    - "Sentry.init() includes reactRouterV6BrowserTracingIntegration with useEffect, useLocation, useNavigationType, createRoutesFromChildren, matchRoutes"
    - "Sentry.init() includes replayIntegration with maskAllText: false and custom PII masking selectors"
    - "Vite build produces hidden source maps and uploads them to Sentry via sentryVitePlugin"
    - "ErrorBoundary.tsx calls Sentry.captureException in componentDidCatch (TODO resolved)"
    - "Routes components in App.tsx are wrapped with withSentryReactRouterV6Routing for meaningful page names"
  artifacts:
    - path: "src/lib/sentry.ts"
      provides: "Sentry init with browserTracing + replay integrations"
      contains: "reactRouterV6BrowserTracingIntegration"
    - path: "src/App.tsx"
      provides: "SentryRoutes wrapper for route instrumentation"
      contains: "withSentryReactRouterV6Routing"
    - path: "vite.config.ts"
      provides: "Source map generation and upload"
      contains: "sentryVitePlugin"
    - path: "src/components/ErrorBoundary.tsx"
      provides: "Sentry error capture in boundary"
      contains: "captureException"
  key_links:
    - from: "src/lib/sentry.ts"
      to: "Sentry dashboard"
      via: "reactRouterV6BrowserTracingIntegration sends Core Web Vitals"
      pattern: "reactRouterV6BrowserTracingIntegration"
    - from: "vite.config.ts"
      to: "Sentry releases"
      via: "sentryVitePlugin uploads source maps on build"
      pattern: "sentryVitePlugin"
    - from: "src/App.tsx"
      to: "src/lib/sentry.ts"
      via: "SentryRoutes wraps Routes for page load tracking"
      pattern: "SentryRoutes"
---

<objective>
Activate Sentry performance tracing with React Router v6 integration, configure session replay with PII-aware defaults, set up source map upload via Vite plugin, and wire the ErrorBoundary to Sentry.

Purpose: MON-01 (browser tracing for Core Web Vitals), MON-02 (source maps for readable stack traces), MON-04 partial (replay integration configured with maskAllText: false + mask selectors).
Output: Updated sentry.ts, App.tsx, vite.config.ts, ErrorBoundary.tsx, and .gitignore for Sentry build plugin env file.
</objective>

<execution_context>
@/Users/ejasper/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ejasper/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-monitoring-hardening/04-RESEARCH.md

@src/lib/sentry.ts
@src/App.tsx
@src/main.tsx
@src/components/ErrorBoundary.tsx
@vite.config.ts
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure Sentry tracing, replay, source maps, and ErrorBoundary wiring</name>
  <files>
    src/lib/sentry.ts
    src/App.tsx
    src/components/ErrorBoundary.tsx
    vite.config.ts
    .gitignore
    .env.sentry-build-plugin
    .env.example
  </files>
  <action>
    **1. Install @sentry/vite-plugin:**
    ```
    npm install --save-dev @sentry/vite-plugin
    ```

    **2. Update src/lib/sentry.ts:**
    - Import `reactRouterV6BrowserTracingIntegration`, `replayIntegration`, and `withSentryReactRouterV6Routing` from `@sentry/react`
    - Import React Router hooks: `useEffect` from 'react', `useLocation`, `useNavigationType`, `createRoutesFromChildren`, `matchRoutes` from 'react-router-dom'
    - Add `integrations` array to Sentry.init():
      ```ts
      integrations: [
        reactRouterV6BrowserTracingIntegration({
          useEffect,
          useLocation,
          useNavigationType,
          createRoutesFromChildren,
          matchRoutes,
        }),
        replayIntegration({
          maskAllText: false,
          blockAllMedia: false,
          mask: ['[data-sentry-mask]'],
        }),
      ],
      ```
    - Change `replaysSessionSampleRate: 0.1` (10% of normal sessions for replay)
    - Keep `replaysOnErrorSampleRate: 1.0`
    - Export `withSentryReactRouterV6Routing` as a re-export for App.tsx to consume:
      ```ts
      export { withSentryReactRouterV6Routing } from '@sentry/react'
      ```

    **3. Update src/App.tsx:**
    - Import `withSentryReactRouterV6Routing` from `@/lib/sentry`
    - Import `Routes` as `OriginalRoutes` from 'react-router-dom' (or keep as Routes and alias the wrapped version)
    - Create `const SentryRoutes = withSentryReactRouterV6Routing(Routes)` at module level (OUTSIDE components)
    - Replace ALL 4 `<Routes>` usages in AppContent with `<SentryRoutes>`:
      1. Auth routes block (~line 134)
      2. Onboarding routes block (~line 147)
      3. Main routes block (~line 160)
      4. The existing `<Routes>` import from react-router-dom still needed for the wrapper -- import `Routes` and create `SentryRoutes`

    **4. Update src/components/ErrorBoundary.tsx:**
    - Import `captureError` from `@/lib/sentry` (the existing wrapper, NOT direct Sentry import -- avoids circular deps)
    - In `componentDidCatch`, replace the TODO comment with:
      ```ts
      captureError(error, { componentStack: errorInfo.componentStack ?? undefined })
      ```
    - Keep the console.error for dev visibility

    **5. Update vite.config.ts:**
    - Import `sentryVitePlugin` from '@sentry/vite-plugin'
    - Add `build.sourcemap: 'hidden'` to the config
    - Add `sentryVitePlugin()` as the LAST plugin in the plugins array:
      ```ts
      sentryVitePlugin({
        org: process.env.SENTRY_ORG,
        project: process.env.SENTRY_PROJECT,
        authToken: process.env.SENTRY_AUTH_TOKEN,
        sourcemaps: {
          filesToDeleteAfterUpload: ['./dist/**/*.map'],
        },
      }),
      ```
    - Note: The plugin reads from `.env.sentry-build-plugin` automatically AND from process.env. The explicit config ensures it works in CI too.

    **6. Create .env.sentry-build-plugin:**
    ```
    # Sentry Build Plugin Configuration
    # These values are used during `npm run build` to upload source maps
    # Get your auth token from: Sentry -> Settings -> Developer Settings -> Auth Tokens
    SENTRY_AUTH_TOKEN=
    SENTRY_ORG=
    SENTRY_PROJECT=
    ```

    **7. Update .gitignore:**
    - Add `.env.sentry-build-plugin` to the gitignored files (contains auth token)

    **8. Update .env.example:**
    - Add Sentry build plugin section:
      ```
      # Sentry Source Map Upload (Build-time only)
      # Required for readable stack traces in production
      # Create auth token: Sentry -> Settings -> Developer Settings -> Auth Tokens
      SENTRY_AUTH_TOKEN=your-sentry-auth-token
      SENTRY_ORG=your-sentry-org-slug
      SENTRY_PROJECT=your-sentry-project-slug
      ```

    **IMPORTANT NOTES:**
    - Do NOT use generic `browserTracingIntegration` -- use `reactRouterV6BrowserTracingIntegration` (gives route names like `/workouts` instead of generic URLs)
    - The `sentryVitePlugin` MUST be the last plugin in the array
    - Use `sourcemap: 'hidden'` (not `true`) to prevent serving maps to users
    - Pass hooks as REFERENCES (not invocations) to reactRouterV6BrowserTracingIntegration: `useEffect` not `useEffect()`
    - `withSentryReactRouterV6Routing(Routes)` must be called at MODULE LEVEL, not inside a component
  </action>
  <verify>
    1. `npx tsc --noEmit` passes with zero errors
    2. `npm run build 2>&1` completes (source map plugin will warn about missing auth token but build succeeds)
    3. Verify `dist/` contains `.js` files but NO `.map` files (filesToDeleteAfterUpload works, or maps are hidden)
    4. `grep -r "reactRouterV6BrowserTracingIntegration" src/lib/sentry.ts` returns a match
    5. `grep -r "SentryRoutes" src/App.tsx` returns matches (4 usages)
    6. `grep -r "captureError" src/components/ErrorBoundary.tsx` returns a match
    7. `grep -r "sentryVitePlugin" vite.config.ts` returns a match
    8. `grep ".env.sentry-build-plugin" .gitignore` returns a match
  </verify>
  <done>
    - Sentry.init() includes reactRouterV6BrowserTracingIntegration (Core Web Vitals will flow to Sentry)
    - Sentry.init() includes replayIntegration with maskAllText: false and [data-sentry-mask] selector
    - Vite builds with hidden source maps and sentryVitePlugin configured
    - ErrorBoundary.tsx captures exceptions to Sentry (TODO resolved)
    - All 4 Routes blocks in App.tsx wrapped with SentryRoutes
    - .env.sentry-build-plugin gitignored
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` -- zero type errors
2. `npm run build` -- completes without errors (source map upload may warn about missing token)
3. `npx vitest run` -- existing tests still pass
4. Sentry.init has both integrations configured
5. All Routes wrapped with SentryRoutes
6. ErrorBoundary wired to Sentry
7. Source map upload plugin configured as last Vite plugin
</verification>

<success_criteria>
- MON-01: reactRouterV6BrowserTracingIntegration is active in Sentry.init() -- Core Web Vitals will flow to Sentry when deployed
- MON-02: sentryVitePlugin configured with hidden source maps -- readable stack traces when auth token is set
- MON-04 (partial): replayIntegration configured with maskAllText: false and [data-sentry-mask] CSS selector -- replay infrastructure ready for PII masking
</success_criteria>

<output>
After completion, create `.planning/phases/04-monitoring-hardening/04-01-SUMMARY.md`
</output>
