---
phase: 31-splash-screen
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/AnimatedSplashScreen.tsx
  - src/assets/chain-link-crown.svg
autonomous: true
requirements: [SPLASH-01, SPLASH-02, SPLASH-03]

must_haves:
  truths:
    - "User sees chain-link crown logo centered on splash screen"
    - "User sees WELLTRAINED wordmark below logo"
    - "User sees FORGE YOUR LEGEND tagline below wordmark"
    - "User sees gold loading bar animating during initialization"
    - "Splash screen auto-transitions after minimum display time"
  artifacts:
    - path: "src/components/AnimatedSplashScreen.tsx"
      provides: "Splash screen with v2.2 branding"
      min_lines: 80
    - path: "src/assets/chain-link-crown.svg"
      provides: "Chain-link crown logo SVG"
  key_links:
    - from: "src/App.tsx"
      to: "AnimatedSplashScreen"
      via: "component render on mount"
      pattern: "<AnimatedSplashScreen"
---

<objective>
Implement the v2.2 Splash Screen with chain-link crown logo, WELLTRAINED wordmark, FORGE YOUR LEGEND tagline, and animated gold loading bar.

Purpose: Create the branded loading experience users see during app initialization, setting the Obsidian/gold visual tone for the auth flow.

Output: Updated AnimatedSplashScreen component matching the splash_screen_v3.png mockup.
</objective>

<execution_context>
@/Users/ejasper/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ejasper/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@Design inspo/mockups/auth flow/splash_screen_v3.png

Reference the mockup for exact visual layout:
- Chain-link crown logo (gold #D4A853) centered
- WELLTRAINED wordmark in gold below logo
- FORGE YOUR LEGEND tagline in warm gray below wordmark
- Gold loading bar at bottom with animation
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create chain-link crown SVG asset</name>
  <files>src/assets/chain-link-crown.svg</files>
  <action>
Create an inline SVG component or SVG file for the chain-link crown logo based on the mockup:

1. Create `src/assets/chain-link-crown.svg` as an SVG file that can be imported
2. The logo consists of:
   - A circular chain border made of interconnected chain links
   - Three crown prongs (trident-like) in the center
   - Gold color (#D4A853) for the entire logo
3. Size the SVG viewBox to be square (e.g., 200x200) for easy scaling
4. Use stroke and fill attributes that can accept currentColor for potential theming

The SVG should be optimized (no unnecessary attributes, clean paths). Reference the mockup splash_screen_v3.png for the exact design.

Note: If creating a complex SVG is not feasible, create a React component that draws the logo using SVG primitives, keeping it simple but recognizable as a chain-link crown.
  </action>
  <verify>
    <automated>ls -la src/assets/chain-link-crown.svg && grep -q 'svg' src/assets/chain-link-crown.svg</automated>
    <manual>SVG file exists and contains valid SVG markup</manual>
  </verify>
  <done>Chain-link crown SVG asset exists at src/assets/chain-link-crown.svg</done>
</task>

<task type="auto">
  <name>Task 2: Update AnimatedSplashScreen with v2.2 branding</name>
  <files>src/components/AnimatedSplashScreen.tsx</files>
  <action>
Replace the existing AnimatedSplashScreen content to match the v2.2 mockup:

1. **Import the chain-link crown SVG:**
   ```typescript
   import chainLinkCrownSvg from '@/assets/chain-link-crown.svg'
   ```
   Or if using a React SVG component, import that instead.

2. **Update the layout (top to bottom, vertically centered):**
   - Chain-link crown logo (w-40 h-40 or similar, sized prominently)
   - WELLTRAINED wordmark (text-4xl or text-5xl, Oswald font, gold #D4A853)
   - FORGE YOUR LEGEND tagline (text-xs tracking-[0.3em] uppercase, warm gray #8A8A8A)
   - Gold loading bar at bottom (positioned near bottom of screen)

3. **Animation sequence:**
   - Logo: fade-in with subtle scale (0.9 -> 1.0) over 400ms
   - Wordmark: fade-up (y: 20 -> 0) with 200ms delay
   - Tagline: fade-in with 400ms delay
   - Loading bar: animate width from 0% to 100% over ~1800ms (to fill most of splash duration)

4. **Loading bar implementation:**
   - Position fixed at bottom (bottom-20 or similar)
   - Width: w-48 or similar (centered)
   - Height: h-1 (thin bar)
   - Background track: #3A3A3A
   - Fill: #D4A853 (gold)
   - Use framer-motion to animate scaleX from 0 to 1, or width from 0 to 100%

5. **Background:** Keep bg-[#0A0A0A] (Obsidian)

6. **Timing:**
   - Keep minimum display time at 2200ms (existing behavior)
   - Loading bar animation should complete just before the fade-out begins
   - onComplete callback remains unchanged

7. **Remove old content:**
   - Remove hero-welcome.png import and usage
   - Remove "Enter The Protocol" tagline
   - Remove lime (#C8FF00) accent bar

Design tokens from mockup:
- Background: #0A0A0A
- Gold accent: #D4A853
- Warm white text: #F5F0E8
- Muted gray: #8A8A8A
  </action>
  <verify>
    <automated>grep -q "WELLTRAINED" src/components/AnimatedSplashScreen.tsx && grep -q "FORGE YOUR LEGEND" src/components/AnimatedSplashScreen.tsx && grep -q "D4A853" src/components/AnimatedSplashScreen.tsx</automated>
    <manual>Component contains wordmark, tagline, and gold color token</manual>
  </verify>
  <done>AnimatedSplashScreen displays chain-link crown, WELLTRAINED wordmark, FORGE YOUR LEGEND tagline, and animated gold loading bar</done>
</task>

<task type="auto">
  <name>Task 3: Verify splash screen renders and transitions</name>
  <files>src/App.tsx</files>
  <action>
Verify the splash screen integration in App.tsx:

1. **Confirm existing integration is intact:**
   - AnimatedSplashScreen is rendered in App component
   - showSplash state controls visibility
   - onComplete callback hides splash and reveals app content

2. **Test the component locally:**
   - Run `npm run dev` and observe splash screen on refresh
   - Verify:
     - Chain-link crown logo appears with fade-in animation
     - WELLTRAINED wordmark appears below with stagger delay
     - FORGE YOUR LEGEND tagline appears with additional delay
     - Gold loading bar fills from left to right
     - Splash fades out after ~2.2 seconds
     - App content (Auth/Main) appears after splash completes

3. **No changes needed to App.tsx** if existing integration works - just verify it still functions with the updated AnimatedSplashScreen.

4. **Run type check:**
   ```bash
   npx tsc --noEmit
   ```
   Ensure no TypeScript errors in modified files.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>TypeScript compilation passes</manual>
  </verify>
  <done>Splash screen renders correctly and auto-transitions to appropriate destination (Auth or Main)</done>
</task>

</tasks>

<verification>
1. `npm run dev` shows splash screen with chain-link crown logo, WELLTRAINED wordmark, and FORGE YOUR LEGEND tagline
2. Gold loading bar animates during splash display
3. Splash transitions smoothly to Auth screen (when not authenticated) or Main screen (when authenticated)
4. `npx tsc --noEmit` passes with no errors
</verification>

<success_criteria>
- [ ] Chain-link crown SVG asset created at src/assets/chain-link-crown.svg
- [ ] AnimatedSplashScreen displays logo, wordmark, tagline per mockup
- [ ] Gold loading bar animates during initialization
- [ ] Splash auto-transitions after display time completes
- [ ] TypeScript compilation passes
</success_criteria>

<output>
After completion, create `.planning/phases/31-splash-screen/31-01-SUMMARY.md`
</output>
