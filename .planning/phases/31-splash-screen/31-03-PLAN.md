---
phase: 31-splash-screen
plan: 03
type: execute
wave: 3
depends_on: [31-02]
files_modified:
  - src/components/AnimatedSplashScreen.tsx
  - src/assets/chain-link-crown.svg
autonomous: true
requirements: [SPLASH-01, SPLASH-02, SPLASH-03]

must_haves:
  truths:
    - "Splash screen matches mockup with pixel-level accuracy"
    - "All visual gaps from Plan 02 review are addressed"
    - "Animations are smooth and professionally timed"
  artifacts:
    - path: "src/components/AnimatedSplashScreen.tsx"
      provides: "Polished splash screen matching mockup"
      min_lines: 80
  key_links:
    - from: "src/App.tsx"
      to: "AnimatedSplashScreen"
      via: "component render"
      pattern: "<AnimatedSplashScreen"
---

<objective>
Apply pixel-polish refinements to the splash screen based on the Plan 02 review findings.

Purpose: Achieve mockup fidelity by addressing all documented visual gaps from the review phase.

Output: Polished AnimatedSplashScreen that matches splash_screen_v3.png mockup.
</objective>

<execution_context>
@/Users/ejasper/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ejasper/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/31-splash-screen/31-01-SUMMARY.md
@.planning/phases/31-splash-screen/31-02-SUMMARY.md
@.planning/phases/31-splash-screen/31-02-REVIEW.md
@Design inspo/mockups/auth flow/splash_screen_v3.png
@src/components/AnimatedSplashScreen.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply spacing and sizing refinements</name>
  <files>src/components/AnimatedSplashScreen.tsx</files>
  <action>
Read the 31-02-REVIEW.md file to get the specific refinement tasks.

Apply all spacing and sizing fixes identified in the review:

**Common refinements (adjust based on actual review findings):**

1. **Logo sizing:**
   - If too small: increase from w-40 to w-48 or w-52
   - If too large: decrease accordingly
   - Ensure aspect ratio is maintained

2. **Vertical spacing:**
   - Logo to wordmark gap: adjust mt-X or mb-X
   - Wordmark to tagline gap: adjust mt-X
   - Use consistent spacing tokens (mt-6, mt-8, etc.)

3. **Loading bar position:**
   - Adjust bottom-X to match mockup placement
   - If bar appears too high/low, modify the positioning

4. **Overall centering:**
   - Ensure content is vertically centered using flex/justify-center
   - If content appears shifted, adjust wrapper styles

Apply ONLY the specific fixes documented in 31-02-REVIEW.md. Do not make arbitrary changes.
  </action>
  <verify>
    <automated>grep -E "(w-[0-9]+|h-[0-9]+|mt-[0-9]+|mb-[0-9]+|bottom-[0-9]+)" src/components/AnimatedSplashScreen.tsx</automated>
  </verify>
  <done>Spacing and sizing values match review specifications</done>
</task>

<task type="auto">
  <name>Task 2: Apply typography and color refinements</name>
  <files>src/components/AnimatedSplashScreen.tsx</files>
  <action>
Apply typography and color fixes from the review:

1. **Wordmark typography:**
   - Font size: adjust text-Xx to match mockup (likely text-4xl to text-5xl range)
   - Letter spacing: adjust tracking-[X] if needed
   - Font weight: ensure font-bold or font-black as needed
   - Font family: verify Oswald is applied (`fontFamily: "'Oswald', sans-serif"`)

2. **Tagline typography:**
   - Font size: likely text-xs or text-sm
   - Letter spacing: tracking-[0.2em] to tracking-[0.3em] range
   - Color: ensure #8A8A8A (warm gray)
   - Uppercase: text-transform uppercase

3. **Color consistency:**
   - Logo: #D4A853 (gold)
   - Wordmark: #D4A853 (gold)
   - Tagline: #8A8A8A (warm gray)
   - Loading bar fill: #D4A853
   - Loading bar track: #3A3A3A or similar dark gray
   - Background: #0A0A0A

4. **Text rendering:**
   - Add antialiased for smoother text if not present
   - Ensure proper line-height (leading-none or leading-tight for display text)

Apply changes per the specific values documented in 31-02-REVIEW.md.
  </action>
  <verify>
    <automated>grep -E "(text-(xs|sm|base|lg|xl|[0-9]xl)|tracking-|#[A-Fa-f0-9]{6})" src/components/AnimatedSplashScreen.tsx</automated>
  </verify>
  <done>Typography and colors match mockup specifications</done>
</task>

<task type="auto">
  <name>Task 3: Refine animations and final polish</name>
  <files>src/components/AnimatedSplashScreen.tsx, src/assets/chain-link-crown.svg</files>
  <action>
Apply animation timing and final polish from the review:

1. **Animation timing adjustments:**
   - Logo fade-in: adjust duration (typically 300-500ms)
   - Logo scale: adjust spring parameters or duration
   - Wordmark stagger delay: adjust delay value
   - Tagline stagger delay: adjust delay value
   - Loading bar duration: should fill ~80% of total splash time (e.g., 1600-1800ms for 2200ms total)

2. **Animation easing:**
   - Use easeOut or critically damped springs (damping: 25, stiffness: 300)
   - Avoid bouncy animations per project patterns
   - Loading bar: use linear or easeOut for smooth fill

3. **SVG refinements (if needed):**
   - If logo SVG needs adjustments per review, edit src/assets/chain-link-crown.svg
   - Ensure stroke width is appropriate
   - Verify viewBox and dimensions

4. **Final checks:**
   - Remove any unused imports
   - Remove any console.log statements
   - Ensure component is clean and production-ready

5. **Type check:**
   ```bash
   npx tsc --noEmit
   ```
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Animations refined and TypeScript passes</done>
</task>

</tasks>

<verification>
1. All refinement tasks from 31-02-REVIEW.md are addressed
2. `npm run dev` shows splash screen matching mockup
3. Animations are smooth with appropriate timing
4. `npx tsc --noEmit` passes with no errors
5. No console warnings related to AnimatedSplashScreen
</verification>

<success_criteria>
- [ ] All visual gaps from review are fixed
- [ ] Splash screen matches mockup visually
- [ ] Logo, wordmark, tagline, loading bar all properly styled
- [ ] Animations are smooth and professionally timed
- [ ] TypeScript compilation passes
- [ ] Component is production-ready
</success_criteria>

<output>
After completion, create `.planning/phases/31-splash-screen/31-03-SUMMARY.md`
</output>
