---
phase: 28-archetype-and-macros
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/screens/onboarding-v2/ArchetypeScreen.tsx
  - src/screens/onboarding-v2/index.ts
  - src/navigation/OnboardingStack.tsx
autonomous: true
requirements: [ARCH-01, ARCH-02, ARCH-03, ARCH-04, ARCH-05, ARCH-06, ARCH-07]

must_haves:
  truths:
    - "User sees 5 archetype cards (Bro, Himbo, Brute, Pup, Bull) on archetype screen"
    - "Bro is pre-selected when screen loads"
    - "Bull card is dimmed and non-interactive"
    - "User can tap any archetype except Bull to select it"
    - "Selected archetype persists to dpStore when user taps CHOOSE MY ARCHETYPE"
  artifacts:
    - path: "src/screens/onboarding-v2/ArchetypeScreen.tsx"
      provides: "Archetype selection UI with 5 cards"
      min_lines: 120
  key_links:
    - from: "src/screens/onboarding-v2/ArchetypeScreen.tsx"
      to: "src/stores/dpStore.ts"
      via: "setArchetype call on continue"
      pattern: "setArchetype\\(selectedArchetype\\)"
    - from: "src/screens/onboarding-v2/ArchetypeScreen.tsx"
      to: "src/design/constants.ts"
      via: "ARCHETYPE_INFO import"
      pattern: "import.*ARCHETYPE_INFO.*from.*constants"
---

<objective>
Create the archetype selection screen for onboarding with 5 archetype cards matching the mockup design.

Purpose: Users choose their discipline specialization which affects DP earning bonuses. This screen showcases premium value before the paywall.
Output: ArchetypeScreen component with selectable archetype cards, badges (FREE/PREMIUM/COMING SOON), and navigation to macros screen.
</objective>

<execution_context>
@/Users/ejasper/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ejasper/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Design reference
@Design inspo/mockups/onboarding/onboarding_05_archetype_v2.png

# Existing patterns
@src/screens/onboarding-v2/GoalScreen.tsx
@src/design/constants.ts
@src/stores/onboardingStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ArchetypeScreen component</name>
  <files>src/screens/onboarding-v2/ArchetypeScreen.tsx</files>
  <action>
Create ArchetypeScreen.tsx matching the mockup design:

**Layout (top to bottom):**
1. Header with back button (ChevronLeft), ProgressIndicator (totalSteps=5, currentStep=3)
2. "YOUR ARCHETYPE" label in gold (#D4A853), tracking-[0.2em], uppercase, centered
3. "CHOOSE YOUR DISCIPLINE" headline in Oswald font, text-3xl, centered
4. 5 archetype cards in vertical stack with gap-3

**Archetype card structure (per mockup):**
- Horizontal layout with avatar image placeholder on left, text on right
- Card height ~80px, rounded-xl, bg-[#26282B] border border-[#3F3F46]
- Left side: placeholder div with gradient background (actual images come later)
- Right side: archetype name (Oswald, bold), tagline below (text-sm, text-muted)
- Badge in top-right corner of card:
  - Bro: "FREE" badge in green (#22C55E) bg, black text, text-xs, px-2 py-0.5, rounded
  - Himbo/Brute/Pup: "PREMIUM" badge in gold (#D4A853) bg, black text
  - Bull: "COMING SOON" badge in gray (#3F3F46) bg, white text

**State and behavior:**
- Local state: `selectedArchetype` initialized to 'bro'
- Bull card: opacity-40, pointer-events-none (non-interactive)
- Selected card: bg-[rgba(212,168,83,0.08)] border-2 border-[#D4A853]
- Unselected cards: bg-[#26282B] border border-[#3F3F46]
- On card tap (except Bull): haptics.light(), setSelectedArchetype()

**Bottom CTA:**
- "CHOOSE MY ARCHETYPE" button in gold (#D4A853), full-width
- On tap: update onboardingStore.data.archetype, then nextStep()

Use ARCHETYPE_INFO from constants.ts for name/tagline. Use existing animation patterns from GoalScreen (fadeInVariants, containerVariants, cardVariants).
  </action>
  <verify>
    <automated>cd /Users/ejasper/code/trained-app && npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Check that ArchetypeScreen imports compile without errors</manual>
  </verify>
  <done>ArchetypeScreen component renders 5 archetype cards with correct badges, Bro selected by default, Bull dimmed</done>
</task>

<task type="auto">
  <name>Task 2: Export ArchetypeScreen and wire to OnboardingStack</name>
  <files>src/screens/onboarding-v2/index.ts, src/navigation/OnboardingStack.tsx</files>
  <action>
**In src/screens/onboarding-v2/index.ts:**
1. Add export: `export { ArchetypeScreen } from './ArchetypeScreen'`
2. Remove OnboardingArchetype from PlaceholderScreens export (it's now replaced)

**In src/navigation/OnboardingStack.tsx:**
1. Update import to include ArchetypeScreen: `import { ..., ArchetypeScreen } from '@/screens/onboarding-v2'`
2. Replace `<Route path="archetype" element={<OnboardingArchetype />} />` with `<Route path="archetype" element={<ArchetypeScreen />} />`
  </action>
  <verify>
    <automated>cd /Users/ejasper/code/trained-app && npm run build 2>&1 | tail -10</automated>
    <manual>Build succeeds with ArchetypeScreen wired to OnboardingStack</manual>
  </verify>
  <done>ArchetypeScreen accessible at /onboarding/archetype route, navigation from GoalScreen works</done>
</task>

</tasks>

<verification>
1. `npm run build` passes without errors
2. Navigation flow: GoalScreen -> ArchetypeScreen -> MacrosScreen (placeholder)
3. Bro card shows "FREE" badge and is pre-selected
4. Premium archetypes (Himbo, Brute, Pup) show "PREMIUM" badges
5. Bull card shows "COMING SOON" badge and is non-interactive (dimmed)
6. Selecting different archetype updates local state, shows gold border
7. CHOOSE MY ARCHETYPE stores selection and navigates forward
</verification>

<success_criteria>
- ArchetypeScreen renders all 5 archetype cards matching mockup layout
- Badge colors: FREE=#22C55E, PREMIUM=#D4A853, COMING SOON=#3F3F46
- Bro is selected by default with gold border
- Bull is visually dimmed (40% opacity) and cannot be selected
- Selection triggers haptic feedback
- Continue stores archetype in onboardingStore and advances to macros step
</success_criteria>

<output>
After completion, create `.planning/phases/28-archetype-and-macros/28-01-SUMMARY.md`
</output>
