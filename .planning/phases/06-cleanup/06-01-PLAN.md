---
phase: 06-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/themes/ (DELETE)
autonomous: true

must_haves:
  truths:
    - "src/themes/ directory does not exist"
    - "No legacy CSS rules (.theme-trained, .theme-gyg, injectCSSVariables) exist in any file"
    - "tsc --noEmit passes with zero errors"
    - "Full test suite passes"
  artifacts:
    - path: "src/themes/"
      provides: "Directory deleted"
  key_links: []
---

<objective>
Delete the src/themes/ directory and verify the codebase is clean with all tests passing.

Purpose: The themes directory is a tombstone from Phase 2 — it contains only a comment explaining where things moved. Deleting it completes the theme removal. We also verify no legacy CSS selectors remain and run the full test suite.

Output: Clean codebase with no theme infrastructure, all TypeScript checks pass, all tests pass.
</objective>

<execution_context>
@/Users/ejasper/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ejasper/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete src/themes/ directory</name>
  <files>
    src/themes/
  </files>
  <action>
  Delete the entire src/themes/ directory:
  ```bash
  rm -rf src/themes/
  ```

  The directory currently contains only index.ts with a tombstone comment — no code depends on it.

  After deletion, verify no imports reference src/themes:
  ```bash
  grep -rn "from.*themes" src/ --include="*.tsx" --include="*.ts"
  ```
  This should return 0 matches.
  </action>
  <verify>
  Run: `ls src/themes/ 2>&1` -- should show "No such file or directory"
  Run: `grep -rn "from.*themes" src/ --include="*.tsx" --include="*.ts"` -- should return 0 matches
  </verify>
  <done>
  - src/themes/ directory deleted
  - No remaining imports reference the deleted directory
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify no legacy CSS rules remain</name>
  <files>
    (verification only)
  </files>
  <action>
  Verify that no legacy theme-related CSS selectors or functions exist:

  1. Check for .theme-trained and .theme-gyg selectors:
     ```bash
     grep -rn "\.theme-trained\|\.theme-gyg" src/
     ```
     Should return 0 matches.

  2. Check for injectCSSVariables function:
     ```bash
     grep -rn "injectCSSVariables" src/
     ```
     Should return 0 matches.

  3. Check for any remaining theme-related patterns:
     ```bash
     grep -rn "ThemeProvider\|useTheme\|isTrained" src/ --include="*.tsx" --include="*.ts"
     ```
     Should return 0 matches.

  If any matches found, delete the offending code.
  </action>
  <verify>
  Run: `grep -rn "\.theme-trained\|\.theme-gyg\|injectCSSVariables\|ThemeProvider\|useTheme\|isTrained" src/ --include="*.tsx" --include="*.ts" | wc -l` -- should return 0
  </verify>
  <done>
  - No legacy CSS selectors (.theme-trained, .theme-gyg) exist
  - No legacy functions (injectCSSVariables, useTheme) exist
  - No theme branching logic (isTrained) exists
  </done>
</task>

<task type="auto">
  <name>Task 3: Run full verification suite</name>
  <files>
    (verification only)
  </files>
  <action>
  Run the full verification suite to confirm the codebase is clean:

  1. TypeScript check:
     ```bash
     npx tsc --noEmit
     ```
     Should pass with zero errors.

  2. Full test suite:
     ```bash
     npm test
     ```
     Should pass all tests.

  3. Production build:
     ```bash
     npx vite build
     ```
     Should succeed.

  If any failures, investigate and fix.
  </action>
  <verify>
  Run: `npx tsc --noEmit` -- passes
  Run: `npm test` -- all tests pass
  Run: `npx vite build` -- builds successfully
  </verify>
  <done>
  - tsc --noEmit passes with zero errors
  - Full test suite passes (all tests green)
  - Production build succeeds
  - CLEAN-01 and CLEAN-02 requirements satisfied
  </done>
</task>

</tasks>

<verification>
1. `ls src/themes/ 2>&1` -- "No such file or directory"
2. `grep -c "\.theme-trained\|\.theme-gyg\|injectCSSVariables" src/**/*.{ts,tsx,css}` -- all 0
3. `npx tsc --noEmit` passes
4. `npm test` passes
5. `npx vite build` succeeds
</verification>

<success_criteria>
- CLEAN-01 met: src/themes/ directory deleted, no legacy CSS rules exist
- CLEAN-02 met: tsc --noEmit passes, full test suite passes
- Build succeeds with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/06-cleanup/06-01-SUMMARY.md`
</output>
