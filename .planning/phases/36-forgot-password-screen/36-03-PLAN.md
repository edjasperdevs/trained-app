---
phase: 36-forgot-password-screen
plan: 03
type: execute
wave: 3
depends_on:
  - 36-02
files_modified:
  - src/screens/auth-screens/ForgotPasswordScreen.tsx
autonomous: true
requirements:
  - FORGOT-01
  - FORGOT-02
  - FORGOT-03
  - FORGOT-04

must_haves:
  truths:
    - "Forgot Password screen pixel-perfect matches mockup"
    - "All visual gaps from Plan 02 review are resolved"
    - "Form and success state animations are smooth"
  artifacts:
    - path: "src/screens/auth-screens/ForgotPasswordScreen.tsx"
      provides: "Pixel-polished Forgot Password screen"
      min_lines: 200
  key_links: []
---

<objective>
Pixel-polish the Forgot Password screen based on Plan 02 review findings, or verify if no gaps found.

Purpose: Ensure mockup fidelity and smooth user experience
Output: Production-ready Forgot Password screen matching design specifications
</objective>

<execution_context>
@/Users/ejasper/.claude/agents/gsd-executor.md
@/Users/ejasper/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/36-forgot-password-screen/36-01-SUMMARY.md
@.planning/phases/36-forgot-password-screen/36-02-SUMMARY.md
@.planning/phases/36-forgot-password-screen/36-02-REVIEW.md
@Design inspo/mockups/auth flow/auth_forgot_password.png
@src/screens/auth-screens/ForgotPasswordScreen.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply visual refinements from Plan 02 review</name>
  <files>src/screens/auth-screens/ForgotPasswordScreen.tsx</files>
  <action>
Read 36-02-REVIEW.md for specific gaps identified during user review.

**If review approved with zero gaps:**
- Run verification checks only
- Confirm TypeScript compilation passes
- No code changes needed

**If review identified gaps:**
Apply fixes for each gap documented. Common refinement areas:

**Spacing adjustments:**
- Logo positioning and margins
- Icon-to-headline spacing (typically mt-6 or mt-8)
- Headline-to-subline spacing (typically mt-2)
- Field-to-button spacing (typically mt-6)
- Overall vertical rhythm

**Typography refinements:**
- Font weights (Oswald for headlines)
- Font sizes (text-2xl for headlines, text-sm for sublines)
- Letter spacing (tracking-wide for headlines)
- Text colors match exact hex values

**Icon sizing:**
- KeyRound should be 64px (size={64})
- Mail icon should be 20px (w-5 h-5)
- CheckCircle should be 64px in success state

**Button styling:**
- Full width with max-w-sm
- Height h-14
- Border radius rounded-full
- Disabled state opacity-50
- Loading state text

**Focus states:**
- Email input focus ring: ring-2 ring-[#D4A853] ring-opacity-50
- Smooth transitions

**Success state transition:**
- Optional fade-in animation for success state
- Smooth state swap with conditional rendering

Make minimal, targeted changes addressing only documented gaps.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <manual>Visual comparison: refined screen matches mockup perfectly</manual>
  </verify>
  <done>All visual gaps from review are resolved, screen matches mockup pixel-perfect</done>
</task>

<task type="auto">
  <name>Task 2: Add polish and smooth transitions</name>
  <files>src/screens/auth-screens/ForgotPasswordScreen.tsx</files>
  <action>
Add subtle polish for production-ready feel:

**Button interaction:**
- Hover state (if not already present)
- Active state during loading
- Smooth transitions for disabled/enabled states

**Success state transition:**
- Consider fade-in animation for success state appearance
- Smooth swap between form and success states

**Focus management:**
- Auto-focus email input on mount (optional, test for UX)
- Clear focus ring appearance

**Loading state:**
- Button shows "Sending..." during API call
- Cursor not-allowed when disabled

**Error prevention:**
- Form prevents double-submission
- Enter key submits form when valid

**Accessibility:**
- ARIA labels on icon-only buttons (back arrow)
- Form submit handler on form element (not just button click)

Keep refinements subtle - no over-animation or excessive effects. Match the minimal, focused style of other auth screens.
  </action>
  <verify>
    <automated>grep -E "(transition|animation|hover:)" src/screens/auth-screens/ForgotPasswordScreen.tsx</automated>
    <manual>Interactions feel smooth and responsive</manual>
  </verify>
  <done>Screen has polished interactions and smooth state transitions</done>
</task>

<task type="auto">
  <name>Task 3: Final verification and commit</name>
  <files>src/screens/auth-screens/ForgotPasswordScreen.tsx</files>
  <action>
Run final verification checks:

1. **TypeScript compilation:**
   ```bash
   npx tsc --noEmit
   ```
   Must pass with zero errors.

2. **Visual verification:**
   - Screen matches mockup in both form and success states
   - All elements properly sized and spaced
   - Colors match design tokens
   - Transitions are smooth

3. **Functional verification:**
   - Email validation works correctly
   - Button states (disabled/enabled/loading) work
   - Form submission triggers Supabase call
   - Success state displays with correct message
   - All navigation links work
   - Back arrow navigation works

4. **Integration verification:**
   - EmailSignInScreen "Forgot Password?" link navigates correctly
   - Success state "Back to Sign In" returns to signin
   - Deep link URL (welltrained://reset-password) is correct

If all checks pass, screen is production-ready for Phase 36 completion.
  </action>
  <verify>
    <automated>npx tsc --noEmit && echo "TypeScript OK" && grep -q "resetPasswordForEmail" src/screens/auth-screens/ForgotPasswordScreen.tsx && echo "Integration OK"</automated>
    <manual>Complete user flow works: navigate to forgot password, submit email, see success, return to signin</manual>
  </verify>
  <done>All verification checks pass, Forgot Password screen is production-ready</done>
</task>

</tasks>

<verification>
Final checks confirm:
```bash
# TypeScript compilation
npx tsc --noEmit

# Key implementation present
grep -E "(resetPasswordForEmail|CHECK YOUR INBOX|isSuccess)" src/screens/auth-screens/ForgotPasswordScreen.tsx
```

Manual flow:
1. Navigate to /auth/forgot-password
2. Submit valid email
3. See success state with confirmation
4. Click "Back to Sign In"
5. Return to /auth/signin

Phase 36 complete when all success criteria met.
</verification>

<success_criteria>
- [x] All visual gaps from Plan 02 review are fixed
- [x] Screen matches auth_forgot_password.png mockup
- [x] Form and success state transitions are smooth
- [x] Email validation works correctly
- [x] Supabase resetPasswordForEmail integration works
- [x] Success state shows confirmation with email
- [x] All navigation links work correctly
- [x] TypeScript compilation passes
- [x] No console errors or warnings
- [x] Ready for v2.2 Auth Flow Redesign milestone completion
</success_criteria>

<output>
After completion, create `.planning/phases/36-forgot-password-screen/36-03-SUMMARY.md`

Phase 36 complete. Phase 35 Plan 03 likely next for Email Sign In Form refinement, then v2.2 milestone complete (Phases 30-36).
</output>
