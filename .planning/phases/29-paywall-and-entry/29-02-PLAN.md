---
phase: 29-paywall-and-entry
plan: 02
type: execute
wave: 2
depends_on: [29-01]
files_modified:
  - src/screens/onboarding-v2/FinalScreen.tsx
  - src/screens/onboarding-v2/index.ts
  - src/navigation/OnboardingStack.tsx
autonomous: true
requirements: [FINAL-01, FINAL-02, FINAL-03, FINAL-04, FINAL-05, FINAL-06, FINAL-07, FINAL-08]

must_haves:
  truths:
    - "User sees Welcome to Protocol screen with avatar, rank card, and ENTER THE DISCIPLINE CTA"
    - "Avatar fades in with scale-up animation (0.95 to 1.0 over 400ms)"
    - "Rank card shows UNINITIATED with 0 of 250 DP to Initiate"
    - "CTA pulses once after 1 second"
    - "Tapping CTA navigates to Home tab and sets onboardingComplete to true"
  artifacts:
    - path: "src/screens/onboarding-v2/FinalScreen.tsx"
      provides: "Cinematic onboarding completion screen"
      min_lines: 120
  key_links:
    - from: "src/screens/onboarding-v2/FinalScreen.tsx"
      to: "userStore.completeOnboarding"
      via: "CTA tap handler"
      pattern: "completeOnboarding\\(\\)"
    - from: "src/screens/onboarding-v2/FinalScreen.tsx"
      to: "onboardingStore.reset"
      via: "CTA tap handler"
      pattern: "\\.reset\\(\\)"
---

<objective>
Create FinalScreen (Welcome to Protocol) as the cinematic completion of onboarding with avatar reveal, rank card, and entry CTA.

Purpose: Reward user for completing onboarding and teach them what they're working toward (first rank).
Output: FinalScreen.tsx wired into OnboardingStack at /final route
</objective>

<execution_context>
@/Users/ejasper/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ejasper/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/components/EvolvingAvatar.tsx
@src/components/DPDisplay.tsx
@src/stores/userStore.ts (completeOnboarding action)
@src/stores/onboardingStore.ts (reset action)
@src/stores/dpStore.ts (RANKS array for threshold)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create FinalScreen component</name>
  <files>src/screens/onboarding-v2/FinalScreen.tsx</files>
  <action>
Create FinalScreen.tsx with these elements:

**Layout:**
- Full-screen dark background (#0A0A0A)
- Centered content with vertical arrangement
- Subtle radial gradient glow at center (gold tint)

**Header section:**
- "WELCOME TO" small gold label (#D4A853, tracking-[0.2em])
- "THE PROTOCOL" large headline (Oswald, white, 3xl)

**Avatar section (FINAL-02):**
- Use EvolvingAvatar component with size="xl" (240px)
- Avatar should NOT be wrapped with showLocked logic for this screen (always show stage 1)
- Animation: motion.div wrapper with fade-in + scale (initial: { opacity: 0, scale: 0.95 }, animate: { opacity: 1, scale: 1 }, duration 400ms, ease out)

**Rank card (FINAL-03, FINAL-04, FINAL-05):**
- Card background: #26282B with rounded-xl, gold border (#D4A853)
- Rank name: "UNINITIATED" (hardcoded for initial state)
- Progress bar at 0%
- Text below: "0 of 250 DP to Initiate" (use RANKS[1].threshold for 250)
- Animation: slide up from below (initial: { opacity: 0, y: 20 }, animate: { opacity: 1, y: 0 }, duration 400ms, delay 200ms)

**CTA button (FINAL-06):**
- "ENTER THE DISCIPLINE" text (Oswald font, uppercase)
- Full-width gold button (#D4A853 bg, #0A0A0A text)
- Pulse animation: after 1 second delay, single scale pulse (1.0 -> 1.02 -> 1.0)
- Use useState + setTimeout for pulse trigger

**CTA handler (FINAL-07, FINAL-08):**
```tsx
const handleEnter = () => {
  // Reset onboarding store state
  onboardingStore.reset()

  // Mark profile as onboarding complete
  completeOnboarding()

  // Navigate to home (App.tsx will handle routing since onboardingComplete is now true)
}
```

Import notes:
- Import EvolvingAvatar from '@/components/EvolvingAvatar'
- Import RANKS from dpStore for threshold value
- Import useUserStore for completeOnboarding
- Import useOnboardingStore for reset

Do NOT import or use DPDisplay - create custom rank card for this screen to control the exact display (UNINITIATED, 0 of 250).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <manual>Verify FinalScreen renders with avatar, rank card, and CTA</manual>
  </verify>
  <done>FinalScreen component created with avatar animation, rank card, and entry CTA with pulse</done>
</task>

<task type="auto">
  <name>Task 2: Export FinalScreen and wire to OnboardingStack</name>
  <files>src/screens/onboarding-v2/index.ts, src/navigation/OnboardingStack.tsx</files>
  <action>
**In src/screens/onboarding-v2/index.ts:**
- Export FinalScreen from './FinalScreen'
- Remove OnboardingFinal from PlaceholderScreens export (if still there)

**In src/navigation/OnboardingStack.tsx:**
- Replace OnboardingFinal with FinalScreen in imports
- Update Route element for path="final" to use FinalScreen

Verify imports compile and route is correctly wired.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build</automated>
    <manual>Verify FinalScreen loads at /onboarding/final route</manual>
  </verify>
  <done>FinalScreen exported and wired to OnboardingStack final route</done>
</task>

<task type="auto">
  <name>Task 3: Verify complete onboarding flow</name>
  <files></files>
  <action>
Run full build and verify:
1. `npm run build` succeeds with no errors
2. All 8 onboarding screens are properly wired (check OnboardingStack.tsx):
   - welcome -> WelcomeScreen
   - value -> ValueScreen
   - profile -> ProfileScreen
   - goal -> GoalScreen
   - archetype -> ArchetypeScreen
   - macros -> MacrosScreen
   - paywall -> PaywallScreen
   - final -> FinalScreen

3. Verify PlaceholderScreens.tsx no longer exports OnboardingPaywall or OnboardingFinal (these were replaced with real implementations)

This is a verification task - no file modifications expected.
  </action>
  <verify>
    <automated>npm run build</automated>
    <manual>Verify all 8 screens render in sequence</manual>
  </verify>
  <done>Complete onboarding flow verified with all 8 screens implemented</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes
2. `npm run build` succeeds
3. FinalScreen shows avatar with scale-up fade-in animation
4. Rank card shows UNINITIATED with 0/250 DP text
5. Rank card slides up with 200ms delay
6. CTA pulses once after 1 second
7. Tapping CTA sets onboardingComplete and routes to main app
8. All 8 onboarding screens are real implementations (no placeholders)
</verification>

<success_criteria>
- FinalScreen component exists with all required elements
- Avatar animation works (fade-in + scale)
- Rank card animation works (slide-up)
- CTA pulse animation triggers after 1s
- completeOnboarding and reset called on CTA tap
- App routes to Home tab after onboarding complete
- Full flow works: welcome -> value -> profile -> goal -> archetype -> macros -> paywall -> final -> home
</success_criteria>

<output>
After completion, create `.planning/phases/29-paywall-and-entry/29-02-SUMMARY.md`
</output>
