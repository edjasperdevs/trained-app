---
phase: 29-paywall-and-entry
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/screens/onboarding-v2/PaywallScreen.tsx
  - src/screens/onboarding-v2/index.ts
  - src/navigation/OnboardingStack.tsx
autonomous: true
requirements: [PAY-01, PAY-02, PAY-03, PAY-04, PAY-05, PAY-06, PAY-07, PAY-08]

must_haves:
  truths:
    - "User sees paywall with monthly (gold border, MOST POPULAR) and annual subscription options after macros screen"
    - "User can tap START FREE TRIAL to initiate RevenueCat purchase"
    - "User can tap Continue with free access to skip paywall and get 7-day reverse trial"
    - "Paywall has no back arrow - it is a decision point"
    - "Already premium users skip paywall automatically"
  artifacts:
    - path: "src/screens/onboarding-v2/PaywallScreen.tsx"
      provides: "Onboarding paywall with subscription options"
      min_lines: 150
  key_links:
    - from: "src/screens/onboarding-v2/PaywallScreen.tsx"
      to: "subscriptionStore.purchase"
      via: "RevenueCat purchase flow"
      pattern: "purchase\\(.*\\)"
    - from: "src/navigation/OnboardingStack.tsx"
      to: "PaywallScreen"
      via: "route element"
      pattern: "element=\\{<PaywallScreen"
---

<objective>
Create PaywallScreen for the onboarding flow with premium subscription options, reverse trial on skip, and already-premium skip logic.

Purpose: Convert users to premium after they have invested in profile setup, or grant 7-day free trial on skip.
Output: PaywallScreen.tsx wired into OnboardingStack at /paywall route
</objective>

<execution_context>
@/Users/ejasper/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ejasper/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/screens/Paywall.tsx (existing paywall for reference)
@src/stores/subscriptionStore.ts
@src/stores/onboardingStore.ts
@src/screens/onboarding-v2/MacrosScreen.tsx (previous screen pattern)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PaywallScreen component</name>
  <files>src/screens/onboarding-v2/PaywallScreen.tsx</files>
  <action>
Create PaywallScreen.tsx with these elements:

**Layout (no header/back button - PAY-07):**
- Full-screen dark background (#0A0A0A)
- Subtle radial gradient glow at bottom (gold tint)
- No back arrow or progress indicator - this is a decision point

**Hero section:**
- "YOUR PROTOCOL IS READY" headline with gold accent
- Subline explaining 7-day free trial

**Subscription cards:**
- Monthly card: $9.99/mo with "7-DAY FREE TRIAL" prominently displayed, gold border (#D4A853), "MOST POPULAR" badge (PAY-02)
- Annual card: $59.99/year with "SAVE 50%" callout, secondary styling (PAY-03)
- Use subscriptionStore.offerings.current.monthly and .annual for dynamic pricing

**CTAs:**
- "START FREE TRIAL" primary gold button triggers purchase flow (PAY-04)
- "Restore Purchases" small link
- "Continue with free access" secondary link at bottom (PAY-05)

**Skip logic (reverse trial - PAY-06):**
- When user taps "Continue with free access":
  - Call nextStep() to proceed to final screen
  - The reverse trial entitlement is handled by RevenueCat server-side - no client action needed
  - Log analytics.paywallSkipped() if analytics exists

**Already premium check (PAY-08):**
- useEffect on mount: if isPremium is true, immediately call nextStep() to skip paywall

**Purchase flow:**
- On successful purchase: toast.success('Welcome to Premium!'), nextStep()
- On cancellation: do nothing (silent)
- On error: toast.error with message

**Offerings loading state:**
- Show loading spinner while offerings load
- After 5s timeout without offerings, show "Continue to App" fallback

Use patterns from existing Paywall.tsx but adapted for onboarding:
- No X close button (no back navigation)
- Uses nextStep() instead of navigate('/')
- Integrates with onboardingStore flow

Animation: Cards fade in with stagger (delay 0.4, 0.6)
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <manual>Verify PaywallScreen renders with subscription cards</manual>
  </verify>
  <done>PaywallScreen component created with subscription options, purchase flow, reverse trial skip, and premium bypass</done>
</task>

<task type="auto">
  <name>Task 2: Export PaywallScreen and wire to OnboardingStack</name>
  <files>src/screens/onboarding-v2/index.ts, src/navigation/OnboardingStack.tsx</files>
  <action>
**In src/screens/onboarding-v2/index.ts:**
- Export PaywallScreen from './PaywallScreen'
- Remove OnboardingPaywall from PlaceholderScreens export (if still there)

**In src/navigation/OnboardingStack.tsx:**
- Replace OnboardingPaywall with PaywallScreen in imports
- Update Route element for path="paywall" to use PaywallScreen

Verify imports compile and route is correctly wired.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build</automated>
    <manual>Verify PaywallScreen loads at /onboarding/paywall route</manual>
  </verify>
  <done>PaywallScreen exported and wired to OnboardingStack paywall route</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes
2. `npm run build` succeeds
3. PaywallScreen displays subscription cards with correct pricing from offerings
4. Tapping "START FREE TRIAL" initiates RevenueCat purchase
5. Tapping "Continue with free access" proceeds to final screen
6. Already premium users skip paywall automatically
7. No back button visible on paywall screen
</verification>

<success_criteria>
- PaywallScreen component exists with all required elements
- Monthly card has gold border and MOST POPULAR badge
- Annual card shows save percentage
- Purchase and skip flows work correctly
- Premium bypass on mount works
- Component wired to OnboardingStack
</success_criteria>

<output>
After completion, create `.planning/phases/29-paywall-and-entry/29-01-SUMMARY.md`
</output>
