---
phase: 19-subscriptions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/stores/subscriptionStore.ts
  - src/stores/index.ts
  - src/lib/revenuecat.ts
  - src/App.tsx
  - ios/App/App/App.entitlements
  - package.json
  - package-lock.json
autonomous: true
requirements: [SUB-01, SUB-05]

user_setup:
  - service: revenuecat
    why: "iOS in-app purchase subscription management"
    env_vars:
      - name: VITE_REVENUECAT_IOS_API_KEY
        source: "RevenueCat Dashboard -> Project Settings -> API Keys -> iOS app-specific API key"
    dashboard_config:
      - task: "Create RevenueCat project and connect to App Store Connect"
        location: "RevenueCat Dashboard -> Project Settings -> App Store Connect"
      - task: "Create 'premium' entitlement and map to subscription products"
        location: "RevenueCat Dashboard -> Entitlements"
      - task: "Create monthly and annual offerings"
        location: "RevenueCat Dashboard -> Offerings"
  - service: app_store_connect
    why: "IAP product creation"
    dashboard_config:
      - task: "Create auto-renewable subscription products (monthly, annual)"
        location: "App Store Connect -> Monetization -> In-App Purchases"
      - task: "Add sandbox tester account"
        location: "App Store Connect -> Users and Access -> Sandbox"

must_haves:
  truths:
    - "RevenueCat SDK initializes with Supabase user ID on native app launch"
    - "Subscription status (isPremium) is cached locally and persists across app restarts"
    - "App shows loading state until subscription status is known on native"
    - "Web platform runs without RevenueCat errors (all calls guarded)"
  artifacts:
    - path: "src/stores/subscriptionStore.ts"
      provides: "Zustand store with isPremium, offerings, purchase actions"
      exports: ["useSubscriptionStore"]
    - path: "src/lib/revenuecat.ts"
      provides: "SDK initialization and configuration helper"
      exports: ["initializeRevenueCat", "loginToRevenueCat", "logoutFromRevenueCat"]
  key_links:
    - from: "src/App.tsx"
      to: "src/lib/revenuecat.ts"
      via: "initializeRevenueCat call after auth"
      pattern: "initializeRevenueCat.*user\\.id"
    - from: "src/stores/subscriptionStore.ts"
      to: "@revenuecat/purchases-capacitor"
      via: "Purchases SDK methods"
      pattern: "Purchases\\.(getCustomerInfo|purchasePackage|restorePurchases)"
---

<objective>
Install RevenueCat SDK, create subscriptionStore with Zustand persist, wire initialization into App.tsx, and configure Xcode entitlements.

Purpose: Establish subscription infrastructure that future plans depend on for premium feature gating.
Output: Working subscriptionStore with isPremium selector, SDK initialization on auth, Xcode IAP capability.
</objective>

<execution_context>
@/Users/ejasper/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ejasper/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-subscriptions/19-RESEARCH.md
@src/stores/accessStore.ts (Zustand persist pattern)
@src/stores/dpStore.ts (Recent store pattern)
@src/lib/platform.ts (isNative guard pattern)
@src/App.tsx (Auth initialization pattern)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install RevenueCat SDK and create subscriptionStore</name>
  <files>
    package.json
    package-lock.json
    src/stores/subscriptionStore.ts
    src/stores/index.ts
    src/lib/revenuecat.ts
  </files>
  <action>
1. Install RevenueCat Capacitor SDK (pinned version per STATE.md):
   ```bash
   npm install @revenuecat/purchases-capacitor@11.3.2
   npx cap sync ios
   ```

2. Create `src/lib/revenuecat.ts` with SDK initialization helpers:
   - `initializeRevenueCat(userId: string)`: Configure SDK with iOS API key from env var `VITE_REVENUECAT_IOS_API_KEY`, pass userId as appUserID
   - `loginToRevenueCat(userId: string)`: Call `Purchases.logIn({ appUserID: userId })`
   - `logoutFromRevenueCat()`: Call `Purchases.logOut()`
   - Guard ALL exports with `if (!isNative()) return` at top
   - Set LOG_LEVEL.DEBUG in development mode only

3. Create `src/stores/subscriptionStore.ts` following accessStore pattern:
   - State: `isLoading`, `isPremium`, `customerInfo`, `offerings`
   - Actions: `initialize()`, `checkEntitlements()`, `purchase(pkg)`, `restorePurchases()`
   - Use `persist` middleware, partialize to only persist `isPremium` (not offerings/customerInfo)
   - Store name: `'trained-subscription'`
   - ENTITLEMENT_ID constant: `'premium'` (must match RevenueCat dashboard)
   - `purchase()` returns `{ success: boolean; error?: string }`, handles PURCHASE_CANCELLED silently
   - `restorePurchases()` returns same shape, updates isPremium from restored customerInfo

4. Export from `src/stores/index.ts`:
   - `export { useSubscriptionStore } from './subscriptionStore'`
   - No type exports needed (CustomerInfo comes from RevenueCat package)

Pattern references:
- accessStore.ts for Zustand persist pattern
- dpStore.ts for action return types
- platform.ts for isNative() guard
  </action>
  <verify>
    <automated>npm run build && npm run test -- --run</automated>
    <manual>Check subscriptionStore.ts exports useSubscriptionStore, revenuecat.ts exports init functions</manual>
  </verify>
  <done>subscriptionStore exists with isPremium/purchase/restore actions, revenuecat.ts helper exists, builds without errors</done>
</task>

<task type="auto">
  <name>Task 2: Wire SDK initialization into App.tsx and add Xcode entitlements</name>
  <files>
    src/App.tsx
    src/stores/authStore.ts
    ios/App/App/App.entitlements
  </files>
  <action>
1. Update `src/App.tsx` AppContent component:
   - Import `initializeRevenueCat` from `@/lib/revenuecat`
   - Import `useSubscriptionStore` from stores barrel
   - Add useEffect after existing auth initialization:
     ```typescript
     useEffect(() => {
       const initSubscriptions = async () => {
         if (user && isNative()) {
           await initializeRevenueCat(user.id)
           await useSubscriptionStore.getState().checkEntitlements()
         }
       }
       initSubscriptions()
     }, [user])
     ```
   - Add loading gate before main content render (only for native):
     ```typescript
     const subscriptionLoading = useSubscriptionStore((s) => s.isLoading)
     if (isNative() && subscriptionLoading && user) {
       return <LoadingScreen /> // or simple spinner div
     }
     ```
   - Place loading gate AFTER auth loading gate, BEFORE route rendering

2. Update `src/stores/authStore.ts` signOut action:
   - Import `logoutFromRevenueCat` from `@/lib/revenuecat`
   - Add `await logoutFromRevenueCat()` before existing cleanup calls
   - Add reset of subscription state: `useSubscriptionStore.getState().reset()` (add reset action to subscriptionStore if not present)

3. Update Xcode entitlements `ios/App/App/App.entitlements`:
   - Add In-App Purchase capability:
     ```xml
     <key>com.apple.developer.in-app-payments</key>
     <array>
         <string>merchant.fitness.welltrained.app</string>
     </array>
     ```
   - Preserve existing entitlements (aps-environment, associated-domains)

4. Run `npx cap sync ios` to sync changes

Note: Xcode IAP capability must also be enabled in Xcode UI (Signing & Capabilities), but the entitlements plist change enables it at file level.
  </action>
  <verify>
    <automated>npm run build && grep -q "initializeRevenueCat" src/App.tsx && grep -q "in-app-payments" ios/App/App/App.entitlements</automated>
    <manual>Open Xcode, verify In-App Purchase capability appears in Signing & Capabilities</manual>
  </verify>
  <done>App initializes RevenueCat after auth, loading gate blocks until subscription status known, entitlements file has IAP key</done>
</task>

</tasks>

<verification>
- [ ] `npm run build` passes
- [ ] `npm run test -- --run` passes
- [ ] subscriptionStore.ts exists with isPremium selector
- [ ] revenuecat.ts exports initializeRevenueCat, loginToRevenueCat, logoutFromRevenueCat
- [ ] App.tsx imports and calls initializeRevenueCat after auth
- [ ] App.tsx has subscription loading gate for native
- [ ] authStore signOut calls logoutFromRevenueCat
- [ ] ios/App/App/App.entitlements contains in-app-payments key
</verification>

<success_criteria>
SUB-01: RevenueCat SDK is installed and configured with Supabase user ID binding
SUB-05: isPremium is persisted via Zustand persist middleware, survives app restart
</success_criteria>

<output>
After completion, create `.planning/phases/19-subscriptions/19-01-SUMMARY.md`
</output>
