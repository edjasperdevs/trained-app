---
phase: 19-subscriptions
plan: 02
type: execute
wave: 2
depends_on: [19-01]
files_modified:
  - src/screens/Paywall.tsx
  - src/screens/Terms.tsx
  - src/screens/Settings.tsx
  - src/App.tsx
autonomous: true
requirements: [SUB-02, SUB-04, SUB-06, SUB-07]

must_haves:
  truths:
    - "User sees a paywall screen with monthly and annual subscription options"
    - "User can tap Restore Purchases on paywall and recover previous subscription"
    - "User can view subscription status and manage subscription from Settings"
    - "Paywall displays all Apple-required legal text (auto-renewal, pricing, policy links)"
  artifacts:
    - path: "src/screens/Paywall.tsx"
      provides: "Full-screen paywall with subscription options and legal text"
      min_lines: 80
    - path: "src/screens/Terms.tsx"
      provides: "Terms of Use screen (Apple requirement)"
      min_lines: 40
  key_links:
    - from: "src/screens/Paywall.tsx"
      to: "src/stores/subscriptionStore.ts"
      via: "purchase and restorePurchases calls"
      pattern: "useSubscriptionStore.*purchase|restorePurchases"
    - from: "src/screens/Settings.tsx"
      to: "subscriptionStore.isPremium"
      via: "Subscription status display"
      pattern: "isPremium|Subscription"
---

<objective>
Create Paywall screen with Apple-compliant legal text, Terms of Use screen, restore purchases functionality, and Settings subscription management.

Purpose: Enable users to subscribe, restore purchases, and manage subscription status.
Output: Complete user-facing subscription UI meeting Apple Schedule 2 Section 3.8(b) requirements.
</objective>

<execution_context>
@/Users/ejasper/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ejasper/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-subscriptions/19-RESEARCH.md
@src/screens/Privacy.tsx (Screen layout pattern)
@src/stores/subscriptionStore.ts (From 19-01)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Paywall and Terms screens</name>
  <files>
    src/screens/Paywall.tsx
    src/screens/Terms.tsx
    src/App.tsx
  </files>
  <action>
1. Create `src/screens/Terms.tsx` following Privacy.tsx pattern:
   - Same header layout with back button and title "Terms of Use"
   - Content sections: Use of Service, User Accounts, Subscriptions, Termination, Disclaimers, Limitation of Liability, Contact
   - Subscription section must mention auto-renewal terms (Apple requirement)
   - Link to Privacy Policy within Terms
   - Use same Tailwind classes as Privacy.tsx for consistency

2. Create `src/screens/Paywall.tsx` with Dopamine Noir V2 styling:
   - Full-screen layout: min-h-screen bg-background
   - Header: "Unlock Premium" title with optional X close button
   - Feature highlights section (what premium unlocks):
     - Specialized archetypes with DP modifiers
     - Weekly Protocol Orders
     - Avatar evolution stages 3-5
     - (Use Signal #C8FF00 checkmark icons for each)

   - Subscription options from offerings:
     ```typescript
     const offerings = useSubscriptionStore((s) => s.offerings)
     const monthly = offerings?.current?.monthly
     const annual = offerings?.current?.annual
     ```
   - Two cards: Monthly (priceString/month), Annual (priceString/year with "Best Value" badge)
   - Purchase buttons call `purchase(pkg)`, show loading state, toast error if failed
   - IMPORTANT: Handle PURCHASE_CANCELLED silently (no error toast)

   - Restore Purchases link: "Already subscribed? Restore Purchases"
     ```typescript
     const restorePurchases = useSubscriptionStore((s) => s.restorePurchases)
     ```

   - Apple-required legal text (SUB-06 - ALL of these are REQUIRED):
     ```
     Payment will be charged to your iTunes Account at confirmation of purchase.

     Subscription automatically renews unless auto-renew is turned off at least
     24-hours before the end of the current period.

     Account will be charged for renewal within 24-hours prior to the end of
     the current period.

     Subscriptions may be managed and auto-renewal may be turned off by going
     to your Account Settings after purchase.
     ```

   - Policy links (tappable, navigate to /privacy and /terms):
     - Privacy Policy
     - Terms of Use

   - Redirect to home if already premium:
     ```typescript
     const isPremium = useSubscriptionStore((s) => s.isPremium)
     useEffect(() => {
       if (isPremium) navigate('/')
     }, [isPremium])
     ```

3. Add routes to `src/App.tsx`:
   - Import Paywall and Terms (lazy load)
   - Add `/paywall` route in authenticated section
   - Add `/terms` route in BOTH authenticated and unauthenticated sections (like /privacy)

Styling notes:
- Use bg-card for subscription option cards
- Use bg-primary text-primary-foreground for primary purchase button
- Use text-xs text-muted-foreground for legal text
- Use text-primary for tappable links
  </action>
  <verify>
    <automated>npm run build && grep -q "Payment will be charged" src/screens/Paywall.tsx && grep -q "Terms of Use" src/screens/Terms.tsx</automated>
    <manual>Review Paywall.tsx for all 4 Apple legal text requirements</manual>
  </verify>
  <done>Paywall screen displays subscription options with Apple-required legal text, Terms screen exists, routes added</done>
</task>

<task type="auto">
  <name>Task 2: Add subscription management to Settings</name>
  <files>
    src/screens/Settings.tsx
  </files>
  <action>
1. Add Subscription section to Settings.tsx after Profile section:
   - Import `useSubscriptionStore` from stores barrel
   - Import `isNative` from `@/lib/platform`

   - Conditionally render section (only on native):
     ```typescript
     const isPremium = useSubscriptionStore((s) => s.isPremium)
     const restorePurchases = useSubscriptionStore((s) => s.restorePurchases)
     ```

   - Section layout (following existing Settings card patterns):
     ```
     Subscription
     ├── Status: "Premium" badge (if isPremium) or "Free" text
     ├── [if isPremium] Manage Subscription button
     │   - Opens device settings for subscription management
     │   - Use Linking.openURL('https://apps.apple.com/account/subscriptions')
     ├── [if !isPremium] Upgrade to Premium button
     │   - Navigates to /paywall
     └── Restore Purchases button
         - Calls restorePurchases(), shows toast on success/error
     ```

2. Subscription management URL:
   - iOS App Store subscriptions: `https://apps.apple.com/account/subscriptions`
   - Import `Capacitor` and use `Capacitor.Plugins.Browser` or standard Linking

3. Add loading state for restore action:
   ```typescript
   const [restoring, setRestoring] = useState(false)
   const handleRestore = async () => {
     setRestoring(true)
     const { success, error } = await restorePurchases()
     setRestoring(false)
     if (success) toast.success('Purchases restored')
     else if (error) toast.error(error)
   }
   ```

4. Guard entire Subscription section with `isNative()` check (web has no IAP)

Visual styling:
- Premium badge: bg-primary text-primary-foreground rounded px-2 py-0.5 text-xs
- Free text: text-muted-foreground
- Use existing Settings button patterns for consistency
  </action>
  <verify>
    <automated>npm run build && grep -q "Subscription" src/screens/Settings.tsx && grep -q "Restore Purchases" src/screens/Settings.tsx</automated>
    <manual>Check Settings has subscription section with upgrade/manage/restore options</manual>
  </verify>
  <done>Settings screen shows subscription status, upgrade/manage buttons, restore purchases functionality</done>
</task>

</tasks>

<verification>
- [ ] `npm run build` passes
- [ ] Paywall.tsx exists with subscription options
- [ ] Paywall.tsx contains ALL 4 Apple legal text requirements
- [ ] Paywall.tsx has Privacy Policy and Terms links
- [ ] Terms.tsx exists with Terms of Use content
- [ ] /paywall and /terms routes exist in App.tsx
- [ ] Settings.tsx has Subscription section (native only)
- [ ] Settings has Restore Purchases button
- [ ] Settings shows Premium badge or Upgrade button based on isPremium
</verification>

<success_criteria>
SUB-02: User sees paywall with monthly and annual options
SUB-04: User can tap "Restore Purchases" on both paywall and Settings
SUB-06: Paywall displays ALL Apple-required subscription transparency text
SUB-07: User can view and manage subscription status from Settings
</success_criteria>

<output>
After completion, create `.planning/phases/19-subscriptions/19-02-SUMMARY.md`
</output>
