---
phase: 04-screen-refresh
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/screens/XPClaimModal.tsx
  - src/screens/CheckInModal.tsx
  - src/components/Skeleton.tsx
  - src/components/EmptyState.tsx
autonomous: true

must_haves:
  truths:
    - "CheckInModal slides up from the bottom of the screen as a bottom sheet"
    - "XPClaimModal slides up from the bottom of the screen as a bottom sheet"
    - "Skeleton loading states use new palette colors (bg-surface, bg-surface-elevated) with no old theme colors"
    - "EmptyState uses new palette colors (bg-surface-elevated, text-text-secondary) with no old theme colors"
  artifacts:
    - path: "src/screens/XPClaimModal.tsx"
      provides: "Bottom sheet modal with slide-up animation"
    - path: "src/screens/CheckInModal.tsx"
      provides: "Bottom sheet modal with slide-up animation (already partially done)"
    - path: "src/components/Skeleton.tsx"
      provides: "Skeleton components using new palette colors"
    - path: "src/components/EmptyState.tsx"
      provides: "EmptyState using new palette colors"
  key_links:
    - from: "src/screens/XPClaimModal.tsx"
      to: "motion/react"
      via: "bottom sheet animation (y: '100%' → y: 0)"
      pattern: "initial.*y.*100"
    - from: "src/components/Skeleton.tsx"
      to: "src/index.css @theme"
      via: "Tailwind token classes (bg-surface, bg-surface-elevated)"
      pattern: "bg-surface"
---

<objective>
Convert XPClaimModal to bottom sheet pattern (CheckInModal already partially uses it) and verify all skeleton/empty state colors use the new design palette.

Purpose: Centered modals feel like desktop web. Bottom sheets feel native and premium on mobile. The XPClaimModal currently uses centered layout with scale animation -- it needs to slide up from the bottom like CheckInModal. Additionally, skeleton and empty state colors must use the new token palette (no old bg-bg-primary, etc.).

Output: Both modals render as bottom sheets, skeleton and empty state components confirmed on new palette.
</objective>

<execution_context>
@/Users/ejasper/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ejasper/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/screens/CheckInModal.tsx
@src/screens/XPClaimModal.tsx
@src/components/Skeleton.tsx
@src/components/EmptyState.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert XPClaimModal to bottom sheet and refine CheckInModal</name>
  <files>
    src/screens/XPClaimModal.tsx
    src/screens/CheckInModal.tsx
  </files>
  <action>
  **XPClaimModal -- full bottom sheet conversion:**

  The XPClaimModal currently renders centered with scale animation:
  - Outer: `flex items-center justify-center`
  - Inner: `initial={{ scale: 0.8, opacity: 0 }}` / `animate={{ scale: 1, opacity: 1 }}`

  Convert to bottom sheet pattern matching CheckInModal's approach:

  1. **Outer overlay div:**
     - Change `flex items-center justify-center` to `flex items-end sm:items-center justify-center`
     - This makes it bottom-aligned on mobile, center on desktop

  2. **Inner content div (motion.div wrapping Card/content):**
     - Change animation from scale to slide-up:
       ```
       initial={{ y: '100%' }}
       animate={{ y: 0 }}
       exit={{ y: '100%' }}
       transition={{ type: 'spring', damping: 25, stiffness: 300 }}
       ```
     - Add classes: `w-full max-w-md bg-surface max-h-[90vh] overflow-y-auto rounded-t-lg sm:rounded-lg`
     - Remove `mx-4` (bottom sheet is full-width on mobile)

  3. **Content phases (preview, claiming, complete, levelup):**
     - The `preview` phase currently wraps in `<Card>` -- replace the Card wrapper with a plain `<div className="p-6">` since the bottom sheet container itself provides the surface background
     - The `claiming` phase has `py-12` -- change to `p-6` for consistency
     - The `complete` phase wraps in `<Card>` -- same treatment, replace with `<div className="p-6">`
     - The `levelup` phase has `py-8` -- change to `p-6`

  4. **Confetti positioning:**
     - Confetti div should remain `absolute inset-0 pointer-events-none` within the overlay -- this still works with bottom sheet

  **CheckInModal -- refinement:**

  CheckInModal already partially uses bottom sheet pattern (it has `items-end sm:items-center` and `y: '100%'` animation). Verify and fix:

  1. Confirm the outer div uses `flex items-end sm:items-center justify-center` -- it already does this per the code reading
  2. Confirm inner motion.div has `rounded-t-lg sm:rounded-lg` -- it already has this
  3. The CheckInModal is already correctly implemented as a bottom sheet. Only minor cleanup if needed:
     - Ensure the overlay background is `bg-background/80` (currently correct)
     - Ensure spring params are `damping: 25, stiffness: 300` (currently correct)
  </action>
  <verify>
  Run: `grep -n "items-end" src/screens/XPClaimModal.tsx src/screens/CheckInModal.tsx` -- both should show `items-end sm:items-center`
  Run: `grep -n "y: '100%'" src/screens/XPClaimModal.tsx src/screens/CheckInModal.tsx` -- both should show bottom-sheet animation
  Run: `grep -n "rounded-t-lg" src/screens/XPClaimModal.tsx src/screens/CheckInModal.tsx` -- both should have rounded top corners
  Run: `npx tsc --noEmit` -- no type errors
  </verify>
  <done>
  - XPClaimModal slides up from bottom on mobile, centered on desktop (sm: breakpoint)
  - Both modals use identical animation parameters (spring damping 25, stiffness 300)
  - Both modals have rounded-t-lg on mobile, rounded-lg on desktop
  - Both modals have max-h-[90vh] with overflow-y-auto for scroll safety
  - No Card wrappers inside bottom sheet content (plain divs with p-6)
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify and update skeleton and empty state colors to new palette</name>
  <files>
    src/components/Skeleton.tsx
    src/components/EmptyState.tsx
  </files>
  <action>
  **Audit current Skeleton.tsx colors:**

  Current skeleton uses these color classes:
  - `bg-surface-elevated` on Skeleton pulse element -- CORRECT (new palette)
  - `bg-surface` on card outlines -- CORRECT
  - `border-border` on card borders -- CORRECT
  - `bg-bg-primary` on screen backgrounds -- LEGACY alias, should be `bg-background`

  Changes needed:
  1. Replace all `bg-bg-primary` with `bg-background` in skeleton screen components (HomeSkeleton, WorkoutsSkeleton, MacrosSkeleton, AchievementsSkeleton, AvatarSkeleton, SettingsSkeleton, OnboardingSkeleton)
  2. Verify `bg-surface` and `bg-surface-elevated` are used (not `bg-bg-secondary` or `bg-bg-card`)
  3. Verify `border-border` is used (not any old border colors)

  **Audit current EmptyState.tsx colors:**

  Current EmptyState uses:
  - `bg-surface-elevated` on icon circle -- CORRECT
  - `text-text-secondary` on icon and description -- CORRECT
  - `text-text-primary` on title -- CORRECT

  EmptyState appears to already use new palette tokens. Verify no legacy aliases are present. If all clean, no changes needed.

  **Summary of expected changes:**
  - Skeleton.tsx: ~7 replacements of `bg-bg-primary` → `bg-background`
  - EmptyState.tsx: Likely no changes (already on new palette)
  </action>
  <verify>
  Run: `grep -n "bg-bg-" src/components/Skeleton.tsx` -- should return 0 matches (no legacy aliases)
  Run: `grep -n "bg-bg-" src/components/EmptyState.tsx` -- should return 0 matches
  Run: `grep -n "text-accent-" src/components/Skeleton.tsx src/components/EmptyState.tsx` -- should return 0 matches (no legacy accent aliases)
  Run: `grep -n "bg-surface\|bg-background\|border-border\|text-text-" src/components/Skeleton.tsx | head -10` -- all colors should be new palette tokens
  Run: `npx tsc --noEmit` -- no type errors
  </verify>
  <done>
  - All skeleton screen variants use `bg-background` (not `bg-bg-primary`)
  - All skeleton elements use new palette tokens: bg-surface, bg-surface-elevated, border-border
  - EmptyState uses new palette tokens: bg-surface-elevated, text-text-primary, text-text-secondary
  - Zero legacy color aliases (bg-bg-*, text-accent-*, bg-bg-card) in Skeleton.tsx or EmptyState.tsx
  </done>
</task>

</tasks>

<verification>
1. Both modals slide up from bottom: `grep -n "y: '100%'" src/screens/XPClaimModal.tsx src/screens/CheckInModal.tsx`
2. No legacy colors in skeletons: `grep -c "bg-bg-" src/components/Skeleton.tsx` returns 0
3. No legacy colors in empty state: `grep -c "bg-bg-\|text-accent-" src/components/EmptyState.tsx` returns 0
4. `npx tsc --noEmit` passes
5. `npx vite build` succeeds
</verification>

<success_criteria>
- SCREEN-04 met: CheckInModal and XPClaimModal render as bottom sheets (slide up from bottom, not centered)
- SCREEN-05 met: Skeleton loaders and empty states use colors from the new palette (no old theme colors)
- Both modals have consistent spring animation (damping 25, stiffness 300)
- Build succeeds with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-screen-refresh/04-02-SUMMARY.md`
</output>
