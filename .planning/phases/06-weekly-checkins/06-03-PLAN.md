---
phase: 06-weekly-checkins
plan: 03
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/screens/Coach.tsx
autonomous: true

must_haves:
  truths:
    - "Coach sees a list of pending check-ins sorted by submission date with client name"
    - "Coach can click a pending check-in to see all fields organized by section"
    - "Coach can write a response and submit the review, changing status to 'reviewed'"
    - "Reviewed check-ins disappear from the pending list"
    - "Coach can see a client's check-in history in the client detail modal"
  artifacts:
    - path: "src/screens/Coach.tsx"
      provides: "Check-ins dashboard tab + client detail check-ins tab + review flow"
      contains: "Check-ins"
  key_links:
    - from: "src/screens/Coach.tsx"
      to: "src/hooks/useWeeklyCheckins.ts"
      via: "fetchPendingCheckins, fetchClientCheckins, submitReview"
      pattern: "useWeeklyCheckins"
---

<objective>
Add coach-side check-in review UI: a dashboard-level pending check-ins view and a client detail check-ins tab.

Purpose: This delivers CHECK-03 (coach sees pending check-ins) and CHECK-04 (coach can review and respond). The coach can see all pending check-ins across clients, review them, and add a response.
Output: Updated Coach.tsx with "Check-ins" dashboard view toggle option and review flow.
</objective>

<execution_context>
@/Users/ejasper/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ejasper/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-weekly-checkins/06-RESEARCH.md
@.planning/phases/06-weekly-checkins/06-01-SUMMARY.md
@src/screens/Coach.tsx
@src/hooks/useWeeklyCheckins.ts
@src/lib/database.types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Coach dashboard check-ins view</name>
  <files>src/screens/Coach.tsx</files>
  <action>
Modify Coach.tsx to add check-in review capabilities. This is a single-file change (Coach.tsx is already large with all coach features inline).

**1. Add "Check-ins" to the dashboard view toggle:**

Update DashboardView type: `type DashboardView = 'clients' | 'templates' | 'checkins'`

Add a third segment to the existing segmented control in the header:
```
<button onClick={() => setDashboardView('checkins')} ...>
  <ClipboardCheck size={14} />
  Check-ins
</button>
```

Import ClipboardCheck from lucide-react. Also import useWeeklyCheckins from the hook (direct import).

**2. Add state for check-in review:**

```typescript
const [selectedCheckin, setSelectedCheckin] = useState<WeeklyCheckin | null>(null)
const [reviewResponse, setReviewResponse] = useState('')
const [isReviewing, setIsReviewing] = useState(false)
```

Call `useWeeklyCheckins()` and destructure `fetchPendingCheckins`, `fetchClientCheckins`, `submitReview`, `pendingCheckins`.

**3. Dashboard check-ins view (CHECK-03):**

When dashboardView === 'checkins', render:

- If loading: show "..." loading pattern (matching existing template loading)
- If no pending check-ins: empty state with ClipboardCheck icon + "No pending check-ins" + "All client check-ins have been reviewed"
- If pending check-ins exist: render list of cards, each showing:
  - Client name (from joined profile data) and email
  - "Week of {week_of}" date
  - "Submitted {timeAgo}" using the existing getTimeAgo helper
  - Click to open review view (set selectedCheckin)

Cards sorted by created_at ascending (oldest first -- coach reviews in order).

Header should show count: `{pendingCheckins.length} pending` in the subtitle area.

Auto-fetch pending check-ins when switching to 'checkins' view (useEffect on dashboardView).

**4. Check-in review view:**

When a pending check-in is selected (selectedCheckin is set), replace the list with a review view:

- Back button (ArrowLeft) to return to pending list
- Client name + "Week of {date}" header
- Auto-populated data summary card at top (compact):
  ```
  TRACKED DATA card with grid showing:
  - Weight: {auto_weight_current} lbs ({auto_weight_change > 0 ? '+' : ''}{auto_weight_change} lbs)
  - Weekly Avg: {auto_weight_weekly_avg} lbs
  - Macro Hit Rate: {auto_macro_hit_rate}%
  - Workouts: {auto_workouts_completed}
  - Steps: Not tracked (when null)
  - Cardio: Not tracked (when null)
  ```

- Client-submitted fields grouped by section (same 5 sections as the form):
  - Only show fields that have content (skip null/empty -- research pitfall #5)
  - Section headers: Nutrition, Training, Lifestyle, Health, Open Feedback
  - Each field shows label in text-muted-foreground and value below
  - For scale fields (hunger_level, sleep_quality, stress_level), show as "{value}/5"
  - For sleep_hours, show as "{value} hours"

- Coach response textarea at the bottom:
  - Label: "Coach Response"
  - Textarea with placeholder "Add your response for the client..."
  - "Submit Review" button (full width)

- Submit review flow:
  1. Validate response is not empty (at least a few characters)
  2. Call submitReview(checkinId, reviewResponse)
  3. On success: toast.success('Review submitted'), clear selectedCheckin, refetch pending list
  4. On error: toast.error('Failed to submit review')

**5. Client detail modal -- add "Check-ins" tab (5th tab):**

Update ClientDetailTab type: `type ClientDetailTab = 'overview' | 'progress' | 'activity' | 'programs' | 'checkins'`

Add 'checkins' to the tab navigation bar in the client detail modal.

When activeTab === 'checkins':
- Fetch client's check-in history using fetchClientCheckins(clientId)
- Show list of check-ins with:
  - Week of date
  - Status badge: "Submitted" (warning) or "Reviewed" (success)
  - Click to expand and show the full check-in (same sectioned view as the review, but read-only -- no response textarea if already reviewed, show coach_response if it exists)

This tab provides the per-client check-in history (research recommends both dashboard-level and client-level views).

**Important notes:**
- Use the existing `getTimeAgo` helper already defined in Coach.tsx
- Follow the existing Card/CardContent/Button patterns
- Use text-muted-foreground for labels, text-foreground for values
- Import Textarea from '@/components/ui/textarea' (direct import)
- All new state should use devBypass pattern consistently
  </action>
  <verify>Check TypeScript compiles: `cd /Users/ejasper/code/trained-app && npx tsc --noEmit --pretty 2>&1 | head -30`</verify>
  <done>Coach dashboard has a "Check-ins" view toggle showing pending check-ins. Coach can click to review a check-in, see all fields grouped by section, see auto-populated data, write a response, and submit. Client detail modal has a 5th "Check-ins" tab showing that client's check-in history. Status changes from "submitted" to "reviewed" on review submission.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- Coach.tsx has DashboardView type including 'checkins'
- Coach.tsx has ClientDetailTab type including 'checkins'
- Segmented control in header has 3 options: Clients, Templates, Check-ins
- Pending check-ins list renders with client name and submission date
- Review view shows all 5 field sections + auto data + response textarea
- submitReview changes status to 'reviewed'
- Client detail modal has 5 tabs including Check-ins
</verification>

<success_criteria>
Coach can switch to "Check-ins" view, see pending check-ins sorted by date, click one to review it with all fields visible in sections, write a response, and submit the review. Coach can also see a client's check-in history in the client detail modal's Check-ins tab.
</success_criteria>

<output>
After completion, create `.planning/phases/06-weekly-checkins/06-03-SUMMARY.md`
</output>
