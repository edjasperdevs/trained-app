---
phase: 20-health-tracking
plan: 02
type: execute
wave: 2
depends_on:
  - "20-01"
files_modified:
  - src/screens/HealthPermission.tsx
  - src/components/HealthCard.tsx
  - src/components/ManualHealthEntry.tsx
  - src/screens/Home.tsx
  - src/App.tsx
  - supabase/migrations/013_daily_health.sql
autonomous: true
requirements:
  - HEALTH-02
  - HEALTH-04
  - HEALTH-05

must_haves:
  truths:
    - "User sees soft-ask screen explaining HealthKit benefits before native prompt"
    - "User who denies HealthKit sees manual entry UI without re-prompting"
    - "User can manually enter steps and sleep values"
    - "User earns +10 DP when steps >= 10,000 (once per day)"
    - "User earns +10 DP when sleep >= 7 hours (once per day)"
    - "Health data displays on Home screen"
  artifacts:
    - path: "src/screens/HealthPermission.tsx"
      provides: "Soft-ask screen before HealthKit prompt"
      min_lines: 40
    - path: "src/components/HealthCard.tsx"
      provides: "Steps and sleep display for Home screen"
      min_lines: 30
    - path: "src/components/ManualHealthEntry.tsx"
      provides: "Manual entry form for steps/sleep"
      min_lines: 50
    - path: "supabase/migrations/013_daily_health.sql"
      provides: "daily_health table with RLS"
      contains: "CREATE TABLE daily_health"
  key_links:
    - from: "src/screens/HealthPermission.tsx"
      to: "src/stores/healthStore.ts"
      via: "setPermissionStatus after native prompt"
      pattern: "setPermissionStatus"
    - from: "src/components/HealthCard.tsx"
      to: "src/stores/dpStore.ts"
      via: "awardDP for steps/sleep"
      pattern: "awardDP\\('steps'\\)|awardDP\\('sleep'\\)"
    - from: "src/screens/Home.tsx"
      to: "src/components/HealthCard.tsx"
      via: "component import"
      pattern: "HealthCard"
---

<objective>
Create HealthPermission soft-ask screen, HealthCard display component, ManualHealthEntry form, wire DP awards for health thresholds, and create Supabase daily_health table.

Purpose: Complete user-facing health tracking with DP integration and data persistence.
Output: Working health tracking that awards DP and syncs to Supabase.
</objective>

<execution_context>
@/Users/ejasper/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ejasper/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/20-health-tracking/20-RESEARCH.md
@.planning/phases/20-health-tracking/20-01-SUMMARY.md
@src/stores/dpStore.ts
@src/screens/Home.tsx
@src/components/CheckInModal.tsx (pattern for DP award with todayLog check)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create HealthPermission screen and ManualHealthEntry component</name>
  <files>
    src/screens/HealthPermission.tsx
    src/components/ManualHealthEntry.tsx
    src/App.tsx
  </files>
  <action>
1. Create src/screens/HealthPermission.tsx:
   - Soft-ask screen shown ONCE when permissionStatus === 'unknown' and isIOS() is true
   - Header: "Track Your Health" with heart/activity icon
   - Explanation: "Connect Apple Health to automatically track steps and sleep"
   - Benefits list showing DP rewards:
     - "+10 DP for 10,000+ daily steps"
     - "+10 DP for 7+ hours sleep"
   - "Enable Apple Health" button (primary) - calls requestHealthPermission from health.ts, then setPermissionStatus based on result, navigates to Home
   - "Enter Manually" button (ghost/secondary) - sets permissionStatus to 'denied', navigates to Home
   - Style: Use existing Button component, Card layout, Dopamine Noir V2 tokens

2. Create src/components/ManualHealthEntry.tsx:
   - Modal or sheet component for manual entry
   - Two input fields:
     - Steps: number input, placeholder "10000"
     - Sleep: hours/minutes inputs (convert to total minutes for storage)
   - "Save" button calls healthStore.setManualSteps and setManualSleep
   - Validation: steps >= 0, sleep 0-24 hours
   - After save, check and award DP (with todayLog guard)

3. Update src/App.tsx:
   - Add route for HealthPermission screen: /health-permission
   - In main app flow after onboarding complete:
     - If isIOS() && healthStore.permissionStatus === 'unknown', redirect to /health-permission
     - This ensures soft-ask shows once for iOS users who haven't made a choice
   - Note: Do NOT show during onboarding (per HEALTH-06: "contextually, not during onboarding")
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q "HealthPermission" src/App.tsx</automated>
  </verify>
  <done>HealthPermission screen renders with enable/manual buttons, ManualHealthEntry modal saves to healthStore, App.tsx routes to health permission when needed</done>
</task>

<task type="auto">
  <name>Task 2: Create HealthCard and integrate DP awards on Home</name>
  <files>
    src/components/HealthCard.tsx
    src/screens/Home.tsx
    supabase/migrations/013_daily_health.sql
  </files>
  <action>
1. Create src/components/HealthCard.tsx:
   - Display component for Home screen showing:
     - Steps: getEffectiveSteps() with footsteps icon, progress bar toward 10k
     - Sleep: getEffectiveSleep() formatted as "Xh Ym" with moon icon, progress bar toward 7h (420 min)
   - Each metric shows green checkmark if threshold met
   - "Edit" button opens ManualHealthEntry modal
   - If permissionStatus === 'unknown' and isIOS(), show "Connect Health" prompt
   - DP award logic (on mount or when values change):
     - Check dpStore.getTodayLog() for existing awards
     - If getEffectiveSteps() >= 10000 && todayLog.steps === 0, call awardDP('steps')
     - If getEffectiveSleep() >= 420 && todayLog.sleep === 0, call awardDP('sleep')
     - Show RankUpModal if rankedUp === true (same pattern as Macros.tsx)

2. Update src/screens/Home.tsx:
   - Import HealthCard component
   - Add HealthCard to dashboard layout (after DPDisplay, before other cards)
   - On mount: call healthStore.fetchTodayHealth() to load current data
   - Add useEffect to trigger DP check when health values load

3. Create supabase/migrations/013_daily_health.sql:
   ```sql
   CREATE TABLE daily_health (
     id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
     user_id UUID NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE,
     date DATE NOT NULL,
     steps INTEGER,
     sleep_minutes INTEGER,
     steps_source TEXT NOT NULL DEFAULT 'manual',
     sleep_source TEXT NOT NULL DEFAULT 'manual',
     dp_awarded_steps BOOLEAN NOT NULL DEFAULT false,
     dp_awarded_sleep BOOLEAN NOT NULL DEFAULT false,
     created_at TIMESTAMPTZ DEFAULT now(),
     updated_at TIMESTAMPTZ DEFAULT now(),
     UNIQUE(user_id, date)
   );

   ALTER TABLE daily_health ENABLE ROW LEVEL SECURITY;

   CREATE POLICY "Users can manage own health data"
     ON daily_health FOR ALL
     USING (auth.uid() = user_id);

   CREATE INDEX idx_daily_health_user_date ON daily_health(user_id, date);
   ```

   Note: Migration sync to Supabase is not automated - will be applied manually or via Supabase CLI.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q "HealthCard" src/screens/Home.tsx && test -f supabase/migrations/013_daily_health.sql</automated>
  </verify>
  <done>HealthCard displays steps/sleep with progress, awards DP when thresholds met (once per day), Home shows HealthCard, migration file exists for daily_health table</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- HealthPermission screen exists with enable/manual buttons
- ManualHealthEntry component saves to healthStore
- HealthCard displays on Home screen
- DP awards trigger for steps >= 10k and sleep >= 7h (only once per day)
- Migration file 013_daily_health.sql created
</verification>

<success_criteria>
- iOS users see soft-ask before HealthKit native prompt
- Users who deny HealthKit or use web can enter steps/sleep manually
- Steps >= 10,000 awards +10 DP (once daily)
- Sleep >= 7h (420 min) awards +10 DP (once daily)
- Health data visible on Home screen dashboard
- daily_health Supabase table migration created
</success_criteria>

<output>
After completion, create `.planning/phases/20-health-tracking/20-02-SUMMARY.md`
</output>
