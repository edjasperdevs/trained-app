---
phase: 03-analytics-enhancement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/03-analytics-enhancement/EVENTS.md
  - .planning/phases/03-analytics-enhancement/FUNNELS.md
autonomous: true

must_haves:
  truths:
    - "A documented event naming convention exists with rules for event names, property keys, and property values"
    - "All 22 existing events in analytics.ts are listed in the convention doc and verified to follow the convention"
    - "Three funnel definitions are documented: Signup to First Workout, Signup to Habit Formation, Daily Engagement Loop"
    - "Each funnel step maps to a specific Plausible event name that exists in analytics.ts"
  artifacts:
    - path: ".planning/phases/03-analytics-enhancement/EVENTS.md"
      provides: "Event naming convention and complete event inventory"
      contains: "Title Case with Spaces"
    - path: ".planning/phases/03-analytics-enhancement/FUNNELS.md"
      provides: "Funnel definitions with step-to-event mappings"
      contains: "Signup Completed"
  key_links:
    - from: "FUNNELS.md"
      to: "EVENTS.md"
      via: "Every funnel step references an event name from the event inventory"
      pattern: "Signup Completed|Onboarding Completed|Workout Started|Check-In Completed"
---

<objective>
Document the event naming convention and funnel definitions for Plausible analytics.

Purpose: Establishes the standard all events follow (existing and new) and defines the three user funnels that will be configured in the Plausible dashboard after launch. Satisfies ANLYT-01 (convention) and ANLYT-04 (funnel definitions).

Output: Two markdown docs -- EVENTS.md (convention + full event inventory) and FUNNELS.md (three funnel definitions with step-to-event mappings).
</objective>

<execution_context>
@/Users/ejasper/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ejasper/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/lib/analytics.ts
@.planning/phases/03-analytics-enhancement/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create event naming convention and full event inventory</name>
  <files>.planning/phases/03-analytics-enhancement/EVENTS.md</files>
  <action>
Create `.planning/phases/03-analytics-enhancement/EVENTS.md` with these sections:

1. **Event Naming Convention** — document the three rules:
   - Event names: Title Case with Spaces (e.g., `Workout Started`, `Check-In Completed`, `Meal Logged`)
   - Property keys: snake_case (e.g., `workout_type`, `duration_minutes`, `training_days`)
   - Property values: Lowercase strings or numbers (e.g., `'push'`, `42`, `'manual'`)

2. **Complete Event Inventory** — a table listing all 22 events from `analytics.ts`:
   | # | Method | Event Name | Properties | Wired In | Status |
   For each event, list:
   - The analytics object method name (e.g., `onboardingStarted`)
   - The Plausible event name string (e.g., `Onboarding Started`)
   - Properties it sends (e.g., `training_days: number`)
   - Which file currently calls it (or "Not wired" for the 14 missing)
   - Status: "Wired" or "Missing"

   Already wired (8): workoutStarted, quickWorkoutLogged, workoutCompleted (x2 call sites), onboardingCompleted, checkInCompleted, xpClaimed, levelUp

   Missing (14): onboardingStarted, mealLogged, mealSaved, proteinTargetHit, calorieTargetHit, badgeEarned, avatarEvolved, appOpened, settingsViewed, achievementsViewed, dataExported, signupCompleted, loginCompleted, coachDashboardViewed, clientViewed

3. **Plausible Dashboard Checklist** — list all 22 event names that must be created as Goals in Plausible dashboard (Settings > Goals). Note this is a post-deployment manual step.

Read `src/lib/analytics.ts` to extract the exact event names and properties. Verify every event follows the Title Case with Spaces convention. Document any deviations (there should be none -- research confirmed all 22 follow it).
  </action>
  <verify>The file exists, contains all 22 events, and every event name listed matches the Title Case with Spaces convention.</verify>
  <done>EVENTS.md exists with naming convention, full 22-event inventory table, and dashboard checklist. All events verified to follow the convention.</done>
</task>

<task type="auto">
  <name>Task 2: Create funnel definitions with step-to-event mappings</name>
  <files>.planning/phases/03-analytics-enhancement/FUNNELS.md</files>
  <action>
Create `.planning/phases/03-analytics-enhancement/FUNNELS.md` with three funnel definitions. Each funnel has a name, description, steps table, and notes about Plausible dashboard configuration.

**Funnel 1: Signup to First Workout**
| Step | Plausible Event | Description |
|------|----------------|-------------|
| 1 | Signup Completed | User creates account |
| 2 | Onboarding Completed | User finishes onboarding wizard |
| 3 | Workout Started | User starts their first workout |
| 4 | Workout Completed | User finishes their first workout |

**Funnel 2: Signup to Habit Formation (7-Day Retention)**
| Step | Plausible Event | Description |
|------|----------------|-------------|
| 1 | Signup Completed | User creates account |
| 2 | Onboarding Completed | User finishes onboarding |
| 3 | Check-In Completed | First daily check-in (streak=1) |
| 4 | XP Claimed | First weekly XP claim |
| 5 | Check-In Completed | Check-in at streak >= 7 (7-day retention) |

Note: Plausible funnels treat repeated events as progression. Step 5 fires when the same "Check-In Completed" event fires with streak >= 7. In practice, Plausible funnels use unique events per step (max 8 steps). Document that steps 3 and 5 use the same event name with different property values -- this means Plausible will count any check-in for both steps. Add a note that this funnel is best analyzed by filtering the Check-In Completed event by streak property value in the dashboard rather than using the built-in funnel feature.

**Funnel 3: Daily Engagement Loop**
| Step | Plausible Event | Description |
|------|----------------|-------------|
| 1 | App Opened | User opens the app |
| 2 | Meal Logged | User logs nutrition |
| 3 | Workout Completed | User finishes workout |
| 4 | Check-In Completed | User completes daily report |

Add a **Dashboard Configuration** section noting:
- Funnels are configured in Plausible dashboard (Settings > Funnels), not in code
- Each funnel requires 2-8 steps, each step mapping to a Goal (event)
- All events referenced must first be created as Goals (see EVENTS.md checklist)
- Plausible funnels are strict-ordered (user must trigger events in sequence)
  </action>
  <verify>FUNNELS.md exists with three funnel definitions, each step maps to a valid Plausible event name from EVENTS.md, and dashboard configuration instructions are included.</verify>
  <done>FUNNELS.md exists with three funnel definitions, all steps map to trackable events, and Plausible dashboard setup instructions are documented.</done>
</task>

</tasks>

<verification>
1. `cat .planning/phases/03-analytics-enhancement/EVENTS.md` contains naming convention + 22 events
2. `cat .planning/phases/03-analytics-enhancement/FUNNELS.md` contains 3 funnels with step-to-event mappings
3. Every funnel step references an event name that exists in the EVENTS.md inventory
4. No new code files were created or modified -- this plan is documentation only
</verification>

<success_criteria>
- EVENTS.md documents the naming convention (Title Case with Spaces / snake_case / lowercase values)
- EVENTS.md lists all 22 events with their wiring status
- FUNNELS.md defines 3 funnels with specific Plausible event names per step
- Both docs are self-contained references usable during Plausible dashboard configuration
</success_criteria>

<output>
After completion, create `.planning/phases/03-analytics-enhancement/03-01-SUMMARY.md`
</output>
