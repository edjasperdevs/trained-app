---
phase: 03-analytics-enhancement
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/screens/Auth.tsx
  - src/screens/Onboarding.tsx
  - src/screens/Home.tsx
  - src/screens/Macros.tsx
  - src/screens/Settings.tsx
  - src/screens/Achievements.tsx
  - src/screens/Coach.tsx
  - src/screens/Workouts.tsx
  - src/screens/CheckInModal.tsx
  - src/screens/XPClaimModal.tsx
  - src/App.tsx
autonomous: true

must_haves:
  truths:
    - "Signing up fires Signup Completed event"
    - "Logging in fires Login Completed event"
    - "Advancing past welcome step in onboarding fires Onboarding Started event"
    - "Opening the app fires App Opened event once per session"
    - "Quick-logging macros fires Meal Logged event with source 'manual'"
    - "Logging a saved meal fires Meal Logged event with source 'saved'"
    - "Saving a new meal fires Meal Saved event"
    - "Hitting protein target fires Protein Target Hit event (once per day)"
    - "Hitting calorie target fires Calorie Target Hit event (once per day)"
    - "Earning a new badge fires Badge Earned event with badge name and rarity"
    - "Avatar evolving fires Avatar Evolved event with stage number"
    - "Visiting Settings fires Settings Viewed event"
    - "Visiting Achievements fires Achievements Viewed event"
    - "Exporting data fires Data Exported event"
    - "Visiting Coach dashboard fires Coach Dashboard Viewed event"
    - "Viewing a client fires Client Viewed event"
    - "SPA pageview tracking fires on route changes"
  artifacts:
    - path: "src/screens/Auth.tsx"
      provides: "Signup Completed and Login Completed events"
      contains: "analytics.signupCompleted"
    - path: "src/screens/Onboarding.tsx"
      provides: "Onboarding Started event"
      contains: "analytics.onboardingStarted"
    - path: "src/App.tsx"
      provides: "App Opened event on mount"
      contains: "analytics.appOpened"
    - path: "src/screens/Macros.tsx"
      provides: "Meal Logged, Meal Saved, Protein Target Hit, Calorie Target Hit events"
      contains: "analytics.mealLogged"
    - path: "src/screens/Settings.tsx"
      provides: "Settings Viewed and Data Exported events"
      contains: "analytics.settingsViewed"
    - path: "src/screens/Achievements.tsx"
      provides: "Achievements Viewed event"
      contains: "analytics.achievementsViewed"
    - path: "src/screens/Coach.tsx"
      provides: "Coach Dashboard Viewed and Client Viewed events"
      contains: "analytics.coachDashboardViewed"
    - path: "src/screens/Workouts.tsx"
      provides: "Badge Earned event after badge check"
      contains: "analytics.badgeEarned"
    - path: "src/screens/XPClaimModal.tsx"
      provides: "Avatar Evolved and Badge Earned events"
      contains: "analytics.avatarEvolved"
  key_links:
    - from: "src/screens/Auth.tsx"
      to: "src/lib/analytics.ts"
      via: "import { analytics } from '@/lib/analytics'"
      pattern: "analytics\\.signupCompleted|analytics\\.loginCompleted"
    - from: "src/App.tsx"
      to: "src/lib/analytics.ts"
      via: "import { analytics } from '@/lib/analytics'"
      pattern: "analytics\\.appOpened"
    - from: "src/screens/Macros.tsx"
      to: "src/lib/analytics.ts"
      via: "import { analytics } from '@/lib/analytics'"
      pattern: "analytics\\.mealLogged|analytics\\.mealSaved"
---

<objective>
Wire all 14 missing Plausible analytics events into their corresponding screens and verify SPA pageview tracking.

Purpose: Every user action in the funnel is now tracked, enabling visibility into where users drop off from sign-up through habit formation. Satisfies ANLYT-02 (all events wired) and ANLYT-03 (SPA pageviews).

Output: 11 source files updated with analytics calls; all 22 events fire when their corresponding user actions occur.
</objective>

<execution_context>
@/Users/ejasper/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ejasper/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/lib/analytics.ts
@.planning/phases/03-analytics-enhancement/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire all 14 missing analytics events into screens</name>
  <files>
    src/screens/Auth.tsx
    src/screens/Onboarding.tsx
    src/screens/Macros.tsx
    src/screens/Settings.tsx
    src/screens/Achievements.tsx
    src/screens/Coach.tsx
    src/screens/Workouts.tsx
    src/screens/CheckInModal.tsx
    src/screens/XPClaimModal.tsx
    src/App.tsx
  </files>
  <action>
Add `import { analytics } from '@/lib/analytics'` to each file that doesn't already import it. Then wire each event as follows:

**Auth.tsx** (2 events):
- `analytics.signupCompleted()` — add after the successful signup block (line ~56, after `setSuccess(...)` and before `setMode('login')`)
- `analytics.loginCompleted()` — add after the successful login block. Login success is implicit (no error after `signIn()`), so add it inside the `if (mode === 'login')` block right after the `if (error)` block ends (around line ~79, before the comment "Navigation happens automatically"). Add it as: `if (!error) { analytics.loginCompleted() }` pattern -- but since `error` is already handled by early return/setError, the analytics call should go in an else branch or after the error check completes without returning. Looking at the code: `signIn` returns `{ error, code }`, then if `error` is truthy it sets error messages. So add `else { analytics.loginCompleted() }` after the last `else { setError(error) }` in the login block.

**Onboarding.tsx** (1 event):
- `analytics.onboardingStarted()` — fire when user advances past the 'welcome' step. Find where the step transitions from 'welcome' to the next step (likely in a handleNext or step advancement function). Add the call right when step changes FROM 'welcome'. If there's a `setStep` or similar, add it right before/after that call when current step is 'welcome'.

**App.tsx** (1 event):
- `analytics.appOpened()` — add a `useEffect` that fires once on mount inside `AppContent`. Import `analytics` from `@/lib/analytics`. Add: `useEffect(() => { analytics.appOpened() }, [])` — place it after the existing useEffect blocks (around line ~94). This fires once per page load (session).

**Macros.tsx** (4 events):
- `analytics.mealLogged('manual')` — add inside `DailyView`'s `handleQuickLog` function, after `onLogMacros(...)` is called (around line ~163).
- `analytics.mealLogged('saved')` — add inside `LogMealView`'s `handleLogSavedMeal` function, after `onLogMeal(...)` is called (around line ~740).
- `analytics.mealSaved()` — add inside `LogMealView`'s `handleSaveMeal` function, after `onSaveMeal(...)` is called (around line ~731).
- `analytics.proteinTargetHit()` and `analytics.calorieTargetHit()` — these should fire once per day when the target is first hit. Add these in the `DailyView` component using a `useEffect` that watches `proteinHit` and `caloriesHit` props. Use a `useRef` to track whether each event has already fired this render session to prevent duplicate fires:
  ```
  const proteinTracked = useRef(false)
  const caloriesTracked = useRef(false)

  useEffect(() => {
    if (proteinHit && !proteinTracked.current) {
      analytics.proteinTargetHit()
      proteinTracked.current = true
    }
  }, [proteinHit])

  useEffect(() => {
    if (caloriesHit && !caloriesTracked.current) {
      analytics.calorieTargetHit()
      caloriesTracked.current = true
    }
  }, [caloriesHit])
  ```
  Import `useRef` and `useEffect` (useEffect may already be imported in the parent Macros component but DailyView is a separate function component that needs its own imports — check what's already imported at the top of the file and add to the existing import). Import analytics at the file level.

**Settings.tsx** (2 events):
- `analytics.settingsViewed()` — add a `useEffect(() => { analytics.settingsViewed() }, [])` in the `Settings` component, right after the existing `useEffect` blocks.
- `analytics.dataExported()` — add inside `handleExport`, after the successful `link.click()` line and before `URL.revokeObjectURL(url)` (inside the try block, around line ~185).

**Achievements.tsx** (1 event):
- `analytics.achievementsViewed()` — add a `useEffect(() => { analytics.achievementsViewed() }, [])` in the `Achievements` component.

**Coach.tsx** (2 events):
- `analytics.coachDashboardViewed()` — add a `useEffect(() => { analytics.coachDashboardViewed() }, [])` in the `Coach` component.
- `analytics.clientViewed()` — fire when `selectedClient` changes to a non-null value. Add: `useEffect(() => { if (selectedClient) { analytics.clientViewed() } }, [selectedClient])`.

**Workouts.tsx** (1 event):
- `analytics.badgeEarned(badge.name, badge.rarity)` — add inside the `checkBadgesWithToast` function. After `checkAndAwardBadges()` returns `newBadgeIds`, for each new badge ID, look up the badge and fire the event. The code already does `const badge = allBadges.find(b => b.id === newBadgeIds[0])`. Expand to loop: `newBadgeIds.forEach(id => { const b = allBadges.find(badge => badge.id === id); if (b) analytics.badgeEarned(b.name, b.rarity) })`.

**CheckInModal.tsx** (1 event — badgeEarned):
- `analytics.badgeEarned(badge.name, badge.rarity)` — in the badge check timeout (around line ~142), after `const newBadges = checkAndAwardBadges()`, add: `newBadges.forEach(id => { const b = useAchievementsStore.getState().getAllBadges().find(badge => badge.id === id); if (b) analytics.badgeEarned(b.name, b.rarity) })`. Note: `analytics` is already imported in CheckInModal.tsx.

**XPClaimModal.tsx** (2 events — avatarEvolved, badgeEarned):
- `analytics.avatarEvolved(stage)` — in `handleClaim`, after `updateEvolutionStage(result.newLevel)` call (line ~162). The `updateEvolutionStage` function may return info about whether evolution occurred. Check the return value. If it returns `{ evolved: true, stage: number }` or similar, fire conditionally. If it returns void, call `useAvatarStore.getState().evolutionStage` after the call to get the current stage and fire: `const evoResult = updateEvolutionStage(result.newLevel)` -- actually, read the store's `updateEvolutionStage` implementation first. If unclear, fire the event by reading the current evolution stage from the store: `const currentStage = useAvatarStore.getState().evolutionStage; analytics.avatarEvolved(currentStage)`. Only fire if stage actually changed -- compare before/after: capture `const prevStage = useAvatarStore.getState().evolutionStage` before calling `updateEvolutionStage`, then `const newStage = useAvatarStore.getState().evolutionStage` after, and only fire if `newStage !== prevStage`.
- `analytics.badgeEarned(badge.name, badge.rarity)` — in the claiming phase useEffect (line ~105 area), after `const newBadges = checkAndAwardBadges()`, add: `newBadges.forEach(id => { const b = useAchievementsStore.getState().getAllBadges().find(badge => badge.id === id); if (b) analytics.badgeEarned(b.name, b.rarity) })`.

**IMPORTANT PATTERNS:**
- Import `analytics` from `@/lib/analytics` (NOT from barrel exports — direct import to avoid circular deps, matching existing pattern in Workouts.tsx/CheckInModal.tsx/XPClaimModal.tsx)
- `analytics.*()` already returns early in DEV mode (built into `trackEvent`), so no dev guards needed
- Use `useEffect(() => { ... }, [])` for screen-viewed events (fires once on mount)
- Use direct calls in event handlers for user-action events
  </action>
  <verify>
Run `grep -r "analytics\." src/screens/ src/App.tsx --include="*.tsx" | grep -v "import" | grep -v "//"` and confirm all 22 event methods appear at least once across the codebase. The count should be approximately 22+ call sites (some events like workoutCompleted have 2 call sites).
  </verify>
  <done>All 14 previously-unwired analytics events now fire from their corresponding screens. Every user action in the funnel is tracked.</done>
</task>

<task type="auto">
  <name>Task 2: Verify SPA pageview tracking and add manual fallback if needed</name>
  <files>src/App.tsx</files>
  <action>
Plausible's standard `script.js` (loaded in `index.html`) automatically intercepts `history.pushState` calls, which is what react-router-dom v6 uses internally for navigation. This means SPA pageviews should already work.

**Verification approach:**
1. Read `index.html` and confirm the Plausible script tag uses the standard `script.js` (NOT `script.hash.js` or `script.manual.js`). The standard script includes automatic SPA tracking.
2. Confirm react-router-dom uses `BrowserRouter` (which uses pushState) — check `src/main.tsx` for the router setup.

**If standard script.js + BrowserRouter:** SPA pageviews work automatically. No code changes needed. Document this in the summary as "verified working by configuration analysis."

**If NOT working (e.g., HashRouter or manual script variant):** Add a `useLocation` hook-based fallback in `App.tsx`:
```tsx
import { useLocation } from 'react-router-dom'

// Inside AppContent, after other hooks:
const location = useLocation()
useEffect(() => {
  if (window.plausible) {
    window.plausible('pageview')
  }
}, [location.pathname])
```

The HIGH confidence assessment from research is that the standard script handles this automatically. This task is primarily verification, not implementation.
  </action>
  <verify>
1. Confirm `index.html` contains `src="https://plausible.io/js/script.js"` (standard, not manual variant)
2. Confirm `src/main.tsx` uses `BrowserRouter` from react-router-dom
3. If both are true: SPA pageviews work automatically, no code changes needed
4. The app builds without errors: `npx tsc --noEmit` passes
  </verify>
  <done>SPA pageview tracking is verified working (either by Plausible auto-detection or manual fallback). Route changes fire pageview events.</done>
</task>

</tasks>

<verification>
1. `grep -c "analytics\." src/screens/Auth.tsx` returns >= 2 (signupCompleted, loginCompleted)
2. `grep -c "analytics\." src/screens/Onboarding.tsx` returns >= 2 (onboardingStarted, onboardingCompleted)
3. `grep -c "analytics\." src/screens/Macros.tsx` returns >= 4 (mealLogged x2, mealSaved, proteinTargetHit, calorieTargetHit)
4. `grep -c "analytics\." src/screens/Settings.tsx` returns >= 2 (settingsViewed, dataExported)
5. `grep -c "analytics\." src/screens/Achievements.tsx` returns >= 1 (achievementsViewed)
6. `grep -c "analytics\." src/screens/Coach.tsx` returns >= 2 (coachDashboardViewed, clientViewed)
7. `grep -c "analytics\." src/App.tsx` returns >= 1 (appOpened)
8. `grep -c "analytics\." src/screens/Workouts.tsx` returns >= 5 (existing 4 + badgeEarned)
9. `grep -c "analytics\." src/screens/XPClaimModal.tsx` returns >= 4 (existing 2 + avatarEvolved, badgeEarned)
10. `grep -c "analytics\." src/screens/CheckInModal.tsx` returns >= 2 (existing 1 + badgeEarned)
11. `npx tsc --noEmit` passes with zero type errors
12. SPA pageview tracking verified (standard Plausible script.js + BrowserRouter)
</verification>

<success_criteria>
- All 22 analytics events have at least one call site in the codebase
- No type errors introduced
- Screen-viewed events fire on component mount (useEffect with empty deps)
- User-action events fire in the corresponding event handlers
- SPA pageview tracking confirmed working on route changes
- The app builds cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/03-analytics-enhancement/03-02-SUMMARY.md`
</output>
