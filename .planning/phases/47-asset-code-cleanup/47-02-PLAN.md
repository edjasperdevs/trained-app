---
phase: 47-asset-code-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements:
  - ASSET-02

must_haves:
  truths:
    - "Logo files in public directory are under 100 KB each"
    - "Web page initial load time improves due to smaller asset sizes"
    - "Visual quality of logos is maintained"
  artifacts: []
  key_links: []
---

<objective>
Optimize oversized logo files in the public directory.

Purpose: Reduce web page load times by eliminating 3.1 MB logo files that significantly impact performance.
Output: Optimized logo files (or confirmation that files don't exist).
</objective>

<execution_context>
@/Users/ejasper/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ejasper/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@AUDIT_REPORT.md
</context>

<tasks>

<task type="auto">
  <name>Investigate and optimize logo files</name>
  <files>public/icon-only.png, public/WT Logo.png (if they exist)</files>
  <action>
The audit report mentions `public/icon-only.png` and `public/WT Logo.png` as 3.1 MB each, but the file listing shows these files do not currently exist in the public directory.

Step 1: Verify if these files exist:
```bash
ls -lh public/icon-only.png public/"WT Logo.png" 2>/dev/null || echo "Files not found"
```

Step 2: If files exist:
- Use ImageMagick or sharp to optimize:
  ```bash
  # For lossy PNG compression
  pngquant --quality=80-95 --speed 1 input.png -o output.png
  # OR convert to WebP
  cwebp -q 85 input.png -o output.webp
  ```
- Target: under 100 KB per file
- Maintain visual quality (no noticeable degradation)
- If original files are > 3 MB, investigate why and whether they're actually needed

Step 3: If files don't exist:
- Document that the audit finding is outdated or incorrect
- Check if there are other large image files in public/ that need optimization:
  ```bash
  find public -type f \( -name "*.png" -o -name "*.jpg" -o -name "*.webp" \) -size +500k -exec ls -lh {} \;
  ```
- Verify that current assets are appropriately sized

Step 4: Update .gitignore if needed to prevent future additions of oversized assets.

Note: Based on the actual file listing, the public directory contains reasonably-sized assets (largest is pwa-512x512.png at 229 KB). This task may be a no-op if the audit finding was based on outdated information.
  </action>
  <verify>
    <automated>find public -type f \( -name "*.png" -o -name "*.jpg" \) -size +1M -exec ls -lh {} \; | wc -l | grep -q "^0$" &amp;&amp; echo "PASS: no large image files" || echo "FAIL: large files remain"</automated>
    <manual>Optional: compare visual quality of any optimized files to originals</manual>
    <sampling_rate>run after this task commits</sampling_rate>
  </verify>
  <done>All logo/image files in public directory are under 500 KB, or documented that oversized files don't exist</done>
</task>

</tasks>

<verification>
**Logo optimization:**
- [ ] Verified that icon-only.png and WT Logo.png either don't exist or are optimized
- [ ] No image files in public/ exceed 1 MB
- [ ] Visual quality of any optimized files is maintained
- [ ] Automated verification passes
</verification>

<success_criteria>
All image assets in the public directory are appropriately sized (under 500 KB ideally, definitely under 1 MB). Web performance is not degraded by oversized logo files.
</success_criteria>

<output>
After completion, create `.planning/phases/47-asset-code-cleanup/47-02-SUMMARY.md`
</output>
