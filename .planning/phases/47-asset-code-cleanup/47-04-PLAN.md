---
phase: 47-asset-code-cleanup
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/screens/Onboarding.tsx
  - src/App.tsx
autonomous: true
requirements:
  - INFRA-02

must_haves:
  truths:
    - "Legacy Onboarding.tsx (1,017 lines) is deleted from codebase"
    - "Application builds successfully without legacy onboarding"
    - "No broken imports or references remain"
    - "Bundle size decreases measurably"
  artifacts:
    - path: "src/App.tsx"
      provides: "Application routing without legacy onboarding import"
      contains: "OnboardingStack"
      not_contains: "@/screens/Onboarding"
  key_links:
    - from: "src/App.tsx"
      to: "src/navigation/OnboardingStack.tsx"
      via: "Active onboarding routing"
      pattern: "OnboardingStack"
---

<objective>
Remove the legacy 1,017-line Onboarding.tsx file and its references.

Purpose: Eliminate dead code that adds bundle size and maintenance burden. The v2 onboarding is confirmed stable and fully integrated.
Output: Codebase without legacy onboarding, confirmed building successfully.
</objective>

<execution_context>
@/Users/ejasper/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ejasper/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@AUDIT_REPORT.md
@src/App.tsx
@src/navigation/OnboardingStack.tsx
</context>

<tasks>

<task type="auto">
  <name>Remove legacy onboarding file and references</name>
  <files>src/screens/Onboarding.tsx, src/App.tsx</files>
  <action>
Current state:
- Legacy `src/screens/Onboarding.tsx` exists (1,017 lines)
- Lazy-loaded in `src/App.tsx` line 26: `const Onboarding = lazy(() => import('@/screens/Onboarding').then(m => ({ default: m.Onboarding })))`
- Current onboarding uses `src/navigation/OnboardingStack.tsx` which imports from `src/screens/onboarding-v2/`
- No active routes use the legacy Onboarding component

Step 1: Search for any remaining references to the legacy Onboarding:
```bash
grep -r "from '@/screens/Onboarding'" src/
grep -r "import.*Onboarding" src/ | grep -v "onboarding-v2" | grep -v "OnboardingStack"
```

Step 2: Remove the lazy-load import from App.tsx (line 26):
- Delete the line: `const Onboarding = lazy(() => import('@/screens/Onboarding').then(m => ({ default: m.Onboarding })))`
- Ensure no routes reference the Onboarding component

Step 3: Delete the legacy file:
```bash
rm src/screens/Onboarding.tsx
```

Step 4: Verify build succeeds:
```bash
npm run build
```

Step 5: Verify no broken imports:
```bash
npm run type-check  # or tsc --noEmit
```

Note: The legacy onboarding was only accessible via a dev bypass route and is not part of the active application flow. Its removal is safe.
  </action>
  <verify>
    <automated>! test -f src/screens/Onboarding.tsx &amp;&amp; ! grep -q "@/screens/Onboarding" src/App.tsx &amp;&amp; echo "PASS: legacy onboarding removed" || echo "FAIL: legacy onboarding still exists"</automated>
    <manual>None needed</manual>
    <sampling_rate>run after this task commits</sampling_rate>
  </verify>
  <done>src/screens/Onboarding.tsx deleted, App.tsx import removed, application builds successfully</done>
</task>

<task type="auto">
  <name>Verify build and type safety</name>
  <files>None (verification only)</files>
  <action>
Run build and type-check to ensure no broken imports or build errors after removing legacy onboarding.

Step 1: Type check:
```bash
npm run type-check || tsc --noEmit
```

Step 2: Build:
```bash
npm run build
```

Expected: Both commands succeed with no errors related to missing Onboarding imports.

If errors occur:
- Check for any remaining references to @/screens/Onboarding
- Verify OnboardingStack is correctly handling all onboarding routes
- Fix any import issues

Note: The application should continue to work identically since the legacy onboarding was not in active use.
  </action>
  <verify>
    <automated>npm run build 2>&amp;1 | grep -i "error" | wc -l | grep -q "^0$" &amp;&amp; echo "PASS: build succeeds" || echo "FAIL: build errors"</automated>
    <manual>None needed</manual>
    <sampling_rate>run after this task commits</sampling_rate>
  </verify>
  <done>npm run build and npm run type-check both succeed with zero errors</done>
</task>

</tasks>

<verification>
**Legacy code removal:**
- [ ] src/screens/Onboarding.tsx file deleted
- [ ] App.tsx no longer imports from @/screens/Onboarding
- [ ] Application type-checks successfully (tsc --noEmit)
- [ ] Application builds successfully (npm run build)
- [ ] No broken imports remain in codebase
- [ ] Automated verifications pass
</verification>

<success_criteria>
The legacy Onboarding.tsx file (1,017 lines) is deleted from the codebase, all references are removed, and the application builds successfully without any errors. The v2 onboarding continues to work correctly via OnboardingStack.
</success_criteria>

<output>
After completion, create `.planning/phases/47-asset-code-cleanup/47-04-SUMMARY.md`
</output>
