---
phase: 47-asset-code-cleanup
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
autonomous: true
requirements:
  - ASSET-03

must_haves:
  truths:
    - "Repository contains zero .DS_Store files"
    - "Future git commits cannot add .DS_Store files"
    - "Git history no longer tracks .DS_Store files"
  artifacts:
    - path: ".gitignore"
      provides: "Prevention of .DS_Store file commits"
      contains: ".DS_Store"
  key_links: []
---

<objective>
Remove all .DS_Store files from the repository and prevent future additions.

Purpose: Clean up repository hygiene by removing macOS metadata files that serve no purpose in version control and can leak directory structure information.
Output: Clean repository with .DS_Store files removed and .gitignore updated.
</objective>

<execution_context>
@/Users/ejasper/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ejasper/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@AUDIT_REPORT.md
@.gitignore
</context>

<tasks>

<task type="auto">
  <name>Remove .DS_Store files and prevent future additions</name>
  <files>.gitignore</files>
  <action>
Current state: 9 .DS_Store files exist in the repository:
- node_modules/.DS_Store
- src/assets/.DS_Store
- src/.DS_Store
- docs/.DS_Store
- Design inspo/mockups/onboarding/.DS_Store
- Design inspo/mockups/locked/.DS_Store
- Design inspo/.DS_Store
- Design inspo/mockups/.DS_Store
- .DS_Store (root)

Step 1: Add .DS_Store to .gitignore if not already present:
```bash
# Check if .DS_Store is in .gitignore
grep -q "^\.DS_Store$" .gitignore || echo ".DS_Store" >> .gitignore
```

Step 2: Remove all .DS_Store files from git tracking:
```bash
find . -name '.DS_Store' -type f -print0 | xargs -0 git rm -f --ignore-unmatch
```

Step 3: Verify removal:
```bash
git status | grep -i "DS_Store" || echo "All .DS_Store files removed"
find . -name '.DS_Store' -type f | wc -l  # Should be 0 or only in ignored dirs
```

Note: The .gitignore file currently does not have .DS_Store listed (based on the read earlier). This task will add it and clean up all existing .DS_Store files.

Important: Do NOT remove .DS_Store from node_modules/ manually since node_modules/ is already gitignored. Focus on removing tracked .DS_Store files and preventing future commits.
  </action>
  <verify>
    <automated>git ls-files | grep -i "DS_Store" | wc -l | grep -q "^0$" &amp;&amp; echo "PASS: no .DS_Store files tracked" || echo "FAIL: .DS_Store files still tracked"</automated>
    <manual>None needed</manual>
    <sampling_rate>run after this task commits</sampling_rate>
  </verify>
  <done>All .DS_Store files removed from git tracking and .gitignore updated to prevent future additions</done>
</task>

</tasks>

<verification>
**.DS_Store cleanup:**
- [ ] .gitignore contains .DS_Store entry
- [ ] No .DS_Store files in git tracking (git ls-files check passes)
- [ ] Automated verification passes
</verification>

<success_criteria>
The repository contains no .DS_Store files in git tracking, and .gitignore prevents future additions. Running `git ls-files | grep DS_Store` returns no results.
</success_criteria>

<output>
After completion, create `.planning/phases/47-asset-code-cleanup/47-03-SUMMARY.md`
</output>
