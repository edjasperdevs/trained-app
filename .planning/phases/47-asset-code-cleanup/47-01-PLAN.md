---
phase: 47-asset-code-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - resources/splash.png
autonomous: false
requirements:
  - ASSET-01

must_haves:
  truths:
    - "Native iOS app launch displays WellTrained branding during splash screen"
    - "Splash screen shows logo or W mark instead of generic green glow"
  artifacts:
    - path: "resources/splash.png"
      provides: "Branded splash screen for native app launch"
      min_size: 50000
      max_size: 512000
  key_links:
    - from: "ios/App/App/Assets.xcassets/Splash.imageset"
      to: "resources/splash.png"
      via: "Capacitor asset generation"
      pattern: "splash\\.png"
---

<objective>
Replace the generic dim green glow splash screen with proper WellTrained branding.

Purpose: Provide a professional first impression during app launch and ensure users recognize the app immediately upon opening.
Output: Branded splash.png file with WellTrained logo that maintains minimalist aesthetic and reasonable file size.
</objective>

<execution_context>
@/Users/ejasper/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ejasper/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@AUDIT_REPORT.md
@resources/splash.png
</context>

<tasks>

<task type="auto">
  <name>Create branded splash screen</name>
  <files>resources/splash.png</files>
  <action>
Current splash screen (640x640 PNG, 198KB) shows only a dim green glow on black background with no branding.

Create a new splash.png that:
1. Maintains 640x640 dimensions (Capacitor requirement)
2. Uses dark background (#0A0A0A or similar to match app theme)
3. Includes the WellTrained "W" logo or full logo centered
4. Uses gold/lime accent color (#C8FF00 or #D4AF37) for logo
5. Maintains minimalist aesthetic consistent with Dopamine Noir V2 design
6. Keeps file size under 500 KB

Implementation approach:
- If logo SVG or high-res PNG exists in assets, use that as source
- Use image editing (ImageMagick, sharp, or manual design) to composite logo onto dark background
- Ensure contrast is sufficient for visibility
- Test that generated PNG is valid and displays correctly

The splash.png is used by Capacitor to generate iOS native splash screens during build.
  </action>
  <verify>
    <automated>file resources/splash.png &amp;&amp; du -h resources/splash.png | awk '{if($1 ~ /K/ &amp;&amp; $1+0 &lt; 500) print "PASS: size OK"; else if($1 ~ /M/) print "FAIL: too large"; else print "PASS: size OK"}'</automated>
    <manual>Visually inspect resources/splash.png to confirm WellTrained branding is visible and aesthetic is maintained</manual>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>resources/splash.png contains WellTrained logo on dark background, file size under 500 KB, dimensions are 640x640</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Branded splash screen with WellTrained logo</what-built>
  <how-to-verify>
1. Open `resources/splash.png` in an image viewer or browser
2. Confirm the following:
   - WellTrained logo or "W" mark is clearly visible
   - Background is dark (matching app theme)
   - Logo uses gold/lime accent color
   - Overall aesthetic matches the minimalist Dopamine Noir V2 style
   - Image is 640x640 pixels
   - File size is under 500 KB (check with `ls -lh resources/splash.png`)
3. If native build is available, optionally test in iOS Simulator to see splash on actual app launch
4. Compare to current generic green glow splash to confirm improvement

Expected: Branded splash screen that looks professional and recognizable as WellTrained
  </how-to-verify>
  <resume-signal>Type "approved" if branding looks good, or describe needed adjustments</resume-signal>
</task>

</tasks>

<verification>
**Splash screen branding:**
- [ ] resources/splash.png exists and is a valid PNG
- [ ] Image dimensions are 640x640 pixels
- [ ] File size is under 500 KB
- [ ] WellTrained logo or "W" mark is visible
- [ ] Background is dark and matches app theme
- [ ] User confirms visual quality is acceptable
</verification>

<success_criteria>
The splash screen displays WellTrained branding instead of a generic green glow. When the iOS app launches, users immediately recognize the WellTrained brand during the loading phase.
</success_criteria>

<output>
After completion, create `.planning/phases/47-asset-code-cleanup/47-01-SUMMARY.md`
</output>
