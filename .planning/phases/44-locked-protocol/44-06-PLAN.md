---
phase: 44-locked-protocol
plan: 06
type: execute
wave: 4
depends_on: ["44-04", "44-05"]
files_modified:
  - src/screens/WeeklyReportScreen.tsx
  - src/stores/weeklyReportStore.ts
autonomous: false
requirements:
  - LOCK-12
  - LOCK-13

must_haves:
  truths:
    - "Weekly Report shows LOCKED STREAK card when protocol is active"
    - "Highlights include Locked Protocol day count and DP earned"
    - "Visual checkpoint confirms all Locked Protocol features work end-to-end"
  artifacts:
    - path: "src/screens/WeeklyReportScreen.tsx"
      provides: "Locked streak integration in weekly report"
      contains: "LOCKED STREAK"
  key_links:
    - from: "src/screens/WeeklyReportScreen.tsx"
      to: "src/stores/lockedStore.ts"
      via: "useLockedStore for streak data"
      pattern: "useLockedStore"
---

<objective>
Integrate Locked Protocol into Weekly Report and perform visual verification.

Purpose: Complete the feature integration and verify all components work together.
Output: Weekly Report shows locked streak, visual checkpoint confirms feature completeness.
</objective>

<execution_context>
@/Users/ejasper/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ejasper/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/44-locked-protocol/44-04-SUMMARY.md
@.planning/phases/44-locked-protocol/44-05-SUMMARY.md
@Design inspo/mockups/locked/WellTrained — Feature Brief.md
@src/screens/WeeklyReportScreen.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add LOCKED STREAK card to Weekly Report</name>
  <files>src/screens/WeeklyReportScreen.tsx</files>
  <action>
1. Import useLockedStore:
```typescript
import { useLockedStore } from '@/stores/lockedStore'
```

2. Get locked protocol state in component:
```typescript
const { activeProtocol, currentStreak, totalDPEarned } = useLockedStore()
```

3. Add LOCKED STREAK stat card when protocol is active (after existing stat cards):
```tsx
{activeProtocol && (
  <View style={styles.statCard}>
    <Lock size={20} color="#D4A853" />
    <Text style={styles.statValue}>{currentStreak}</Text>
    <Text style={styles.statLabel}>LOCKED STREAK</Text>
  </View>
)}
```

4. Add locked protocol highlight to auto-generated highlights:

In the highlights generation logic (wherever highlights are computed):
```typescript
// Add locked highlight if protocol active
if (activeProtocol && currentStreak > 0) {
  highlights.push({
    icon: 'Lock',
    title: `Locked Protocol: Day ${currentStreak}`,
    subtitle: `${totalDPEarned} DP earned from compliance`,
  })
}
```

The highlight should appear alongside other highlights (protein compliance, PRs, streak milestones).
  </action>
  <verify>
    <automated>cd /Users/ejasper/code/trained-app && grep -n "LOCKED STREAK" src/screens/WeeklyReportScreen.tsx</automated>
  </verify>
  <done>Weekly Report shows LOCKED STREAK card and generates locked highlight when protocol active</done>
</task>

<task type="auto">
  <name>Task 2: Update highlights store if separate</name>
  <files>src/stores/weeklyReportStore.ts</files>
  <action>
If weekly report highlights are generated in a separate store (weeklyReportStore.ts or similar):

1. Import useLockedStore:
```typescript
import { useLockedStore } from './lockedStore'
```

2. Update highlight generation to include locked protocol:
```typescript
function generateHighlights() {
  const highlights = []

  // ... existing highlight generation ...

  // Add locked protocol highlight
  const { activeProtocol, currentStreak, totalDPEarned } = useLockedStore.getState()
  if (activeProtocol && currentStreak > 0) {
    highlights.push({
      type: 'locked',
      icon: 'Lock',
      title: `Locked Protocol: Day ${currentStreak}`,
      subtitle: `${totalDPEarned} DP earned`,
      priority: 3, // medium priority
    })
  }

  return highlights
}
```

If highlights are generated inline in WeeklyReportScreen.tsx, this task becomes a no-op (mark complete after verifying Task 1 handles it).
  </action>
  <verify>
    <automated>cd /Users/ejasper/code/trained-app && grep -rn "lockedStore" src/stores/weekly* 2>/dev/null || echo "No separate weekly store or already integrated"</automated>
  </verify>
  <done>Highlights generation includes locked protocol data (either in store or inline)</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of complete Locked Protocol feature</name>
  <what-built>
Complete Locked Protocol feature including:
1. Database tables (locked_protocols, locked_logs)
2. lockedStore with all actions
3. LockedProtocolScreen with acceptance flow and daily logging
4. dpStore integration for DP awards
5. Notifications for compliance reminders
6. Share cards (Protocol Initiated, Milestone)
7. Settings integration
8. Weekly Report integration
  </what-built>
  <how-to-verify>
1. **Acceptance Flow:**
   - Navigate to Settings > Locked Protocol
   - Verify acceptance screen shows protocol type selection (Continuous/Day Lock)
   - Verify goal selection pills (7, 14, 21, 30, 60, 90 days)
   - Verify contract text matches feature brief copy
   - Tap "I ACCEPT" - verify share prompt appears

2. **Active Protocol View:**
   - Verify Day counter shows correctly
   - Verify stat cards show +15 DP/day, earned DP, next milestone
   - Verify milestone list shows locked/completed states

3. **Daily Logging:**
   - Tap "LOG COMPLIANCE" (or "LOCK UP" for Day Lock)
   - Verify +15 DP toast appears
   - Verify button changes to "Locked in." and disables
   - Verify streak increments

4. **Milestone Rewards:**
   - If at milestone day (7, 14, etc.), verify bonus DP awarded
   - Verify milestone share prompt appears
   - Verify share card displays correctly

5. **Settings:**
   - Verify "Locked Protocol" row appears under Protocol section
   - Verify notification toggle works
   - For Day Lock: verify evening reminder option appears

6. **Weekly Report:**
   - Verify LOCKED STREAK card appears when protocol active
   - Verify locked highlight appears in highlights section

7. **End Protocol:**
   - Tap "End Protocol"
   - Verify confirmation dialog appears
   - Confirm - verify returns to acceptance flow with reset state
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
- Weekly Report shows LOCKED STREAK when protocol active
- All feature components integrate correctly
- User approval of visual verification
</verification>

<success_criteria>
- LOCKED STREAK card displays in Weekly Report
- Locked highlight appears in auto-generated highlights
- All Locked Protocol features verified working end-to-end
- User approves visual checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/44-locked-protocol/44-06-SUMMARY.md`
</output>
