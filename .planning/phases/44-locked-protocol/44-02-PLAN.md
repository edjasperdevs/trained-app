---
phase: 44-locked-protocol
plan: 02
type: execute
wave: 2
depends_on: ["44-01"]
files_modified:
  - src/screens/LockedProtocolScreen.tsx
  - src/navigation/AppNavigator.tsx
autonomous: true
requirements:
  - LOCK-03
  - LOCK-04
  - LOCK-05

must_haves:
  truths:
    - "User can access Locked Protocol screen via navigation"
    - "User sees acceptance flow with protocol type and goal selection before starting"
    - "User sees Day X counter, streak, daily bonus, and milestone list on active protocol"
    - "User can tap LOG COMPLIANCE button to log daily check-in"
    - "User can end protocol via End Protocol link with confirmation"
  artifacts:
    - path: "src/screens/LockedProtocolScreen.tsx"
      provides: "Main Locked Protocol UI screen"
      min_lines: 200
    - path: "src/navigation/AppNavigator.tsx"
      provides: "Route registration for /locked-protocol"
      contains: "LockedProtocolScreen"
  key_links:
    - from: "src/screens/LockedProtocolScreen.tsx"
      to: "src/stores/lockedStore.ts"
      via: "useLockedStore hook"
      pattern: "useLockedStore"
---

<objective>
Build the LockedProtocolScreen with acceptance flow and daily logging UI.

Purpose: Provide the primary interface for users to start, manage, and log their Locked Protocol.
Output: Fully functional screen with acceptance flow, active protocol display, and compliance logging.
</objective>

<execution_context>
@/Users/ejasper/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ejasper/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/44-locked-protocol/44-01-SUMMARY.md
@Design inspo/mockups/locked/WellTrained — Feature Brief.md
@Design inspo/mockups/locked/screen_protocol_acceptance.png
@Design inspo/mockups/locked/screen_locked_protocol.png
@src/stores/lockedStore.ts
@src/screens/WeeklyReportScreen.tsx (for screen pattern reference)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create LockedProtocolScreen with acceptance flow</name>
  <files>src/screens/LockedProtocolScreen.tsx</files>
  <action>
Create `src/screens/LockedProtocolScreen.tsx` with two main views:

**1. Acceptance Flow (when no active protocol):**

Reference `screen_protocol_acceptance.png` for layout:
- Chain-link crown logo centered at top (reuse from SignUpScreen inline SVG pattern)
- Thin gold divider line
- Large serif headline: "Accept the Protocol."
- Protocol Type selection labeled "HOW DO YOU PRACTICE?":
  - Two side-by-side cards: CONTINUOUS (default) / DAY LOCK
  - CONTINUOUS subtext: "Locked around the clock"
  - DAY LOCK subtext: "Locked during waking hours"
- Goal Duration labeled "YOUR GOAL":
  - Horizontal row of pill buttons: 7, 14, 21, 30 (default), 60, 90 days
- Dark charcoal contract card with text (use feature brief copy exactly):
  - "By activating the Locked Protocol, you place yourself under the authority of WellTrained."
  - "You will log compliance daily."
  - "You will not break without consequence."
  - "WellTrained accepts the role of keyholder."
- Gold padlock icon centered below card (use Lucide Lock icon)
- Full-width gold "I ACCEPT" button
- Muted "I'm not ready" link (navigates back)

**2. Active Protocol View (when protocol is active):**

Reference `screen_locked_protocol.png` for layout:
- Header: Back arrow + "LOCKED PROTOCOL" title + chain crown icon
- Hero card:
  - Geometric padlock icon in gold
  - Large serif "Day {currentStreak}" (or "Day 1" if streak is 0 and logged today)
  - Gold "LOCKED STREAK" label
  - Muted subtext: "Locked. Protocol active."
- Three stat cards in a row:
  - "+15 DP/day" / "DAILY BONUS"
  - "{totalDPEarned} DP" / "EARNED"
  - "Day {nextMilestone}" / "NEXT MILESTONE" (or "COMPLETE" if all milestones reached)
- "MILESTONE REWARDS" section:
  - List showing 7, 14, 21, 30, 60, 90 day milestones
  - Gold checkmarks for completed, lock icons for locked
  - Show DP bonus and badge/title for each
- Gold CTA button:
  - If not logged today: "LOG COMPLIANCE" (or "LOCK UP" for day_lock protocol)
  - If logged today: "Locked in." (disabled state)
- "End Protocol" muted text link at bottom

**Implementation details:**
- Use useState for local view state (acceptance vs active)
- Call lockedStore.fetchProtocol() on mount via useEffect
- Use Dopamine Noir V2 tokens (#0A0A0A bg, #D4A853 gold, #26282B surface)
- Use Oswald font for display text, Inter for body
- Use Haptics.impact for button presses (match existing pattern)

```typescript
import { useState, useEffect } from 'react'
import { View, Text, ScrollView, TouchableOpacity, StyleSheet, Alert } from 'react-native'
import { useNavigation } from '@react-navigation/native'
import { Haptics } from '@capacitor/haptics'
import { Lock, ChevronLeft, Check, Crown } from 'lucide-react-native'
import { useLockedStore, MILESTONES, MILESTONE_DP, ProtocolType } from '@/stores/lockedStore'

const GOAL_OPTIONS = [7, 14, 21, 30, 60, 90]

// Milestone metadata
const MILESTONE_INFO: Record<number, { badge: string; title?: string }> = {
  7: { badge: 'Restrained' },
  14: { badge: 'Controlled' },
  21: { badge: 'Disciplined', title: 'The Disciplined' },
  30: { badge: 'Locked', title: 'Locked by Protocol' },
  60: { badge: 'Locked and Bound', title: 'Locked and Bound' },
  90: { badge: 'Absolute', title: 'Locked. Absolute.' },
}

export function LockedProtocolScreen() {
  // ... implement both views
}
```
  </action>
  <verify>
    <automated>cd /Users/ejasper/code/trained-app && npx tsc --noEmit 2>&1 | grep -E "(error|LockedProtocol)" | head -20</automated>
  </verify>
  <done>LockedProtocolScreen renders acceptance flow and active protocol view with all UI elements from mockups</done>
</task>

<task type="auto">
  <name>Task 2: Register route and wire up navigation</name>
  <files>src/navigation/AppNavigator.tsx</files>
  <action>
1. Import LockedProtocolScreen at top of AppNavigator.tsx:
```typescript
import { LockedProtocolScreen } from '@/screens/LockedProtocolScreen'
```

2. Add route in the Stack.Navigator (inside MainTabNavigator or as a stack screen):
```typescript
<Stack.Screen
  name="LockedProtocol"
  component={LockedProtocolScreen}
  options={{ headerShown: false }}
/>
```

3. Also export the route name type if using typed navigation.

4. Verify the screen is accessible by adding a temporary test navigation (or rely on Settings entry in later plan).
  </action>
  <verify>
    <automated>cd /Users/ejasper/code/trained-app && grep -n "LockedProtocol" src/navigation/*.tsx</automated>
  </verify>
  <done>LockedProtocolScreen registered in AppNavigator, accessible via navigation.navigate('LockedProtocol')</done>
</task>

<task type="auto">
  <name>Task 3: Wire up protocol actions and state</name>
  <files>src/screens/LockedProtocolScreen.tsx</files>
  <action>
Complete the screen by wiring up all interactions:

1. **Acceptance flow actions:**
   - Protocol type selection: update local state, show gold border on selected
   - Goal selection: update local state, show gold background on selected pill
   - "I ACCEPT" button: call `lockedStore.startProtocol(protocolType, goalDays)`, navigate to active view
   - "I'm not ready" link: `navigation.goBack()`

2. **Active protocol actions:**
   - "LOG COMPLIANCE" / "LOCK UP" button:
     - Call `lockedStore.logCompliance()`
     - If milestone reached, show toast: "+{dp} DP - {badge} milestone reached."
     - Trigger haptic feedback
   - "End Protocol" link:
     - Show Alert.alert confirmation: "End your Locked Protocol? Your streak will be reset."
     - If confirmed, call `lockedStore.endProtocol()`, stay on screen (shows acceptance flow)

3. **Loading states:**
   - Show skeleton/loading while `isLoading` is true
   - Disable buttons during async operations

4. **Share prompt (for Plan 05):**
   - After "I ACCEPT", set a flag to show share prompt in UI
   - Text: "Announce your protocol?"
   - This will wire to share card in Plan 05 - for now, just show the prompt text with placeholder buttons
  </action>
  <verify>
    <automated>cd /Users/ejasper/code/trained-app && npx tsc --noEmit 2>&1 | grep -E "error" | head -10</automated>
    <manual>Run app, navigate to Locked Protocol, test acceptance flow and daily logging</manual>
  </verify>
  <done>All protocol actions wired: start protocol, log compliance, end protocol work correctly with haptic feedback</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- LockedProtocolScreen accessible via navigation
- Acceptance flow shows protocol type and goal selection
- Active protocol shows streak, stats, milestones
- LOG COMPLIANCE button works and disables after tap
- End Protocol shows confirmation and resets
</verification>

<success_criteria>
- User can start a new Locked Protocol with type and goal selection
- User can log daily compliance (button disables after first tap)
- User can end protocol with confirmation
- Streak counter increments correctly
- Milestones display with completed/locked states
- No TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/44-locked-protocol/44-02-SUMMARY.md`
</output>
