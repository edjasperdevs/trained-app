---
phase: 33-sign-in-screen
plan: 03
type: execute
wave: 3
depends_on: [33-02]
files_modified:
  - src/screens/auth-screens/SignInScreen.tsx
autonomous: true
requirements: [SIGNIN-01]

must_haves:
  truths:
    - "All visual gaps identified in 33-02-REVIEW.md are resolved"
    - "Sign In screen matches mockup pixel-perfectly"
    - "Spacing, typography, and colors match design reference"
  artifacts:
    - path: "src/screens/auth-screens/SignInScreen.tsx"
      provides: "Pixel-perfect Sign In screen implementation"
      min_lines: 150
  key_links: []
---

<objective>
Apply pixel-perfect refinements to the Sign In screen based on the gap analysis from Plan 02.

Purpose: Ensure the Sign In screen matches the mockup exactly with correct spacing, typography, and styling.

Output: Refined SignInScreen.tsx that passes visual comparison.
</objective>

<execution_context>
@/Users/ejasper/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ejasper/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/33-sign-in-screen/33-02-REVIEW.md
@.planning/phases/33-sign-in-screen/33-02-SUMMARY.md
@Design inspo/mockups/auth flow/auth_signin.png
@src/screens/auth-screens/SignInScreen.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply refinements from review document</name>
  <files>src/screens/auth-screens/SignInScreen.tsx</files>
  <action>
Read the 33-02-REVIEW.md document and apply all identified fixes:

**For each gap in the review document:**
1. Read the "Fix" column for the specific change needed
2. Apply the change to SignInScreen.tsx
3. Verify the fix addresses the gap

**Common refinement areas (apply as documented in review):**

1. **Back Arrow adjustments:**
   - Position (absolute top-X left-X within safe area)
   - Size (w-X h-X)
   - Color value
   - Padding/touch target

2. **Logo and Wordmark spacing:**
   - Logo size adjustments
   - Margin between logo and wordmark
   - Top padding from safe area

3. **Headline typography:**
   - Ensure fontFamily: "'Oswald', sans-serif" via style prop
   - Font size (text-2xl or specific size)
   - Letter spacing (tracking-wide)

4. **Button refinements:**
   - Border radius (rounded-full)
   - Icon positioning (absolute left-5)
   - Button spacing (space-y-4)
   - Button heights (h-14)

5. **Link styling:**
   - Ensure "Create Account" has underline per mockup
   - Ensure "Forgot Password?" has underline per mockup
   - Correct spacing between links

6. **Safe area handling:**
   - Verify pt-safe pb-safe classes
   - Test with iOS simulator if available

**If review indicates "approved" or minimal gaps:**
- Perform verification-only pass
- Confirm TypeScript compiles
- Confirm screen renders correctly
  </action>
  <verify>
    <automated>grep -q "WELCOME BACK" src/screens/auth-screens/SignInScreen.tsx && grep -q "Oswald" src/screens/auth-screens/SignInScreen.tsx</automated>
    <manual>All review gaps addressed in implementation</manual>
  </verify>
  <done>All gaps from 33-02-REVIEW.md are resolved</done>
</task>

<task type="auto">
  <name>Task 2: Final TypeScript and visual verification</name>
  <files>src/screens/auth-screens/SignInScreen.tsx</files>
  <action>
Verify the refined implementation:

1. **Type check:**
   ```bash
   npx tsc --noEmit
   ```
   Ensure no TypeScript errors.

2. **Run development server:**
   ```bash
   npm run dev
   ```

3. **Visual verification checklist:**
   - [ ] Back arrow positioned top-left within safe area
   - [ ] Logo centered and correctly sized
   - [ ] WELLTRAINED wordmark in gold Oswald font
   - [ ] WELCOME BACK headline in warm white Oswald font
   - [ ] Subline in muted gray
   - [ ] Three auth buttons with correct styling
   - [ ] OR divider properly styled
   - [ ] "New to WellTrained? Create Account" with underline
   - [ ] "Forgot Password?" with underline
   - [ ] Background is Obsidian (#0A0A0A)

4. **Navigation verification:**
   - [ ] Back arrow navigates back
   - [ ] Email button navigates to /auth/email-signin
   - [ ] Create Account navigates to /auth/signup
   - [ ] Forgot Password navigates to /auth/forgot-password

5. **Clean up:**
   - Remove any console.log statements
   - Ensure no ESLint warnings
   - Format code consistently
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Screen matches mockup and all navigation works</manual>
  </verify>
  <done>Sign In screen is pixel-perfect and fully functional</done>
</task>

</tasks>

<verification>
1. All gaps from 33-02-REVIEW.md are addressed
2. Sign In screen visually matches auth_signin.png mockup
3. `npx tsc --noEmit` passes with no errors
4. All navigation handlers work correctly
5. Loading and error states function properly
</verification>

<success_criteria>
- [ ] All review gaps resolved
- [ ] Back arrow styled and positioned correctly
- [ ] Typography uses Oswald font for headlines
- [ ] Button styling matches mockup exactly
- [ ] Link underlines match mockup
- [ ] Spacing between elements matches mockup
- [ ] TypeScript compilation passes
- [ ] All navigation works correctly
</success_criteria>

<output>
After completion, create `.planning/phases/33-sign-in-screen/33-03-SUMMARY.md`
</output>
