---
phase: 26-welcome-and-value
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/screens/onboarding-v2/WelcomeScreen.tsx
  - src/screens/onboarding-v2/index.ts
  - src/navigation/OnboardingStack.tsx
autonomous: true
requirements: [WELC-01, WELC-02, WELC-03, WELC-04, WELC-05]

must_haves:
  truths:
    - "User sees Welcome screen with brand mark, wordmark, and headline on first launch"
    - "User can tap BEGIN PROTOCOL to advance to next screen"
    - "User can tap Already initiated? Sign In to navigate to auth screen"
    - "Welcome screen elements animate with staggered fade-up on load"
    - "CTA button pulses after 2 seconds if user hasn't tapped"
  artifacts:
    - path: "src/screens/onboarding-v2/WelcomeScreen.tsx"
      provides: "Welcome screen component with animations"
      min_lines: 80
  key_links:
    - from: "WelcomeScreen.tsx"
      to: "onboardingStore.nextStep"
      via: "BEGIN PROTOCOL button onClick"
      pattern: "nextStep"
    - from: "WelcomeScreen.tsx"
      to: "react-router-dom navigate"
      via: "Sign In link onClick"
      pattern: "navigate.*auth"
---

<objective>
Build the Welcome screen for onboarding with brand identity, animations, and navigation CTAs.

Purpose: Create the first impression screen that hooks users with the WellTrained brand before any data collection
Output: WelcomeScreen.tsx component replacing the placeholder
</objective>

<execution_context>
@/Users/ejasper/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ejasper/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@Design inspo/mockups/onboarding/WellTrained — Onboarding Flow Brief.md
@src/components/AnimatedSplashScreen.tsx (animation patterns with framer-motion)
@src/screens/Auth.tsx (existing splash/brand patterns)
@src/stores/onboardingStore.ts (step navigation)
@src/screens/onboarding-v2/PlaceholderScreens.tsx (screen export pattern)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create WelcomeScreen component with brand elements and animations</name>
  <files>
    src/screens/onboarding-v2/WelcomeScreen.tsx
    src/screens/onboarding-v2/index.ts
  </files>
  <action>
Create WelcomeScreen.tsx in src/screens/onboarding-v2/ with the following implementation:

**Layout (reference onboarding_01_welcome.png mockup):**
- Full height screen with dark background (#0A0A0A)
- Centered content with brand mark (use hero-welcome.png from @/assets)
- WELLTRAINED wordmark in Oswald font, white (#FAFAFA)
- Three-line headline: "YOUR DISCIPLINE." / "YOUR RANK." / "YOUR LEGEND."
- Subline: "The Discipline System. Built for men who train with intention."
- Gold lime rule divider (h-px bg-[#C8FF00])
- "BEGIN PROTOCOL" full-width CTA button (gold bg, dark text)
- "Already initiated? Sign In" secondary link below

**Animations using framer-motion (already in codebase):**
- Logo/brand mark: fade-up from y:20 to y:0, opacity 0 to 1, duration 300ms ease-out
- Wordmark: stagger 150ms after logo
- Headline lines: stagger 150ms between each line (use motion.div with staggerChildren)
- CTA button: animate in after content, then start pulse animation after 2 seconds

**Pulse animation for CTA:**
- Use useState to track if user has interacted
- After 2 second timeout (if not tapped), add subtle scale pulse: scale 1 -> 1.02 -> 1, repeated
- Stop pulse on any tap/interaction

**Navigation:**
- "BEGIN PROTOCOL" calls onboardingStore.nextStep() to advance to step 1 (Value screen)
- "Already initiated? Sign In" uses react-router-dom navigate('/auth') to go to Auth screen

**Pattern to follow:**
Reference AnimatedSplashScreen.tsx for framer-motion animation patterns (motion.div, initial, animate, transition with delay/stagger).

Update src/screens/onboarding-v2/index.ts to export WelcomeScreen (keep placeholder exports for other screens).
  </action>
  <verify>
    <automated>cd /Users/ejasper/code/trained-app && npx tsc --noEmit 2>&1 | head -30</automated>
    <manual>Run app, ensure Welcome screen renders with brand elements</manual>
  </verify>
  <done>WelcomeScreen component exists with brand mark, wordmark, headline, both CTAs, and staggered animations</done>
</task>

<task type="auto">
  <name>Task 2: Wire WelcomeScreen into OnboardingStack and verify navigation</name>
  <files>
    src/navigation/OnboardingStack.tsx
  </files>
  <action>
Update OnboardingStack.tsx to import and use the new WelcomeScreen instead of the placeholder:

1. Change import from:
   `OnboardingWelcome` (placeholder)
   to:
   `WelcomeScreen` from '@/screens/onboarding-v2'

2. Update the Route element:
   `<Route path="welcome" element={<WelcomeScreen />} />`

The placeholder OnboardingWelcome can remain exported from PlaceholderScreens.tsx for now (other phases may reference it), but OnboardingStack should use the real WelcomeScreen.

**Verify navigation flow:**
- BEGIN PROTOCOL should increment currentStep from 0 to 1
- URL should change from /onboarding/welcome to /onboarding/value
- Sign In link should navigate to /auth (exits onboarding flow)
  </action>
  <verify>
    <automated>cd /Users/ejasper/code/trained-app && npx tsc --noEmit 2>&1 | head -30</automated>
    <manual>Test BEGIN PROTOCOL advances to value screen, Sign In goes to auth</manual>
  </verify>
  <done>OnboardingStack uses real WelcomeScreen, navigation to value and auth works correctly</done>
</task>

</tasks>

<verification>
1. TypeScript compiles without errors
2. Welcome screen displays brand mark, wordmark, headline, and both CTAs
3. BEGIN PROTOCOL advances to step 1 (value screen)
4. "Already initiated? Sign In" navigates to /auth
5. Elements animate with staggered fade-up on load
6. CTA pulses after 2 seconds of inactivity
</verification>

<success_criteria>
- User sees Welcome screen with full brand identity on app launch (when not onboarded)
- Tapping BEGIN PROTOCOL advances to Value Proposition screen
- Tapping Sign In link exits to Auth screen
- All elements animate smoothly on load
- CTA pulse draws attention after 2 seconds
</success_criteria>

<output>
After completion, create `.planning/phases/26-welcome-and-value/26-01-SUMMARY.md`
</output>
