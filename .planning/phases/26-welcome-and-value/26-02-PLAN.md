---
phase: 26-welcome-and-value
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/screens/onboarding-v2/ValueScreen.tsx
  - src/screens/onboarding-v2/index.ts
  - src/components/onboarding/ProgressIndicator.tsx
  - src/components/onboarding/index.ts
  - src/navigation/OnboardingStack.tsx
autonomous: true
requirements: [VALU-01, VALU-02, VALU-03, VALU-04, PROG-01, PROG-02, PROG-03]

must_haves:
  truths:
    - "User sees Value Proposition screen with headline and three benefit rows"
    - "Benefit rows explain DP system, rank system, and avatar system"
    - "Benefit rows animate in with stagger after headline appears"
    - "User can tap NEXT to proceed to Profile Setup"
    - "Progress indicator shows 5 dots with current step highlighted in gold"
    - "Completed steps show filled dots in progress indicator"
  artifacts:
    - path: "src/screens/onboarding-v2/ValueScreen.tsx"
      provides: "Value Proposition screen with animated benefit rows"
      min_lines: 100
    - path: "src/components/onboarding/ProgressIndicator.tsx"
      provides: "5-dot progress indicator component"
      min_lines: 30
  key_links:
    - from: "ValueScreen.tsx"
      to: "onboardingStore.nextStep"
      via: "NEXT button onClick"
      pattern: "nextStep"
    - from: "ValueScreen.tsx"
      to: "ProgressIndicator"
      via: "component import"
      pattern: "ProgressIndicator"
---

<objective>
Build the Value Proposition screen with animated benefit rows and reusable progress indicator component.

Purpose: Explain the WellTrained discipline system before asking for any personal data, creating desire and understanding
Output: ValueScreen.tsx component and ProgressIndicator.tsx reusable component
</objective>

<execution_context>
@/Users/ejasper/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ejasper/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@Design inspo/mockups/onboarding/WellTrained — Onboarding Flow Brief.md
@src/components/AnimatedSplashScreen.tsx (animation patterns)
@src/stores/onboardingStore.ts (step navigation, ONBOARDING_SCREENS)
@src/screens/onboarding-v2/PlaceholderScreens.tsx (screen pattern)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ProgressIndicator reusable component</name>
  <files>
    src/components/onboarding/ProgressIndicator.tsx
    src/components/onboarding/index.ts
  </files>
  <action>
Create a new directory src/components/onboarding/ and add ProgressIndicator.tsx:

**Component props:**
```typescript
interface ProgressIndicatorProps {
  totalSteps: number // Total dots to show (5 for onboarding screens 2-6)
  currentStep: number // Current step index (0-based, maps to dots 0-4)
  className?: string
}
```

**Visual requirements (reference brief):**
- Shows 5 horizontal dots with spacing
- Current step dot: gold fill (#C8FF00), slightly larger or with glow
- Completed steps (index < currentStep): gold fill (#C8FF00)
- Future steps (index > currentStep): muted gray (#3F3F46) or border-only
- Dots are circular, approximately 8px diameter with 8px gap

**Implementation:**
- Use array map to render dots
- Current dot gets: `bg-[#C8FF00]` with optional subtle scale or glow
- Completed dots get: `bg-[#C8FF00]` (filled gold)
- Future dots get: `bg-[#3F3F46]` or `border border-[#3F3F46]` (empty/muted)

**Note:** The progress indicator shows on screens 2-6 (Value through Macros), representing progress through the data collection portion. Screen 1 (Welcome), 7 (Paywall), and 8 (Final) don't show it. The component receives totalSteps=5 and currentStep as the offset from the Value screen.

Create src/components/onboarding/index.ts to export ProgressIndicator.
  </action>
  <verify>
    <automated>cd /Users/ejasper/code/trained-app && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>ProgressIndicator component exists with props for step tracking and visual states</done>
</task>

<task type="auto">
  <name>Task 2: Create ValueScreen with animated benefit rows</name>
  <files>
    src/screens/onboarding-v2/ValueScreen.tsx
    src/screens/onboarding-v2/index.ts
  </files>
  <action>
Create ValueScreen.tsx in src/screens/onboarding-v2/ with the following implementation:

**Layout (reference onboarding_02_value.png mockup):**
- Full height screen with dark background (#0A0A0A)
- ProgressIndicator at top showing step 1 of 5 (currentStep=0 for Value screen position)
- Large headline: "IMAGINE A FITNESS APP THAT TRAINS YOU LIKE A CHAMPION" in Oswald font, white
- Three benefit rows below with Lucide icons:
  1. **DP System:** Icon: Zap or Target. "Earn Discipline Points for every workout, meal, and healthy habit."
  2. **Rank System:** Icon: Award or Trophy. "Rise through 15 ranks from Initiate to Master as you prove consistency."
  3. **Avatar System:** Icon: User or Shield. "Watch your avatar evolve as your discipline grows."
- "NEXT" full-width CTA button at bottom (gold bg, dark text)

**Animations using framer-motion:**
- Headline: fade-up from y:20, opacity 0 to 1, duration 400ms
- Benefit rows: stagger in 100ms delay each AFTER headline completes
  - Use `motion.div` with `variants` and `staggerChildren: 0.1`
  - Each row fades up from y:15 to y:0
- ProgressIndicator: fade in with headline (no delay)

**Benefit row structure:**
```tsx
<motion.div className="flex items-start gap-4 bg-[#26282B] rounded-xl p-4">
  <div className="w-10 h-10 rounded-full bg-[#C8FF00]/10 flex items-center justify-center">
    <Icon className="w-5 h-5 text-[#C8FF00]" />
  </div>
  <div>
    <p className="text-[#FAFAFA] font-medium">Earn Discipline Points</p>
    <p className="text-[#A1A1AA] text-sm">for every workout, meal, and healthy habit</p>
  </div>
</motion.div>
```

**Navigation:**
- "NEXT" calls onboardingStore.nextStep() to advance to step 2 (Profile screen)

Update src/screens/onboarding-v2/index.ts to export ValueScreen.
  </action>
  <verify>
    <automated>cd /Users/ejasper/code/trained-app && npx tsc --noEmit 2>&1 | head -30</automated>
    <manual>Run app, navigate to Value screen, verify benefit rows animate</manual>
  </verify>
  <done>ValueScreen component exists with headline, three animated benefit rows, and progress indicator</done>
</task>

<task type="auto">
  <name>Task 3: Wire ValueScreen into OnboardingStack</name>
  <files>
    src/navigation/OnboardingStack.tsx
  </files>
  <action>
Update OnboardingStack.tsx to import and use the new ValueScreen instead of the placeholder:

1. Add import:
   `import { WelcomeScreen, ValueScreen } from '@/screens/onboarding-v2'`
   (If Plan 01 hasn't run yet, just add ValueScreen to existing imports)

2. Update the Route element for value:
   `<Route path="value" element={<ValueScreen />} />`

**Verify navigation flow:**
- From Welcome screen, tapping BEGIN PROTOCOL should show ValueScreen
- Progress indicator should show 5 dots with first dot active
- NEXT should advance to Profile screen (step 2, still placeholder)
  </action>
  <verify>
    <automated>cd /Users/ejasper/code/trained-app && npx tsc --noEmit 2>&1 | head -30</automated>
    <manual>Test full flow: Welcome -> Value (verify animations and progress) -> Profile</manual>
  </verify>
  <done>OnboardingStack uses real ValueScreen, navigation from Welcome to Value to Profile works correctly</done>
</task>

</tasks>

<verification>
1. TypeScript compiles without errors
2. ProgressIndicator shows 5 dots with correct states (current gold, completed gold, future muted)
3. ValueScreen displays headline and three benefit rows with icons
4. Benefit rows animate with stagger after headline appears
5. NEXT button advances to Profile screen
6. Progress indicator on ValueScreen shows step 1 of 5 active
</verification>

<success_criteria>
- User sees Value Proposition screen with "IMAGINE A FITNESS APP..." headline
- Three benefit rows explain DP, ranks, and avatar with icons
- Rows animate in with stagger timing
- Progress indicator shows correct step state
- Tapping NEXT proceeds to Profile Setup screen
</success_criteria>

<output>
After completion, create `.planning/phases/26-welcome-and-value/26-02-SUMMARY.md`
</output>
