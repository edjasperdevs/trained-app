---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - vite.config.ts
  - src/index.css
  - src/lib/cn.ts
  - tailwind.config.js
  - postcss.config.js
autonomous: true

must_haves:
  truths:
    - "App builds and runs on Tailwind v4 with @theme directive"
    - "No tailwind.config.js token definitions remain active"
    - "cn() utility is importable from @/lib/cn"
    - "CVA is importable from class-variance-authority"
    - "All existing Tailwind utility classes (bg-background, text-text-primary, rounded-card, shadow-glow, etc.) still resolve correctly"
    - "Premium design tokens (surface hierarchy, border radius 12-16px, spacing, subtle borders) are defined in @theme"
  artifacts:
    - path: "src/index.css"
      provides: "Single source of truth for all design tokens via @theme block"
      contains: "@import \"tailwindcss\""
    - path: "src/lib/cn.ts"
      provides: "cn() utility combining clsx + tailwind-merge"
      exports: ["cn"]
    - path: "vite.config.ts"
      provides: "@tailwindcss/vite plugin integration"
      contains: "@tailwindcss/vite"
  key_links:
    - from: "src/index.css"
      to: "tailwindcss"
      via: "@import directive"
      pattern: "@import.*tailwindcss"
    - from: "vite.config.ts"
      to: "@tailwindcss/vite"
      via: "Vite plugin array"
      pattern: "tailwindcss.*from.*@tailwindcss/vite"
    - from: "src/lib/cn.ts"
      to: "tailwind-merge"
      via: "twMerge import"
      pattern: "twMerge.*clsx"
---

<objective>
Migrate Tailwind CSS v3 to v4 with @theme directive, define premium design tokens as the single CSS source of truth, create cn() utility, and install CVA + tailwind-merge.

Purpose: Establish the foundation token system that all subsequent phases build on. Every color, font, spacing, and radius token must flow from a single @theme block in index.css with zero runtime cost. This replaces the current 4-layer indirection (trained.ts -> ThemeProvider -> setProperty -> CSS vars -> Tailwind config).

Output: Working Tailwind v4 build with @theme tokens, cn() utility, CVA available for import.
</objective>

<execution_context>
@/Users/ejasper/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ejasper/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md

Key source files to read before executing:
@src/index.css
@tailwind.config.js
@postcss.config.js
@vite.config.ts
@package.json
@index.html
@src/themes/trained.ts
@src/themes/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install packages, configure Vite, migrate to Tailwind v4 @theme</name>
  <files>
    package.json
    package-lock.json
    vite.config.ts
    src/index.css
    tailwind.config.js (DELETE)
    postcss.config.js (DELETE)
  </files>
  <action>
Execute the following steps in order:

**Step 1: Install new packages, remove old ones**
```bash
npm uninstall autoprefixer postcss
npm install tailwindcss@latest
npm install -D @tailwindcss/vite
npm install tailwind-merge clsx class-variance-authority
```

Verify installation succeeded by checking package.json has tailwindcss ^4.x, @tailwindcss/vite, tailwind-merge, clsx, class-variance-authority.

**Step 2: Run the Tailwind v4 automated migration tool**
```bash
npx @tailwindcss/upgrade
```

Review the output carefully. The migration tool will:
- Attempt to convert tailwind.config.js into CSS @theme
- Rename utility classes that changed in v4 (shadow/radius/blur)
- Convert `@tailwind base/components/utilities` to `@import "tailwindcss"`

After the migration tool runs, inspect the changes to make sure they are reasonable. If the tool does not run cleanly, proceed with manual migration below.

**Step 3: Update vite.config.ts**
Add `@tailwindcss/vite` as a Vite plugin. Replace any PostCSS-related config.

```typescript
import tailwindcss from '@tailwindcss/vite'
```

Add `tailwindcss()` to the `plugins` array (before or after `react()`).

Also update the `manualChunks` in build.rollupOptions: change `'vendor-motion': ['framer-motion']` to `'vendor-motion': ['motion']` (this will be needed after Plan 02 installs motion, but set it now so the config is ready; if framer-motion is still installed, keep it as-is and Plan 02 will handle the rename).

Remove any references to postcss from vite.config.ts if present.

Remove the Google Fonts runtime caching rules from the workbox configuration in vite.config.ts (the two `runtimeCaching` entries matching `fonts.googleapis.com` and `fonts.gstatic.com`). These will not be needed after Plan 02 self-hosts fonts.

**Step 4: Rewrite src/index.css with @theme block**
Replace the entire file. The new structure must be:

1. `@import "tailwindcss";` at the top (replaces `@tailwind base/components/utilities`)

2. `@theme { }` block containing ALL design tokens. Use the premium values for the new design system (FOUND-02). The token names MUST match the existing CSS variable names so that all current Tailwind utility classes continue to work without changes:

Premium token values for @theme:
```
Colors (keep existing values -- they are already premium):
  --color-background: #0A0A0A
  --color-surface: #141414
  --color-surface-elevated: #1C1C1C
  --color-border: #2A2A2A
  --color-primary: #D55550
  --color-primary-hover: #E0605A
  --color-primary-muted: rgba(213, 85, 80, 0.15)
  --color-secondary: #4A4A4A
  --color-secondary-hover: #5C5C5C
  --color-text-primary: #E8E8E8
  --color-text-secondary: #888888
  --color-text-accent: #E0605A
  --color-text-on-primary: #FFFFFF
  --color-success: #4CAF50
  --color-warning: #D4A843
  --color-error: #D55550
  --color-info: #3A5A7A
  --color-xp-bar: #D55550
  --color-xp-bar-bg: #1C1C1C
  --color-streak-active: #D55550
  --color-streak-inactive: #2A2A2A
  --color-glass-light: rgba(255, 255, 255, 0.05)
  --color-glass-medium: rgba(255, 255, 255, 0.08)
  --color-glass-heavy: rgba(255, 255, 255, 0.12)

Typography (keep current font names -- Plan 02 will update to Variable variants):
  --font-heading: 'Oswald', sans-serif
  --font-body: 'Inter', sans-serif
  --font-mono: 'JetBrains Mono', monospace

Border Radius (UPDATED for premium -- 12-16px per FOUND-02):
  --radius-sm: 6px
  --radius-md: 10px
  --radius-lg: 12px
  --radius-xl: 16px
  --radius-2xl: 20px
  --radius-card: 12px

Shadows:
  --shadow-card: 0 2px 8px rgba(0, 0, 0, 0.4)
  --shadow-modal: 0 8px 32px rgba(0, 0, 0, 0.6)
  --shadow-glow: 0 0 20px rgba(213, 85, 80, 0.2)
  --shadow-glow-intense: 0 0 30px rgba(213, 85, 80, 0.4)

Animations (keep keyframes):
  --animate-pulse-glow: pulse-glow 2s ease-in-out infinite
  --animate-shimmer: shimmer 2s linear infinite
  --animate-pulse-slow: pulse-slow 2s ease-in-out infinite

  @keyframes pulse-glow { ... }
  @keyframes shimmer { ... }
  @keyframes pulse-slow { ... }
```

IMPORTANT: Do NOT include `float` or `xp-pop` keyframes in the new @theme. These will be removed in Phase 5 (Animation Refinement). But since they may still be referenced in components, add them OUTSIDE the @theme block in a `@layer base` or plain CSS section to avoid breaking things now.

3. After the @theme block, keep all the existing utility CSS rules from index.css:
   - `*` box-sizing reset
   - `html` font-family
   - `body` styles (background, color, min-height, overscroll, touch-action)
   - `#root` min-height
   - Custom scrollbar styles
   - `.font-digital` class
   - `.glass`, `.glass-elevated`, `.glass-subtle` classes
   - `.glow-*` and `.text-glow-*` classes
   - `.safe-bottom`, `.safe-top` classes
   - `.btn-press` class
   - `.progress-gradient` class
   - `.card-hover` class
   - Interactive element transitions
   - `.glass-input` styles
   - `.glass-overlay` styles
   - `.theme-trained` heading styles
   - `.theme-trained .btn-primary` styles

Remove the old `:root { }` block entirely -- @theme replaces it.

CRITICAL: Tailwind v4's @theme generates CSS variables AND utility classes automatically. The variable `--color-background` in @theme generates both `var(--color-background)` and the utility class `bg-background`. Verify this by checking that after migration, `bg-background`, `text-text-primary`, `border-border`, `rounded-card`, `shadow-glow`, `font-heading`, `animate-shimmer` all resolve.

Note on border-radius migration: The old tailwind.config.js used `borderRadius.DEFAULT` and `borderRadius.lg`. In Tailwind v4, `--radius-*` tokens generate `rounded-sm`, `rounded-md`, `rounded-lg`, etc. The old `rounded-card` class was defined as `borderRadius.card` in config -- make sure `--radius-card` is defined in @theme so `rounded-card` still works. If the automated migration tool already handled this, verify it is correct.

**Step 5: Delete old config files**
```bash
rm tailwind.config.js postcss.config.js
```

These are fully replaced by @tailwindcss/vite + @theme.

**Step 6: Verify the build compiles**
```bash
npm run build
```

If there are Tailwind class resolution errors, check:
- CSS variable names in @theme match what components expect
- Any renamed utilities from v3->v4 (the migration tool should handle this)
- The @theme keyframes syntax is correct

Also run dev server briefly to confirm the app loads:
```bash
npm run dev
```
Visit localhost and confirm the app renders with correct colors, fonts, and layout. The app should look identical to before the migration (same token values).

**What to avoid and WHY:**
- Do NOT delete or modify src/themes/ directory (ThemeProvider still needed until Phase 2)
- Do NOT remove the :root CSS variable injection from ThemeProvider (it will harmlessly overwrite @theme values with identical values until Phase 2 removes it)
- Do NOT change any component files in this task -- only infrastructure
- Do NOT use `dark:` variants anywhere -- the app is dark-only, @theme IS the dark palette
  </action>
  <verify>
1. `npm run build` completes with zero errors
2. `npm run dev` loads the app with correct colors and layout
3. `tailwind.config.js` and `postcss.config.js` do not exist
4. `src/index.css` contains `@import "tailwindcss"` and `@theme { }` block
5. `vite.config.ts` imports and uses `@tailwindcss/vite`
6. `package.json` shows tailwindcss ^4.x, @tailwindcss/vite, tailwind-merge, clsx, class-variance-authority
7. Run `npx tsc --noEmit` -- zero TypeScript errors
  </verify>
  <done>
App builds and runs on Tailwind v4 with @theme directive as the single source of truth for design tokens. No tailwind.config.js or postcss.config.js exist. Premium border radius values (12-16px) are defined. All existing utility classes resolve correctly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create cn() utility and verify CVA imports</name>
  <files>
    src/lib/cn.ts
  </files>
  <action>
Create `src/lib/cn.ts` with the industry-standard cn() utility:

```typescript
import { twMerge } from 'tailwind-merge'
import clsx, { type ClassValue } from 'clsx'

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs))
}
```

Then verify CVA is importable by running a quick TypeScript check. Do NOT add cn() to any barrel export yet (the components barrel `src/components/index.ts` doesn't export lib utilities -- cn lives in `src/lib/cn.ts` and is imported directly via `@/lib/cn`).

Verify the import path works:
```bash
npx tsc --noEmit
```

As a proof of concept, open any ONE component (suggest `src/components/Button.tsx` since it is a primitive) and add a comment at the top:
```typescript
// TODO(phase-03): Refactor to use cn() + CVA variants
```
Do NOT refactor Button yet -- Phase 3 (Component Primitives) will do this. The comment is a breadcrumb.

**What to avoid and WHY:**
- Do NOT add cn.ts to the barrel `src/lib/index.ts` yet unless it already has a barrel. Direct import `@/lib/cn` is the pattern used by shadcn/ui and prevents circular deps.
- Do NOT refactor any components to use cn() or CVA in this plan -- that is Phase 3 work. This plan only ensures the utilities are installed and importable.
  </action>
  <verify>
1. `src/lib/cn.ts` exists and exports `cn` function
2. `npx tsc --noEmit` passes (cn.ts compiles, CVA types resolve)
3. `npm run build` still passes
  </verify>
  <done>
cn() utility is importable from `@/lib/cn`. CVA is installed and importable from `class-variance-authority`. Both are available for Phase 3 component work.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `npm run build` passes with zero errors
2. `npx tsc --noEmit` passes with zero errors
3. `npm run dev` shows the app with correct colors, fonts, spacing, layout -- visually identical to pre-migration
4. `tailwind.config.js` does not exist
5. `postcss.config.js` does not exist
6. `src/index.css` uses `@import "tailwindcss"` and `@theme { }` block with all design tokens
7. `vite.config.ts` uses `@tailwindcss/vite` plugin
8. `src/lib/cn.ts` exports cn() function
9. `package.json` contains: tailwindcss ^4.x, @tailwindcss/vite, tailwind-merge, clsx, class-variance-authority
10. ThemeProvider still works (it harmlessly overwrites identical values)
</verification>

<success_criteria>
- FOUND-01: Tailwind v4 with @theme directive -- DONE
- FOUND-02: Premium design tokens defined (surface hierarchy, border radius 12-16px, spacing, subtle borders) -- DONE
- FOUND-06: cn() utility created, CVA + tailwind-merge installed -- DONE
- App builds and runs identically to before migration
- Single source of truth: all tokens in src/index.css @theme block
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
