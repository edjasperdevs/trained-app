---
phase: 02-performance-foundation
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "Routes lazy load (Home, Workouts, Macros load on demand, not in initial bundle)"
    - "Service worker uses prompt mode with visible update banner"
    - "Lighthouse Performance score exceeds 90"
    - "Lighthouse Accessibility score exceeds 90"
    - "Supabase API calls cache with NetworkFirst strategy"
  artifacts: []
  key_links: []
---

<objective>
Verify that all Phase 2 success criteria are met by running Lighthouse audits and confirming existing implementations. This plan modifies no files -- it is a verification-only pass to confirm the phase goal is achieved.

Purpose: Phase 2 success criteria require Lighthouse Performance >90 and Accessibility >90. Plans cannot be marked complete without measurement. This plan runs the audit and identifies any remaining gaps.
Output: Lighthouse scores documented, phase success criteria verified or gap list created.
</objective>

<execution_context>
@/Users/ejasper/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ejasper/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-performance-foundation/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify existing implementations and build output</name>
  <files></files>
  <action>
Verify all Phase 2 success criteria that were already implemented before this phase:

**PERF-01: Route lazy loading**
1. Read `src/App.tsx` and confirm all route screens use `React.lazy()` with dynamic imports
2. Run `npm run build` and confirm route chunks exist as separate files (Home, Workouts, Macros, Settings, Achievements, Onboarding should each be separate chunks)
3. Confirm vendor chunks exist (vendor-react, vendor-motion, vendor-supabase, vendor-sentry)

**PERF-02: Service worker prompt mode**
1. Read `vite.config.ts` and confirm `registerType: 'prompt'`
2. Read `src/components/UpdatePrompt.tsx` and confirm it uses `useRegisterSW` and shows update notification
3. Confirm the built output includes a service worker file

**PERF-04: Supabase caching (from Plan 02-01)**
1. Grep the built service worker in `dist/` for "supabase-api-cache" to confirm the rule was included
2. Confirm no `/auth/v1/` pattern exists in the service worker

**Accessibility fixes (from Plan 02-01)**
1. Confirm built `dist/index.html` viewport meta does not contain "user-scalable" or "maximum-scale"
2. Confirm color token values in `src/themes/trained.ts` meet WCAG AA requirements

Document findings for each criterion.
  </action>
  <verify>
All five success criteria from the roadmap can be confirmed from code inspection and build output.
  </verify>
  <done>
Written verification of all 5 Phase 2 success criteria with evidence from codebase and build output.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Phase 2 Performance Foundation is complete. All implementation was done in Plan 02-01. This checkpoint asks you to verify the app visually and functionally:

1. **Lazy loading works:** Open the deployed app, check Network tab -- route chunks should load on navigation
2. **Update banner works:** The UpdatePrompt component shows when a new version is available
3. **Colors look right:** The updated contrast colors should still feel dark/moody but be more readable
4. **Zoom works:** Pinch-to-zoom should now work on mobile (previously disabled)
5. **Lighthouse scores:** Run Lighthouse audit on the deployed app:
   - Open Chrome DevTools > Lighthouse tab
   - Run audit for Mobile (Performance + Accessibility + PWA)
   - Target: Performance >90, Accessibility >90

If Lighthouse scores are below 90, note the failing audits so gap closure plans can address them.
  </what-built>
  <how-to-verify>
1. Open https://trained-app-eta.vercel.app in Chrome
2. Open DevTools > Network tab, navigate between tabs (Home, Workouts, Macros) -- verify lazy-loaded chunks appear
3. Open DevTools > Lighthouse tab
4. Run audit: Mode=Navigation, Device=Mobile, Categories=Performance+Accessibility+Best Practices+PWA
5. Record scores for Performance and Accessibility
6. Check color contrast visually -- red accents, green success indicators, gold warnings should be readable
7. On mobile or mobile emulator: verify pinch-to-zoom works
  </how-to-verify>
  <resume-signal>
Type "approved" with Lighthouse scores, or describe issues found.
If scores are below 90, list the specific failing audits so gap closure plans can be created.
  </resume-signal>
</task>

</tasks>

<verification>
Phase 2 success criteria from ROADMAP.md:
1. Routes lazy load (Home, Workouts, Macros load on demand) -- verified from code + build
2. Service worker uses prompt mode with visible update banner -- verified from code
3. Lighthouse Performance score exceeds 90 -- measured via audit
4. Lighthouse Accessibility score exceeds 90 -- measured via audit
5. Supabase API calls cache with NetworkFirst strategy -- verified from build output
</verification>

<success_criteria>
All 5 Phase 2 success criteria pass, confirmed by human verification and Lighthouse audit.
If Lighthouse scores are below target, specific failing audits are documented for gap closure.
</success_criteria>

<output>
After completion, create `.planning/phases/02-performance-foundation/02-02-SUMMARY.md`
</output>
