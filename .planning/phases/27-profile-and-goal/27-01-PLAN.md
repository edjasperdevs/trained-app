---
phase: 27-profile-and-goal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/screens/onboarding-v2/ProfileScreen.tsx
  - src/screens/onboarding-v2/index.ts
autonomous: true
requirements: [PROF-01, PROF-02, PROF-03, PROF-04, PROF-05, PROF-06, PROF-07, PROF-08]

must_haves:
  truths:
    - "User can enter name in text field"
    - "User can toggle between LBS and KG units"
    - "User can select training days (2-6) via chips"
    - "User can select fitness level via cards"
    - "Selected chips/cards show gold border with gold tint"
    - "Selections trigger haptic feedback"
    - "CONTINUE button is disabled until name has at least one character"
  artifacts:
    - path: "src/screens/onboarding-v2/ProfileScreen.tsx"
      provides: "Profile setup screen component"
      min_lines: 100
      exports: ["ProfileScreen"]
  key_links:
    - from: "src/screens/onboarding-v2/ProfileScreen.tsx"
      to: "src/stores/onboardingStore.ts"
      via: "updateData for name, units, trainingDays, fitnessLevel"
      pattern: "updateData.*name|units|trainingDays|fitnessLevel"
    - from: "src/screens/onboarding-v2/ProfileScreen.tsx"
      to: "src/lib/haptics.ts"
      via: "haptics.light on selection"
      pattern: "haptics\\.light"
---

<objective>
Build the Profile Setup screen for the onboarding flow.

Purpose: Collect user profile data (name, units, training days, fitness level) with styled selection UI and haptic feedback, following the Obsidian design aesthetic from the mockup.

Output: Fully functional ProfileScreen component replacing the placeholder.
</objective>

<execution_context>
@/Users/ejasper/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ejasper/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@Design inspo/mockups/onboarding/onboarding_03_profile.png
@src/screens/onboarding-v2/ValueScreen.tsx
@src/stores/onboardingStore.ts
@src/components/onboarding/ProgressIndicator.tsx
@src/lib/haptics.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ProfileScreen component</name>
  <files>src/screens/onboarding-v2/ProfileScreen.tsx</files>
  <action>
Create ProfileScreen.tsx following the mockup design and ValueScreen patterns:

**Layout structure:**
- Header: Back button (ChevronLeft) + ProgressIndicator (totalSteps=5, currentStep=1) + spacer
- Section label: "YOUR PROFILE" in gold (#D4A853), uppercase, tracking-[0.2em]
- Headline: "TELL US ABOUT YOURSELF" in Oswald, white, centered

**Form elements:**
1. Name input:
   - Label "Name" above input field
   - Input with placeholder "Your name", dark background (#26282B), gold border on focus
   - Use controlled input bound to local state

2. Units toggle (LBS/KG):
   - Two horizontal buttons side by side
   - Selected button: gold border (#D4A853), gold background tint (rgba(212,168,83,0.08))
   - Unselected: charcoal background (#26282B), no border highlight
   - On select: haptics.light()

3. Training days selector (2-6):
   - Label "Training days per week"
   - Five chips in a row: 2, 3, 4, 5, 6
   - Selected chip: gold border, gold background tint, slightly larger (scale-110)
   - Default selection: 4 (per mockup)
   - On select: haptics.light()

4. Fitness level cards (Beginner/Intermediate/Advanced):
   - Label "Fitness level"
   - Three cards, each with dumbbell icon variant and label
   - Icons: Use weight/dumbbell Lucide icons (e.g., Dumbbell with varying bar weights visually)
   - Selected card: gold border (#D4A853), gold background tint
   - Default selection: Intermediate (per mockup)
   - On select: haptics.light()

**State management:**
- Local state for form values: name, units ('imperial'|'metric'), trainingDays (2-6), fitnessLevel
- On CONTINUE tap: call onboardingStore.updateData({ name, units, trainingDays, fitnessLevel }) then nextStep()

**CTA Button:**
- Full-width gold button: "CONTINUE"
- Disabled state (50% opacity, pointer-events-none) when name is empty
- Oswald font, dark text on gold background

**Animations:**
- Fade-in variants for header and form sections
- Form elements stagger in (100ms delay each)
- Use cubic-bezier ease [0, 0, 0.2, 1] per Phase 26 decisions

**Keyboard handling:**
- Input should not be obscured by keyboard (natural scrolling should handle this)
- Add pb-safe padding at bottom for safe area
  </action>
  <verify>
    <automated>cd /Users/ejasper/code/trained-app && npx tsc --noEmit 2>&1 | head -30</automated>
    <manual>Navigate to /onboarding/profile in browser, verify form renders with all elements</manual>
  </verify>
  <done>ProfileScreen renders with name input, units toggle, training days chips, fitness level cards; CONTINUE button disabled when name empty</done>
</task>

<task type="auto">
  <name>Task 2: Export ProfileScreen and wire to OnboardingStack</name>
  <files>src/screens/onboarding-v2/index.ts</files>
  <action>
Update the onboarding-v2 index to export ProfileScreen:

1. Add named export for ProfileScreen:
   `export { ProfileScreen } from './ProfileScreen'`

2. The OnboardingStack already imports OnboardingProfile from PlaceholderScreens. Update the import in OnboardingStack.tsx to use ProfileScreen instead:
   - Change the import: replace OnboardingProfile with ProfileScreen
   - Update the Route element: `<Route path="profile" element={<ProfileScreen />} />`

This ensures the real ProfileScreen renders when navigating to /onboarding/profile.
  </action>
  <verify>
    <automated>cd /Users/ejasper/code/trained-app && npx tsc --noEmit 2>&1 | head -30</automated>
    <manual>Run dev server, navigate through onboarding to profile screen</manual>
  </verify>
  <done>ProfileScreen exported and rendered at /onboarding/profile route</done>
</task>

</tasks>

<verification>
1. TypeScript compilation passes: `npx tsc --noEmit`
2. ProfileScreen displays all form elements per mockup
3. Name input works with controlled state
4. Units toggle switches between LBS/KG with gold selection styling
5. Training days chips allow single selection (2-6) with haptic feedback
6. Fitness level cards allow single selection with haptic feedback
7. CONTINUE button disabled when name is empty, enabled when name has content
8. Form data persists to onboardingStore on CONTINUE
</verification>

<success_criteria>
- User can fill out profile form with name, units, training days, fitness level
- All selections show gold border with subtle gold tint when selected
- Haptic feedback fires on chip/card selections
- CONTINUE button correctly gates on name length
- Data stored in onboardingStore.data before advancing to next screen
</success_criteria>

<output>
After completion, create `.planning/phases/27-profile-and-goal/27-01-SUMMARY.md`
</output>
