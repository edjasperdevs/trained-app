---
phase: 27-profile-and-goal
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/screens/onboarding-v2/GoalScreen.tsx
  - src/screens/onboarding-v2/index.ts
  - src/navigation/OnboardingStack.tsx
autonomous: true
requirements: [GOAL-01, GOAL-02, GOAL-03, GOAL-04, GOAL-05, GOAL-06, GOAL-07]

must_haves:
  truths:
    - "User sees 4 goal cards: Build Muscle, Lose Fat, Get Stronger, Improve Overall Fitness"
    - "Each goal card has gold Lucide icon and two-line label"
    - "User can select one goal at a time"
    - "Selected goal card shows gold border with animation"
    - "Goal selection triggers haptic feedback"
    - "CONTINUE button disabled until goal selected"
    - "Selected goal stored in onboardingStore"
  artifacts:
    - path: "src/screens/onboarding-v2/GoalScreen.tsx"
      provides: "Goal selection screen component"
      min_lines: 80
      exports: ["GoalScreen"]
  key_links:
    - from: "src/screens/onboarding-v2/GoalScreen.tsx"
      to: "src/stores/onboardingStore.ts"
      via: "updateData for goal selection"
      pattern: "updateData.*goal"
    - from: "src/screens/onboarding-v2/GoalScreen.tsx"
      to: "src/lib/haptics.ts"
      via: "haptics.light on card selection"
      pattern: "haptics\\.light"
---

<objective>
Build the Goal Selection screen for the onboarding flow.

Purpose: Let user select their primary training goal from 4 options with styled card UI, gold selection animation, and haptic feedback. This data drives macro calculation in later screens.

Output: Fully functional GoalScreen component replacing the placeholder.
</objective>

<execution_context>
@/Users/ejasper/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ejasper/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@Design inspo/mockups/onboarding/onboarding_04_goal.png
@src/screens/onboarding-v2/ValueScreen.tsx
@src/stores/onboardingStore.ts
@src/components/onboarding/ProgressIndicator.tsx
@src/lib/haptics.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GoalScreen component</name>
  <files>src/screens/onboarding-v2/GoalScreen.tsx</files>
  <action>
Create GoalScreen.tsx following the mockup design and ValueScreen patterns:

**Layout structure:**
- Header: Back button (ChevronLeft) + ProgressIndicator (totalSteps=5, currentStep=2) + spacer
- Section label: "YOUR MISSION" in gold (#D4A853), uppercase, tracking-[0.2em]
- Headline: "WHAT ARE YOU TRAINING FOR" in Oswald, white, centered

**Goal cards (4 total):**
Define goals array:
```typescript
const GOALS = [
  { id: 'build_muscle', label: 'BUILD MUSCLE', subtitle: 'Add size and strength', icon: TrendingUp },
  { id: 'lose_fat', label: 'LOSE FAT', subtitle: 'Lean out and define', icon: Zap },
  { id: 'get_stronger', label: 'GET STRONGER', subtitle: 'Increase raw power', icon: Dumbbell },
  { id: 'improve_fitness', label: 'IMPROVE OVERALL FITNESS', subtitle: 'Build a complete foundation', icon: Heart },
] as const
```

**Card design (per mockup):**
- Full-width charcoal card (#26282B)
- Left side: Gold Lucide icon (24x24)
- Right side: Two-line text - bold label + muted subtitle
- Horizontal layout: flex items-center gap-4, py-4 px-5
- Border radius: rounded-xl
- Unselected: border-transparent or border-[#3F3F46]
- Selected: border-[#D4A853] (gold border), bg-[rgba(212,168,83,0.08)] (subtle gold tint)
- Border transition: 150ms ease-out per GOAL-04

**Interactions:**
- On card tap:
  1. haptics.light()
  2. Set local selectedGoal state
  3. Gold border animates in (CSS transition handles this)
- Single selection only (radio behavior)

**State management:**
- Local state: selectedGoal (null initially)
- On CONTINUE tap: call onboardingStore.updateData({ goal: selectedGoal }) then nextStep()

**CTA Button:**
- Full-width gold button: "CONTINUE"
- Disabled state (50% opacity, pointer-events-none) when no goal selected
- Oswald font, dark text (#0A0A0A) on gold background

**Animations:**
- Fade-in variants for header/section label
- Headline fade-up (same as ValueScreen)
- Cards stagger in with 100ms delay each (containerVariants + cardVariants)
- Use cubic-bezier ease [0, 0, 0.2, 1] per established patterns
- CTA fades in after cards complete (~0.8s delay)
  </action>
  <verify>
    <automated>cd /Users/ejasper/code/trained-app && npx tsc --noEmit 2>&1 | head -30</automated>
    <manual>Navigate to /onboarding/goal, verify 4 cards render with icons and labels</manual>
  </verify>
  <done>GoalScreen renders 4 goal cards with icons, gold selection border animates on tap, haptic fires</done>
</task>

<task type="auto">
  <name>Task 2: Export GoalScreen and wire to OnboardingStack</name>
  <files>
    src/screens/onboarding-v2/index.ts
    src/navigation/OnboardingStack.tsx
  </files>
  <action>
Update exports and routing:

1. In src/screens/onboarding-v2/index.ts:
   - Add: `export { GoalScreen } from './GoalScreen'`

2. In src/navigation/OnboardingStack.tsx:
   - Update import to include GoalScreen:
     ```typescript
     import {
       WelcomeScreen,
       ValueScreen,
       ProfileScreen,  // from Plan 01
       GoalScreen,     // new
       OnboardingArchetype,
       OnboardingMacros,
       OnboardingPaywall,
       OnboardingFinal,
     } from '@/screens/onboarding-v2'
     ```
   - Update goal route: `<Route path="goal" element={<GoalScreen />} />`

Note: If Plan 01 hasn't run yet, the ProfileScreen import may still use OnboardingProfile. Either way, ensure GoalScreen is correctly imported and routed.
  </action>
  <verify>
    <automated>cd /Users/ejasper/code/trained-app && npx tsc --noEmit 2>&1 | head -30</automated>
    <manual>Run dev server, navigate through onboarding to goal screen</manual>
  </verify>
  <done>GoalScreen exported and rendered at /onboarding/goal route</done>
</task>

</tasks>

<verification>
1. TypeScript compilation passes: `npx tsc --noEmit`
2. GoalScreen displays 4 goal cards per mockup layout
3. Each card shows gold icon + label + subtitle
4. Tapping a card selects it with gold border animation
5. Only one goal can be selected at a time
6. Haptic feedback fires on selection
7. CONTINUE button disabled when no goal selected
8. Selected goal persists to onboardingStore.data.goal on CONTINUE
</verification>

<success_criteria>
- All 4 goal cards visible: Build Muscle, Lose Fat, Get Stronger, Improve Overall Fitness
- Gold icon on each card (Lucide icons)
- Single selection with gold border + tint animation (150ms ease-out)
- Haptic feedback on card tap
- CONTINUE gates on goal selection
- Goal stored in onboardingStore for macro calculation
</success_criteria>

<output>
After completion, create `.planning/phases/27-profile-and-goal/27-02-SUMMARY.md`
</output>
