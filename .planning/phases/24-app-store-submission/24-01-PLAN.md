---
phase: 24-app-store-submission
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/migration.ts
  - src/components/FreshStartModal.tsx
  - src/App.tsx
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
  - DATA-04

must_haves:
  truths:
    - "Existing V1 users see a Fresh Start modal once after V2 update"
    - "Modal explains the transition from XP to DP system"
    - "Modal only appears if legacy xpStore data exists"
    - "New users never see the Fresh Start modal"
    - "Workout, macro, weight, and profile data remains intact"
    - "Legacy gamify-gains-xp localStorage key is removed after acknowledgment"
  artifacts:
    - path: "src/lib/migration.ts"
      provides: "V2 migration detection and cleanup logic"
      exports: ["checkV2Migration", "completeV2Migration"]
    - path: "src/components/FreshStartModal.tsx"
      provides: "One-time Fresh Start modal component"
      min_lines: 30
  key_links:
    - from: "src/App.tsx"
      to: "src/lib/migration.ts"
      via: "checkV2Migration on mount"
      pattern: "checkV2Migration"
    - from: "src/App.tsx"
      to: "src/components/FreshStartModal.tsx"
      via: "conditional render when needsMigration"
      pattern: "FreshStartModal"
---

<objective>
Implement the V2 data migration for existing users upgrading from V1.

Purpose: Existing V1 users have XP/level progress that cannot be converted to the new DP/rank system. Rather than silently reset their progress, we show a one-time "Fresh Start" modal that explains the new system, acknowledges their prior work, and confirms all non-gamification data is preserved.

Output: migration.ts utility, FreshStartModal component, App.tsx integration
</objective>

<execution_context>
@/Users/ejasper/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ejasper/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/24-app-store-submission/24-RESEARCH.md

@src/stores/xpStore.ts (legacy store using 'gamify-gains-xp' localStorage key)
@src/stores/dpStore.ts (new store using 'trained-dp' localStorage key, defaults to rank 1)
@src/App.tsx (app entry point, handles auth and routing)
@src/components/ui/dialog.tsx (existing dialog component for modal)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create migration utility and Fresh Start modal</name>
  <files>
    src/lib/migration.ts
    src/components/FreshStartModal.tsx
  </files>
  <action>
Create src/lib/migration.ts with the following functions:

1. `checkV2Migration()`: Returns `{ needsMigration: boolean; legacyXP: number | null }`
   - Check if 'trained-v2-migration-complete' localStorage key exists (already migrated = return early)
   - Check for 'gamify-gains-xp' localStorage key (legacy xpStore)
   - If no legacy data exists, mark migration complete and return needsMigration: false
   - If legacy data exists, parse it to extract totalXP value and return needsMigration: true

2. `completeV2Migration()`: Void function that:
   - Removes 'gamify-gains-xp' localStorage key
   - Sets 'trained-v2-migration-complete' to 'true'

Create src/components/FreshStartModal.tsx:
- Props: `{ legacyXP: number | null; onDismiss: () => void }`
- Uses Dialog from @/components/ui/dialog (already exists)
- Title: "Welcome to WellTrained V2"
- Body text:
  - If legacyXP provided: "The protocol has evolved. Your {legacyXP} XP has been honored, but all trainees now begin fresh in the new Discipline Points system."
  - If no legacyXP: "The protocol has evolved. All trainees now begin fresh in the new Discipline Points system."
  - Second paragraph: "Your workouts, macros, and profile are fully preserved."
- Single CTA button: "Begin Fresh" that calls onDismiss
- Dialog is always open (controlled externally via conditional render)
- Style using existing Tailwind patterns (Signal lime for button, dark backgrounds)
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <manual>Review migration.ts logic and FreshStartModal styling</manual>
  </verify>
  <done>
    - migration.ts exports checkV2Migration and completeV2Migration
    - FreshStartModal renders with correct text for both legacy XP and null cases
    - TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire migration into App.tsx</name>
  <files>
    src/App.tsx
  </files>
  <action>
Modify src/App.tsx to:

1. Import checkV2Migration and completeV2Migration from '@/lib/migration'
2. Import FreshStartModal from '@/components/FreshStartModal'

3. Add state for migration:
   ```typescript
   const [migrationState, setMigrationState] = useState<{
     needsMigration: boolean
     legacyXP: number | null
   }>({ needsMigration: false, legacyXP: null })
   ```

4. Add useEffect (run once on mount, before auth check):
   ```typescript
   useEffect(() => {
     const result = checkV2Migration()
     setMigrationState(result)
   }, [])
   ```

5. Add handler for modal dismiss:
   ```typescript
   const handleMigrationComplete = () => {
     completeV2Migration()
     setMigrationState({ needsMigration: false, legacyXP: null })
   }
   ```

6. Render FreshStartModal conditionally (render BEFORE other content, outside router):
   ```typescript
   {migrationState.needsMigration && (
     <FreshStartModal
       legacyXP={migrationState.legacyXP}
       onDismiss={handleMigrationComplete}
     />
   )}
   ```

Test the migration by:
1. Setting localStorage.setItem('gamify-gains-xp', JSON.stringify({ state: { totalXP: 1234 } }))
2. Refreshing the app
3. Verifying modal appears with "Your 1234 XP has been honored"
4. Clicking "Begin Fresh"
5. Verifying localStorage no longer has 'gamify-gains-xp' key
6. Verifying 'trained-v2-migration-complete' key exists
7. Refreshing and verifying modal does NOT appear again
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build</automated>
    <manual>Test migration flow manually with localStorage manipulation</manual>
  </verify>
  <done>
    - App.tsx imports and renders FreshStartModal conditionally
    - Migration check runs on mount
    - Modal dismiss triggers legacy data cleanup
    - Modal never appears for new users (no legacy data)
    - Modal never appears after first acknowledgment
  </done>
</task>

</tasks>

<verification>
1. TypeScript compilation passes: `npx tsc --noEmit`
2. Production build succeeds: `npm run build`
3. Manual test: Set legacy localStorage, verify modal appears with XP amount
4. Manual test: Dismiss modal, verify legacy key removed
5. Manual test: Refresh, verify modal does not reappear
6. Manual test: Clear localStorage, load fresh, verify no modal appears
</verification>

<success_criteria>
- V1 users see Fresh Start modal once after V2 update
- Modal shows their legacy XP value when available
- Clicking "Begin Fresh" cleans up legacy data and dismisses modal
- New users never see the modal
- All existing data (workouts, macros, profiles) remains untouched
</success_criteria>

<output>
After completion, create `.planning/phases/24-app-store-submission/24-01-SUMMARY.md`
</output>
