---
phase: 24-app-store-submission
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - ios/App/App/PrivacyInfo.xcprivacy
  - public/.well-known/apple-app-site-association
autonomous: false
requirements:
  - LAUNCH-02
  - LAUNCH-04

must_haves:
  truths:
    - "PrivacyInfo.xcprivacy declares HealthKit data collection for app functionality"
    - "AASA file contains the actual Apple Team ID"
    - "Universal Links for password reset work correctly"
  artifacts:
    - path: "ios/App/App/PrivacyInfo.xcprivacy"
      provides: "Privacy manifest with HealthKit data type declaration"
      contains: "NSPrivacyCollectedDataTypeHealthAndFitness"
    - path: "public/.well-known/apple-app-site-association"
      provides: "Universal Links configuration"
      contains: "appIDs"
  key_links:
    - from: "ios/App/App/PrivacyInfo.xcprivacy"
      to: "Apple App Store review"
      via: "Privacy manifest submission"
      pattern: "NSPrivacyCollectedDataTypes"
    - from: "public/.well-known/apple-app-site-association"
      to: "Universal Links"
      via: "Apple CDN verification"
      pattern: "appIDs.*fitness.welltrained.app"
---

<objective>
Update iOS privacy manifest and Universal Links configuration for App Store compliance.

Purpose: Apple requires apps that access HealthKit to declare this in the privacy manifest. Additionally, Universal Links (for password reset emails) require the AASA file to contain the actual Apple Team ID, not a placeholder.

Output: Updated PrivacyInfo.xcprivacy, updated AASA file (requires user action for Team ID)
</objective>

<execution_context>
@/Users/ejasper/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ejasper/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/24-app-store-submission/24-RESEARCH.md

@ios/App/App/PrivacyInfo.xcprivacy (current privacy manifest - missing HealthKit)
@public/.well-known/apple-app-site-association (current AASA - has XXXXXXXXXX placeholder)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update PrivacyInfo.xcprivacy with HealthKit data collection</name>
  <files>
    ios/App/App/PrivacyInfo.xcprivacy
  </files>
  <action>
Update ios/App/App/PrivacyInfo.xcprivacy to declare HealthKit data collection.

The current file has:
- NSPrivacyTracking: false
- NSPrivacyTrackingDomains: empty
- NSPrivacyCollectedDataTypes: empty (NEEDS UPDATE)
- NSPrivacyAccessedAPITypes: UserDefaults (CA92.1)

Add to NSPrivacyCollectedDataTypes array:
```xml
<dict>
  <key>NSPrivacyCollectedDataType</key>
  <string>NSPrivacyCollectedDataTypeHealthAndFitness</string>
  <key>NSPrivacyCollectedDataTypeLinked</key>
  <false/>
  <key>NSPrivacyCollectedDataTypeTracking</key>
  <false/>
  <key>NSPrivacyCollectedDataTypePurposes</key>
  <array>
    <string>NSPrivacyCollectedDataTypePurposeAppFunctionality</string>
  </array>
</dict>
```

This declares:
- Type: Health and Fitness data
- Linked to identity: No (we don't connect health data to user identity for analytics/tracking)
- Used for tracking: No
- Purpose: App functionality (earning DP for steps/sleep)
  </action>
  <verify>
    <automated>plutil -lint ios/App/App/PrivacyInfo.xcprivacy</automated>
    <manual>Review plist structure is valid XML</manual>
  </verify>
  <done>
    - PrivacyInfo.xcprivacy contains NSPrivacyCollectedDataTypeHealthAndFitness
    - Data type marked as not linked to identity
    - Data type marked as not used for tracking
    - Purpose is AppFunctionality
    - Plist validates successfully
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Update AASA with Apple Team ID</name>
  <action>
The AASA file at public/.well-known/apple-app-site-association currently contains a placeholder Team ID:
```json
"appIDs": ["XXXXXXXXXX.fitness.welltrained.app"]
```

User must provide the actual Apple Team ID from their Apple Developer account.

**How to get Team ID:**
1. Go to https://developer.apple.com/account
2. Sign in with your Apple Developer account
3. In the sidebar, click "Membership"
4. Your Team ID is displayed under "Team ID" (10 alphanumeric characters)

**After obtaining Team ID:**
Update public/.well-known/apple-app-site-association:
```json
{
  "applinks": {
    "details": [
      {
        "appIDs": ["YOUR_TEAM_ID.fitness.welltrained.app"],
        "components": [
          {
            "/": "/reset-password*",
            "comment": "Password reset deep links"
          }
        ]
      }
    ]
  }
}
```

Replace YOUR_TEAM_ID with the actual Team ID (e.g., "ABC123XYZ9.fitness.welltrained.app").
  </action>
  <instructions>
1. Navigate to https://developer.apple.com/account and sign in
2. Click "Membership" in the sidebar
3. Copy your Team ID
4. Provide the Team ID to Claude or manually update public/.well-known/apple-app-site-association
  </instructions>
  <resume-signal>Provide Team ID or type "updated" if you edited the file manually</resume-signal>
</task>

</tasks>

<verification>
1. PrivacyInfo.xcprivacy validates: `plutil -lint ios/App/App/PrivacyInfo.xcprivacy`
2. AASA file is valid JSON: `python3 -m json.tool public/.well-known/apple-app-site-association`
3. AASA file no longer contains XXXXXXXXXX placeholder
</verification>

<success_criteria>
- PrivacyInfo.xcprivacy declares HealthKit data collection with correct purposes
- AASA file contains actual Apple Team ID
- Both files are syntactically valid (plist/JSON)
</success_criteria>

<output>
After completion, create `.planning/phases/24-app-store-submission/24-02-SUMMARY.md`
</output>
