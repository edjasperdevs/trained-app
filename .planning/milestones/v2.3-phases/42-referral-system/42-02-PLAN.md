---
phase: 42-referral-system
plan: 02
type: execute
wave: 2
depends_on: ["42-01"]
files_modified:
  - src/screens/RecruitScreen.tsx
  - src/screens/Settings.tsx
  - src/App.tsx
autonomous: true
requirements: [REFR-04, REFR-05, REFR-06, REFR-08]

must_haves:
  truths:
    - "User sees Recruit a Sub screen with their referral link displayed"
    - "User can copy referral link to clipboard via copy button"
    - "User can share referral link via Instagram, X, or Messages buttons"
    - "User sees recruits list showing callsign, rank, status, and DP earned"
    - "Settings has Recruit a Sub entry that navigates to referral screen"
  artifacts:
    - path: "src/screens/RecruitScreen.tsx"
      provides: "Full referral screen with link display, copy, share, and recruits list"
      min_lines: 150
    - path: "src/screens/Settings.tsx"
      provides: "Recruit a Sub navigation entry"
      contains: "Recruit a Sub"
  key_links:
    - from: "src/screens/RecruitScreen.tsx"
      to: "referralStore"
      via: "useReferralStore hook"
      pattern: "useReferralStore"
    - from: "src/screens/Settings.tsx"
      to: "/recruit"
      via: "navigate"
      pattern: "navigate.*recruit"
---

<objective>
Build the Recruit a Sub screen with referral link display, copy/share functionality, and recruits list. Add Settings navigation entry.

Purpose: Give users a complete interface to view and share their referral link and track recruited users.
Output: RecruitScreen component with full functionality, Settings entry point.
</objective>

<execution_context>
@/Users/ejasper/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ejasper/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/42-referral-system/42-01-SUMMARY.md

@src/screens/Settings.tsx
@src/screens/WeeklyReportScreen.tsx
@src/components/share/ShareBottomSheet.tsx
@src/lib/shareCard.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create RecruitScreen with referral link and share functionality</name>
  <files>src/screens/RecruitScreen.tsx</files>
  <action>
Create `src/screens/RecruitScreen.tsx` following obsidian/gold styling from WeeklyReportScreen.

Layout:
1. **Header** (top section):
   - AppHeader component with back button
   - Title: "RECRUIT A SUB" (uppercase, Oswald font, tracking-wider)
   - Subtitle: "Share your referral link to recruit new members"

2. **Referral Link Card** (main card):
   - Surface background (#1A1A1A), gold border (#C9A84C/30)
   - Display link: `app.welltrained.fitness/join/{CODE}`
   - Link text should be selectable, gold color
   - "COPY LINK" button (primary CTA, gold background)
     - Uses `navigator.clipboard.writeText()`
     - Shows toast "Link copied!" on success
   - Copy icon (Lucide Copy) next to button

3. **Share Buttons Row** (horizontal):
   - Three buttons: Instagram, X (Twitter), Messages
   - Use Lucide icons: Instagram, Twitter, MessageCircle
   - Each button triggers native share with pre-filled text:
     - Text: "Join me on WellTrained - the fitness app that trains you like a champion. Use my link: {FULL_URL}"
   - Use @capacitor/share for native, window.open fallback for web
   - Style: outline buttons with gold icons

4. **Recruits Section** (below share):
   - Section header: "YOUR RECRUITS" with count badge
   - If no recruits: empty state message "No recruits yet. Share your link to get started!"
   - If has recruits: list of RecruitCard components

5. **RecruitCard** (for each recruit):
   - Horizontal layout with avatar placeholder (initials or icon)
   - Callsign (username or "Anonymous")
   - Rank name (from RANK_INFO using rank number)
   - Status badge: "Pending" (muted) or "Completed" (gold)
   - DP earned: "+0 DP" (pending) or "+100 DP" (completed, gold text)

Hooks:
- useReferralStore for referralCode, recruits, fetchReferralCode, fetchRecruits
- useEffect to fetch code and recruits on mount
- Show loading skeleton while fetching

Use RANK_INFO from @/design/constants to convert rank number to name.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Check RecruitScreen renders with link card and share buttons</manual>
  </verify>
  <done>RecruitScreen displays referral link, has copy button, share buttons for Instagram/X/Messages, and shows recruits list with status and DP.</done>
</task>

<task type="auto">
  <name>Task 2: Add route and Settings navigation entry</name>
  <files>src/App.tsx, src/screens/Settings.tsx</files>
  <action>
**App.tsx:**
1. Add lazy import for RecruitScreen: `const RecruitScreen = lazy(() => import('@/screens/RecruitScreen').then(m => ({ default: m.RecruitScreen })))`
2. Add route in authenticated section: `<Route path="/recruit" element={<RecruitScreen />} />`

**Settings.tsx:**
1. Find the "Protocol Reminders" Card section (around line 864)
2. Add a new Card BEFORE "Protocol Reminders" for "PROTOCOL" section
3. Add navigation row for "Recruit a Sub":
   - Icon: Users (from lucide-react)
   - Label: "Recruit a Sub"
   - Description: "Earn 100 DP when recruits complete their first week"
   - ChevronRight indicator
   - onClick: `navigate('/recruit')`

Style the row like Settings navigation entries (flex, gap, hover state).

Import Users icon from lucide-react.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Navigate to Settings, tap "Recruit a Sub", verify screen opens</manual>
  </verify>
  <done>RecruitScreen accessible via /recruit route. Settings shows "Recruit a Sub" entry with correct navigation.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. RecruitScreen component renders at /recruit route
3. Copy button copies link to clipboard and shows toast
4. Share buttons trigger native share sheet (or web fallback)
5. Recruits list displays with correct status and DP info
6. Settings has "Recruit a Sub" row that navigates to /recruit
</verification>

<success_criteria>
- Recruit a Sub screen shows user's referral link prominently
- Copy button works and shows feedback
- Instagram, X, Messages share buttons functional
- Recruits list shows callsign, rank, status (pending/completed), DP earned
- Settings has "Recruit a Sub" entry under Protocol section
</success_criteria>

<output>
After completion, create `.planning/phases/42-referral-system/42-02-SUMMARY.md`
</output>
